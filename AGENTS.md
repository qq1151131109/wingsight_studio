<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Wingsight Studio

AI 影视创作无限画布工作台：React Flow 画布 + CopilotKit 聊天 + LangGraph 主 Agent（Python）+ Langflow 技能。设计系统移植自 juben（米黄纸感 oklch token，见 `app/globals.css` 顶部 `@theme`）。

## 结构

- `app/` Next.js 16 前端（Turbopack）。`agent-provider.tsx` 注册自管 Agent；`page.tsx` = 活动栏 + 画布 + 聊天侧栏
- `components/canvas/` 画布（`CanvasView`、`nodes.tsx` 自定义卡、`CanvasShortcuts` 撤销/粘贴、`AssetTray` 素材库面板 + 自动入库、`DirectorPanel` 导演台（语汇来自 `agent/camera.py` 经 `/agent-service/camera-vocab` 下发，编译成 body 的【摄影】段）、`MentionInput` @ 引用内联 chip 编辑器——画布面板与聊天侧栏同款，token 存节点 id、提交时才编号 图N，chip 点击定位卡片、改名/删除实时同步）；`components/copilot/` 桥接（`CanvasAgentBridge` 读写通道、`ProjectManager` 项目同步、`ChatInput` 聊天输入）；`components/shell/ActivityBar`
- `lib/canvas/store.ts` zustand 画布状态（**服务端为唯一事实源**，localStorage 仅离线缓存；`addNode` 自动从 `data.nodeType` 推导 `node.type`，漏推导会渲染成空白默认节点）；`lib/canvas/ops.ts` canvas_ops 契约与校验
- `agent/` Python 服务（FastAPI + LangGraph + ag-ui-langgraph，uv 管理）：`main.py` 路由与端点、`graph.py` 主图与系统提示、`skills.py` Langflow 调用（v1 阻塞 API + tweaks）、`models.py` 出图/文本模型目录（唯一事实源，`GET /models/*` 下发）、`projects.py` 项目/画布/聊天/素材库 SQLite、`compose.py` ffmpeg 视频拼接（compose 卡直连，不经 LLM）、`research.py`/`imgresearch.py` 深度调研与参考图调研、`eventbus.py` 后台任务事件总线（SSE）+ `jobstore.py`/`imagejobs.py` 长任务持久化与重启恢复、`prompts/` 宪法与系统提示、`skills/` 技能手册（SKILL.md）、`auth*.py` 认证、`camera.py` 摄影语汇库
- `langflow/` langflow 引擎源码（自有 fork 经 `git subtree --squash` 并入，含 DMXAPI 出图修复、batch_asset_sheet 定制等补丁；已 tsconfig/eslint exclude）。重建环境：`./scripts/setup-langflow.sh`；更新 fork：`git subtree pull --prefix=langflow langflow main --squash`
- `scripts/` 端到端测试（node 集成测试 + auth 冒烟）+ `setup-langflow.sh`（langflow 环境重建 + flows 导入 + flow id 回写）
- `doc/` 设计调研与对齐笔记（agent 抗懒条款、长对话导航、system prompt / harness 差距对比等横评；非代码，供设计决策溯源）

## 标准制作链 v2（2026-09-11 用户定案：分支 + 资产考据站 + 出图三跳）

`剧本落卡 → ［用户选］先拆分镜？ → ［题材判定］真实题材自动走资产考据 → 出图三跳`，四层同源（宪法「链路一」/ `agent/skills/script-to-assets/SKILL.md` 标准制作链 / `asset-aware-generation` 第 5 条 / 工具描述）。**分支是用户的决定**：只给素材时问一句带默认（默认先拆资产），用户说了顺序就照做。**「分镜先」**= generate_storyboard 不带 `assets_json` → 再 decompose_script(剧本原文 + 分镜行文本)；行给「本镜有什么」的清单、剧本给「长什么样」的依据——**只给分镜行会让拆解 flow 按「合理补全」条款编造外观与造型计划，编造会一路进到出图提示词当事实**。**「资产先」（默认）**= 拆资产建卡（fromId=剧本卡id、占位符同批连）→ generate_storyboard 带资产名单（名单内引用 + 名单外幻觉名剔除）。**调研站 = 资产考据（参考图 research_asset_references / 考证大纲），真实题材自动发起不请示**；**史实深度调研 start_deep_research 永远由用户发起**——混用即 a768423e2069 事故（本轮实测踩过：加「调研自动发起」措辞后「直接开始制作」直接抢答 start_deep_research+confirm，intent-routing 掉到 12/14；把两义写进决策原则 1 + 三个工具 docstring 后 14/14，且轮2 正确走成「考证大纲」）。**出图三跳硬序**：资产设定图 → 造型图 → 分镜镜头图。

配套机械修复（同轮）：
- **服务端携参考出图**：`skills._canvas_ref_cards`（读 `canvases.edges`，取 `e.target=资产卡` 的上游带图卡；`refSource==="research"` → 标签 `reference`，否则按 `nodeType`——母场景变体走这条；上限 4 与前端 `fillAssetImages` 同口径）+ `_inject_canvas_refs` 接进 `generate_asset_images`（只补空载荷，调用方显式给的参考优先）。此前**服务端完全不读 edges**，「调研采纳的参考图 → 聊天出图」这条链是断的（唯一实现在前端 `fillAssetImages`）。同时修掉「母场景图自动进参考」这个只在按钮路径成立的承诺。
- **造型图 agent 工具** `generate_look_images`（`graph.py`，后端工具第 21 个）：`skills.generate_look_images` 出图（载荷与前端 `fillLookImages` **逐字同源**：`rid={charId}#look{idx}`、refs=[定妆照,服饰图]、`assetType:"none"` 单幅版式自述、params 取卡级 gen 不硬编码 aspect、考据简报经 `#` 前缀注入）+ `_build_look_card_ops` 返落卡 ops（造型卡摆角色卡右侧、粗碰撞整块下移；角色/服饰→造型卡连线；角色卡 `looks` 回填 imageUrl/nodeId；≥2 张收「造型图」组框）。**ops.ts 的 update_node 造型账收产物字段、add_node 仍拒收**（`sanitizeLooks(withProduct)`：计划不许自带产物，但服务端回填必须能写进去，否则每次装载重复物化造型卡）。
- **decompose_script 补 existing**：聊天路径此前只传 script，重拆必重复建卡/名字漂移；现按 `_canvas_asset_roster` 现读画布当名单。拆解输入标签从写死的「剧本：」改中性「创作素材（剧本原文与／或分镜表行）：」。
- **shotlist flow 无名单也申报**：原 prompt 要求「名单为空给 `[]`」，而 `missing`（画布缺的资产）的判据正是行内 assets 名不在名单里——**这一条把分镜先分支的缺卡回路关死了**。改为「没有名单时按剧本原名申报本镜资产」，实活实测：无名单跑同一剧本报出 8 个缺卡名（冯太后/御书房/密诏/小皇帝/百官/朝堂/十二旒朝服/玉印），带名单时 missing=[] 且行内 `@名称` 引用正常。拆解 flow 台词同步中性化 + scene flow 加「补全部分必须标明是推断」（实测产出「位于宫城后部（推断）」）。
- **资产卡落卡 ops `_build_asset_card_ops`（2026-09-11 091101 事故：对话框说生成完了、画布一片空白）**：聊天侧 `generate_asset_images` 此前**只返回 image_url 文本 + 一段「把 finalPrompt 写进 genShot」的附录**——落卡全靠模型手抄几十条 `update_node`。实活取证（checkpoint 里的工具调用）：52 张图分两批出完，模型第三轮才手写 52 条 ops（`applied:52`），中间那轮它已经在聊天里口播「角色定妆与场景空镜都落在卡上了」——**用户看到的就是「对话框说生成了、画布还全是空卡」**；根因是返回文本里没有任何「尚未落卡」的信号，模型把「图出了」当「卡有了」。分镜图有 `_build_shot_card_ops`、造型图有 `_build_look_card_ops`，最常用的资产设定图这条路不能是空白。修法：`skills.generate_asset_images` 的结构化结果补落卡身份与审计快照（`nodeId`/`assetType`/`shotlistId`/`description`/注入后的 `visualNotes`/`referenceImages`/`referenceLabels`/`aspect`），`_build_asset_card_ops` 据此产 `update_node`（`imageUrl` + `status:"ready"` + `errorMessage:""` + `genPrompt`=用户原文 + `genShot`（含 `finalPrompt`/`researchNote`）+ `styleSnapshot`——与前端「补资产图」同语义）。**身份解析**：`node_id` 优先且必须真落在画布资产卡上，否则标题精确匹配，再退一步**去空白归一**匹配（场景名带空格，模型常写成「长安后宫祈福殿」）；分镜图（`assetType==="shot"` 或带 `shotlistId`）不进资产账（那是图卡的路）；两条都落空的进 `unresolved` 告警并把 genShot 交回 agent 建卡时带。**失败项只在卡上还没图时才写 error 态**——否则一次重出失败就把卡上原有的图换成重试面板（图被藏起来比报错更难查）。返回串新增「**先经 canvas_ops 原样应用整批 ops，再向用户汇报**」，工具 docstring 同步（旧附录只在 `unresolved` 时出现，不再邀请模型手写）。回归：`test_asset_research_brief.py` K/L 段 + 工具接线段（共 127 项；含标题归一/missing 分支/失败不藏图/分镜不进账）。
- 回归：`cd agent && uv run python test_asset_research_brief.py`（127 项，含参考注入器/造型图资格筛选/protocol 同源/落卡 ops 形状/资产卡回填）、`pnpm dlx tsx scripts/canvas-read-channel-test.mjs`（73 项，含造型账产物回填双向语义；**这个脚本 import 的是 .ts，必须走 tsx，裸 node 会 ERR_MODULE_NOT_FOUND**）、`script-to-canvas`（**重写手册时漏掉「fromId=剧本卡id」那句会把连线全打断——实活踩过，别删**）、`intent-routing` 14/14、`skill-routing` 15/15。

## 常用命令

```bash
./start_wingsight.sh            # 一键 start/stop/status（agent:8123 + 前端:8008，日志在 logs/）
pnpm dev --port 8008            # 前端
cd agent && uv run uvicorn main:app --port 8123 --host 127.0.0.1   # agent
pnpm exec tsc --noEmit && pnpm exec eslint components lib app       # 检查
node scripts/agui-client-test.mjs               # 两轮工具调用闭环（需 agent 在跑）
node scripts/script-to-canvas-test.mjs          # 剧本→建卡全链路
node scripts/card-editing-test.mjs              # 卡内文本编辑回归（光标/IME 组合/外部改值回写）
node scripts/version-history-test.mjs           # 版本历史弹窗回归（portal 防劫持/大图预览/A·B 对比/回滚契约）
node scripts/ref-pipeline-test.mjs              # 参考图管线回归（sanitize 占位标题/@ 候选已连线置顶/计数口径/契约推断 mock 出图）
node scripts/ref-outline-api-test.mjs           # 考证报告/大纲服务端契约（报告分节/cardBriefs 合成/大纲整份替换与防幻觉 nodeIds/主题分发；真 agent 无 LLM 无浏览器）
node scripts/ref-report-reconcile-test.mjs      # 调研产物对账回归（条目→画布：简报落资产卡/采纳图物化参考卡/报告卡与大纲卡单例幂等/era 口径不被前端保存周期抹掉；无 LLM）
cd agent && uv run python test_ref_report.py    # 考据条目与报告单测（归属键/重跑覆盖/大纲/reused 活引用/被主题覆盖不计待补；无 LLM）
# 注：start_wingsight.sh 的 stop 现在按端口（lsof → ss 兜底）收进程并 TERM→5s→KILL 升级 + 收完核实端口真空——旧实现只用 ss（macOS 没有）导致 stop 空转「以为重启了其实还是旧进程」，agent 带常开 SSE 时 TERM 也收不掉（会留下不带端口的僵尸进程同写一个 SQLite）。
# 注：用 window.__wsCanvasStore / __wsSetViewport 的浏览器回归（ref-pipeline / mention-gaps 等）**需要前端跑 dev 模式**——调试钩子只在 NODE_ENV!==production 时挂（CanvasView.tsx），生产模式跑会「Cannot read properties of undefined」。新写 E2E 尽量不依赖这些钩子（走 HTTP + DOM 断言，如 ref-report-reconcile-test）。
node scripts/shotref-binding-test.mjs           # 分镜行→资产引用解析 + 出图软闸点名（mock 出图）
node scripts/chat-storyboard-test.mjs           # 聊天侧整表分镜回归（generate_storyboard 工具路由/写回已有分镜表/不铺独立分镜卡/lighting·sound 透传；真跑 flow+LLM）
cd agent && uv run python ../scripts/shot-card-ops-test.py   # 聊天侧分镜图落卡 ops（shotlist_id/rid 绑定→图卡+连线+行挂载；python 直连工具真出 1 图，约 1 分钟）
node scripts/chat-autotitle-test.mjs           # 会话智能命名闭环（首条截断→LLM 升级→用户命名不覆盖；真跑 LLM，API 级无浏览器）
# 会话标题两连修（2026-09-12「输出完突然没了 + 标题不对」实报）：①纯附件消息（S1 上传无指令）的机械标题直接切首条前 18 字，把上下文界标切出来当标题（「<<<WS-CTX>>> （用户未」）——`projects._title_source_text`/`_fallback_title` 先剥壳：带界标取显示段、纯附件退 manifest 首附件名（「屈原与端午.txt」）；`thread_title_is_mechanical` 补「== _fallback_title 产物」分支（界标消息正文以界标开头，startswith 判不出来，LLM 升级会被闸死）。②**LLM 智能命名几秒~几十秒后才落库，而页签列表只在 threadId 变化时拉一次——升级永不回显，刷新前一直是机械标题**（这就是用户「标题不太对」的主体验；①只是让它格外刺眼）。修法：`list_threads` 随列表下发 `title_mechanical`（服务端权威判定，前端无法自行复算附件名截断），ThreadsBar 见机械标题就 5s 重拉直至升级落地（60s 封顶，正经标题零额外请求）。**排查侧教训**：「消息消失」实报最终证伪——agent 只跑了一轮、两条消息落库完好、刷新即恢复，忠实复刻（切项目→传文档→发送→点页签→60s 采样）无法重现；排查时 agent.log 全是访问行、checkpoint 是唯一真相源（writes 表解码 human/ai/tool 全序列）、telemetry 的 data-track 只记点击（打字/回车无痕）。别急着改代码，先证明现象可复现。**同日输入框红框**：聊天编辑器聚焦的 2px 朱红内环来自全局键盘焦点环规则（`:focus-visible` 对 `[contenteditable]` 也生效且输入类 focus-visible≡focus=打字全程亮着）——contenteditable 聊天编辑器移出该规则（聚焦示意由 `.copilotKitInput:focus-within` 边线加深承担，accent 光晕同步撤掉），input/textarea 保留环但改中性墨色（text-3）；改焦点样式必须同时看 computed outline 与截图，`outline-none` 类压不住更晚的 author 规则
# 对话区「一直在刷新 / 输出突然没了」根因与修复（2026-09-12 定案 + 修复，dev 模式事件级取证）：**AG-UI 每个 run 结束整表替换消息**。事件序列恒为 `RUN_STARTED → STATE_SNAPSHOT → MESSAGES_SNAPSHOT → RUN_FINISHED`，`MESSAGES_SNAPSHOT` 让 @ag-ui/client 整表替换 `agent.messages`；而流式消息 id（langchain_core 按 run 铸造 `lc_run--*`）与快照消息 id（graph state 落库后的 id）**天然不同源**——同一条消息换 id = React 换 key = 气泡卸载重建；一轮对话 10+ 个 run（**每个前端工具调用就是一个 run 边界**：agent 停下等客户端执行 canvas_ops 再续跑），用户看到的就是「执行任务时对话区反复刷新」；本地-only 瞬时消息（reasoning 思考行、progress_* 播报）不在快照里，整表替换时被删——「输出完突然没了」。**修复 = `lib/chat/snapshotStability.ts` 快照稳定中间件**（`attachSnapshotStability(langgraphAgent)` 在 agent-provider 挂载；`HttpAgent.use` 收 `(input, next) => next.run(...).pipe(map(...))`，事件在进 apply 写 agent.messages 之前被改写）：①**id 对齐**——快照消息与本地消息按「角色+正文+工具调用签名」（`keyOf`）匹配，命中沿用本地 id（React 不换 key，气泡原地更新）；②**瞬时回插**——本地 reasoning / progress_* 不在快照时按「其后首条正式消息」锚点插回（尾部瞬时追加末尾），思考行与播报跨 run 存活，刷新页面仍消失（ChatPersistence 不落库）；③**出站 id 还原**——对齐命中记 localId→serverId 映射，run input 发出前翻译回服务端 id（ag_ui_langgraph 按 id 与 checkpoint 合并历史，收到本地 id 会当新消息**重复插入**，重复对把带 reasoning 的正身挤掉 → DeepSeek 400「reasoning_content must be passed back」，实测踩过）；④**progress_* 播报不回传**（无 reasoning 的 assistant 历史 → DeepSeek 思考模式同款 400——这就是 chat-collapse-ux E6/E7「DeepSeek reasoning_content 400 留档待查」的真身：修复前 progress 偶然存活于 input 偶发触发，修复回插后稳定暴露，现根治）；reasoning **必须**回传（服务端靠它重建 AIMessage.reasoning_content，DeepSeek 硬要求，过滤不得）。守卫：首条 user 内容一致才稳定化（线程切换/跨会话原样放行）。**同 run 边界的两面性**：UI 侧 id 要稳定（本地 id 不变），协议侧 id 要还原（服务端认识）——两个方向都不能省。回归：`pnpm dlx tsx scripts/snapshot-stability-test.mjs`（17 项纯函数：对齐/回插/守卫/还原/播报过滤/不重复认领）+ dev 端到端（drops 恒零、peak=final、无 400）；`pnpm add rxjs` 是中间件 pipe(map) 的直依赖（版本与 @copilotkit 同源 7.8.1）。**取证工具**（dev-only，生产不暴露）：`window.__wsAgent`（agent-provider，订阅 AG-UI 事件流——`subscribe({onEvent})` 可打事件类型 + `agent.messages` 快照，定位替换时刻的唯一手段）、`[session] setThreadId` 调用栈打印（lib/chat/session.ts）、`[persist] hydrate SKIP/ABORT/OVERWRITE` 三态（ChatPersistence）；**测这类问题必须跑 dev 模式**（生产构建把这些日志全 strip 了，且 `__wsAgent` 不挂）——生产 8008 只能看现象，看不到因果。**已排除的嫌疑**（别再查这几条）：整页刷新（framenavigated 恒定、canvas 只拉 5 次）、409 合并风暴（0 次）、SSE 断连（29 次/全天）、水合覆盖（`[persist] hydrate SKIP` 生效、无 OVERWRITE）、threadId 漂移（`setThreadId` 全天仅进项目 null + 首存 tid 两次）
pnpm dlx tsx scripts/canvas-read-channel-test.mjs # 画布读通道 v2 回归（摘要索引化/锚点不丢/validateOps 干跑/applyOps 同批建卡即连线/文本卡尺寸分档；纯函数无 LLM）
node scripts/capabilities-panel-test.mjs         # 助手能力面板回归（三分区/手册展开/示例句插入输入条；无 LLM 约 15s）
node scripts/chat-skill-invoke-test.mjs       # 技能点名调用闭环（「按此技能处理」→ read_skill 工具卡 → 照手册作答；真跑 LLM 约 1 分钟）
node scripts/skill-routing-test.mjs           # 手册触发 + 入口行为回归（R1-R3 三手册触发 / R4 只给题材→先问意图不出清单 / R5 虚构开放想法→brainstorming 且不动手 / R6 探索委托→先铺开再收敛；真跑 LLM 约 5-6 分钟）
node scripts/tour-depth-test.mjs [题材词]      # 主题导览深度探针（真 LLM 无浏览器约 1 分钟）：判读分三级（硬失败：没读手册/一篇原文没读 → 警告：骨架块覆盖欠账 → 达标），打印工具调用序列/骨架块数/过程叙述与最终答复字数（分开量）/钩子候选行/全文供人工判读。**验不了两件事**：钩子是否真有出处（那是资料核查的活）、有没有违规铺卡（不放前端工具，该口径归 skill-routing R4）
node scripts/intent-routing-test.mjs          # 意图路由场景组（S1 上传无指令→带默认问/S2 直接制作→标准链/S3 调研二义/S4 改道/S5 调研范围默认全量；真跑 LLM+拆解/分镜 flow 约 6-8 分钟）
node scripts/style-picker-handoff-test.mjs    # 画风选择回传（open_style_picker 等待式：选完 agent 同轮知道所选；关面板=未选择收尾；真跑 LLM，Playwright 约 2 分钟）
node scripts/ref-adopt-tool-test.mjs          # 参考图自动采纳闭环（调研完成即自动采纳 top-3→「带 5 张」调整补齐+SSE 终态事件断言；真跑 LLM+搜图约 5 分钟）
node scripts/agent-events-ui-test.mjs         # 后台任务事件流浏览器闭环（SSE 连通/调研完成自动续跑「任务通知」/出图只通知不插话；真跑 LLM，Playwright 约 2 分钟）
node scripts/free-image-studio-test.mjs             # 自由生图工作台回归（A 组 API 真出 1 图+@图N 注解/多模型批次/错误 400；B 组 Playwright mock 出图：表单/画廊/复用参考/@ 弹层/回填；SKIP_API=1 FIXED_IMAGE=<url> 可只跑 B 组）
node scripts/asset-create-test.mjs              # 手动建资产卡（空名聚焦/同名提醒/AI 写设定/素材库建为资产）
node scripts/shotlist-resume-compose-test.mjs   # 分镜表断点恢复/补缺图/一键成片（自建测试项目+mock，不出真实图）
node scripts/shot-video-test.mjs               # 分镜参考生视频全链（RunningHub H3；mock 12 断言；REAL=1 追加 1 条真出，走 RunningHub 额度）
cd agent && uv run python ../scripts/job-recovery-test.py   # 任务持久化/重启恢复回归（jobstore 孤儿终态化/拆解 partial 收回/启动清扫/HTTP 轮询回退；无 LLM）
node scripts/script-review-test.mjs             # 剧本审查三维度回归（真跑 flow+Serper，需 agent+langflow 在跑，约 1 分钟）
node scripts/card-export-test.mjs               # 文本类卡导出回归（txt/md/docx；docx 解包断言横版表格/表头重复）
node scripts/doc-fullscreen-test.mjs            # 卡片全屏文档模式回归（工具条入口/聚焦文末/卡面实时同源/Esc/落库/衬线/万字长文；无 LLM 约 40s）
node scripts/node-toolbar-select-test.mjs       # 卡上工具条显隐语义回归（单选 1 条·全选清零·多选工具条在位·清空·回落单选；纯 DOM 断言不依赖 dev 钩子，生产实例可跑；WS_BASE 可指别的实例）
node scripts/image-node-ops-test.mjs            # 图片节点操作层回归（右键图片段/裁剪原位替换+版本+撤销/机位球控+打光光位球全交互弹窗建卡连线+画风闸/自由缩放/宫格导出/加号手柄/全景环视 2:1 出卡+PSV 灯箱环视；mock 出图，裁剪走真上传；79 断言自删项目）
node scripts/sidebar-width-reserve-test.mjs      # 工作台布局/口径回归（34 项：侧栏让位与拖宽回灌同源/底坞模型与画风标签说人话+目录 500 明报/空画布不渲染小地图/窄窗口侧栏 clamp（装载·拖拽·resize 同口径）下画布保住 420/顶栏右组不折行/工具条齿轮不越界；自建临时项目结束删除，无 LLM）
node scripts/chat-spacing-test.mjs               # 聊天间距回归（10 项：消息边界恒 20px/段内恒 16/typography 幻影已掐/纯卡消息空 prose 不占槽/横向内缩「左 20 / 右 42」/气泡 88%；真发三轮真跑 LLM，自删项目）
# 思考行（reasoning UI，2026-09-11「跳来跳去」修复）：单行贯穿、原地替换——v2 默认 CopilotChatReasoningMessage 是 `isOpen(isStreaming)`：流式强制展开全文（越滚越长）、结束自动折叠（高度 N 行→1 行，消息区整段上弹）。自绘 `components/copilot/ReasoningMessage.tsx` 经 Sidebar `messageView.reasoningMessage` 槽位替换（v2 槽位链：role:"reasoning" 消息 → MemoizedReasoningMessage → 组件 props {message, messages, isRunning}）。范式 = opencode TUI ReasoningPart（源码注释原话 "a single line throughout, so the layout never shifts"）：全生命周期只占一个固定单行——流式 `spinner+实时标题`（标题 = reasoning 文本的首个 **加粗**/标题行，否则最后一个非空行——codex latest_summary_line / gemini parseThought 三家同思路正则提取，零额外调用），结束同一行原位变「已思考 · N 秒 · 标题」；点击展开正文（max-h-64 滚动，不弹布局）；展开态不落 effect setState（React Compiler 禁）——记 {id, stream} 于 state、渲染期派生，新一轮流式自动回折叠基线。事件链：DeepSeek `additional_kwargs.reasoning_content` → `_OneShotToolArgsCompatChatOpenAI` 捡回（langchain-openai 1.6 会丢）→ ag_ui_langgraph REASONING_MESSAGE_* SSE → @ag-ui/client 落 role:"reasoning" 消息。**历史不回放思考**（ChatPersistence 只存 user/assistant）——实时轮可见、刷新即逝，维持现状（要回放得让 reasoning 落库，另议）。CSS：`.ws-reasoning` 与 v2 `cpk:my-1` 同规则掐自我 margin。验收口径：Playwright 300ms 采样思考行高度全程恒定（实测 22 采样恒 24px）+ 流式/收尾两态都出现 + 展开收起可用。**已知待议**：DeepSeek 思考语言是英文（实测 "The user asks…"），中文产品里标题行突兀——思考语言归 provider（系统提示管不到 reasoning_content），要在意得换模型或在桥层过滤，用户未拍板勿动
pnpm dlx tsx scripts/snapshot-stability-test.mjs  # 快照稳定中间件纯函数回归（id 对齐/瞬时回插/守卫/出站 id 还原/progress 播报过滤/不重复认领；17 项无 LLM——对话区反复刷新根治，背景见上方「对话区一直在刷新」段）
pnpm dlx tsx scripts/run-retry-test.mjs       # run 级自动重试纯函数回归（停止/卸载不重试、传输层可重试、退避表 1s→2s 封顶 5s、流式残片剥离；14 项无 LLM——传输中断自动重连，背景见下方「run 级重试」段）
# run 级自动重试（2026-09-12 P0，业界对比定差距后补齐——codex 流 5 次指数退避 + 连接级无限重试 + WS→HTTPS 传输降级、opencode HTTP 层 2 次、gemini-cli 10 次封顶，我们此前为零，一次 SSE 截断就把整轮打成「本次响应出错」）：`lib/chat/runRetry.ts` `attachRunRetry(langgraphAgent)`（**后挂 = 更外层**，错误经快照中间件透传后才被 retry 接住，重连时内层 defer 链随之重发）。**通道语义即重试边界**（不判状态码）：observable error 通道 = 传输层失败（fetch 拒绝/SSE 截断）→ 重试安全（LangGraph 从 checkpoint 续跑，已完成工具不重执行，最多重跑未完成那步 LLM 调用）；RUN_ERROR 事件 = 服务端已判定的语义失败（DeepSeek 400 类确定性错误）走事件通道不走 error 通道，天然不重试照常报错。停止/卸载（AbortError/unmount）不重试——重试它们等于拒绝停止。重试前剥掉本次失败尝试的流式残片（run 开始后新出现的消息，`keepBaselineMessages`），否则与重连成功后的同内容新 id 消息重复成两条气泡；toast「连接中断，N 秒后自动重连（第 n/2 次）」对齐 codex「Reconnecting n/m」。退避 1s→2s（封顶 5s，同机代理无需长退避），共 3 次尝试。E2E 手法：Playwright `route.abort("connectionreset")` 掐断首个 `**/agent-service` POST 后放行——断言 toast 出现、无错误横幅、重连后拿全回复（实测 2s 内）。**中间件挂载顺序**：snapshotStability 先挂、runRetry 后挂（use 后挂者在最外层）
# 进度播报迁出消息流（2026-09-12 P1，业界共识「进度是 composer 旁的状态行，不进 transcript」——codex status_indicator / opencode 单行状态 / gemini-cli spinner 三家同款）：`skills._emit_progress` 从 `manually_emit_message`（落 progress_* 聊天消息）改为 **eventbus 广播 kind="progress"**（job_id=thread_id、project_id 空、title/summary=消息文本，无订阅者空操作）；前端 `AgentJobKind` 加 `"progress"`，TaskEvents 按 `agentThreadId === e.job_id` 过滤渲染浮条（8s 自动收起，跨会话不串）；ResearchNotice 删聊天插消息段（浮条保留，「助手自知终态」由 agent 查 chat_jobs 达成）。**服务端只改 _emit_progress 一个函数体，六个调用点不动**。消息流因此彻底无 progress：AssistantMessage「只渲染最新一条」与快照稳定/重试中间件的 progress 处理**保留作防御**（ResearchNotice 类客户端插入若回归仍兜得住）。E2E 判据踩坑：工具卡执行态行（toolCards RunningRow「正在拆解剧本…」）渲染在消息气泡内属**合法 UI**，别拿文本正则判迁移成败——硬判据是 `[data-ws-msg-id^="progress_"]` 容器为零 + 浮条（`.ws-toast-in.fixed.bottom-16`）出现。回归：agent-events-ui 8/8、chat-interrupt-queue 14/14
# reasoning 落库回放（2026-09-12 P1，业界对比第二差距——codex rollout 把 ReasoningItem 写会话文件 resume 全量回放、opencode 的 reasoning 是消息持久化 part 随 SQLite 回放，我们此前刷新即逝）：ChatPersistence.toRecords 与 `projects.save_chat_messages` 的 role 白名单加 `"reasoning"`（progress_* 仍不落库）；回放消息无计时信息 → ReasoningMessage 结束态 label 只在 `elapsed>0`（本会话真实流式过）才带「N 秒」，回放只显示「已思考 · 标题」；导出 Markdown 跳过 reasoning（对齐气泡「过程折叠」口径）。**回放的 reasoning 会随下一轮 run input 回传服务端**——与快照稳定的 reasoning 回传语义一致（DeepSeek 思考模式硬要求 reasoning_content 在场，`_sanitize_messages_for_model` 已按 provider 分流：DeepSeek 回传/GLM 剥除），顺带消灭「刷新后继续追问缺 reasoning → DeepSeek 400」的潜在毒化路径。E2E（真跑 LLM）：建卡轮（思考行峰值 2）→ reload 断言思考行 2 条带标题回放 → 同会话追问一轮断言无 400 正常落卡。**水合只回放 UI**：模型上下文以服务端 checkpoint 为准，回放消息 id 与 checkpoint id 经 ag_ui_langgraph 的 echoed-history 合并对齐（同既有刷新后续跑路径，fork/分支测试覆盖）
# 确定性循环检测 + 步数宽限收尾（2026-09-12 P2，LLM 自察不了循环——同工具同参数原样重发一路烧到 recursion_limit，GraphRecursionError 把本轮全部产出与叙事一起报废；codex/opencode 均有同类确定性守卫）：`graph.py` 两级守卫。**循环检测**（`_consecutive_repeat` 尾部连续同签名计数，签名=`_tool_call_sig` 名称+键排序参数 JSON）：只覆盖纯后端调用（tool_node 循环才累积步数；**recursion_limit 按 invocation 计**，前端工具续跑每 run 重置，不构成递归风险）；连续第 3 次同参 → 拦下不执行 + 合成 ToolMessage 反思提醒（换参数/换工具/读错误换路径/等后台任务就 END 等通知）+ `Command(goto="chat_node")` 自环重跑（模型仍持有工具）；容忍 2 次是给瞬时故障原样重试留余地（出图 503 重掷同参合理）；提醒后仍第 4 次 → 止损：再拦 + 尾部留 `[循环止损]` 标记 ToolMessage，下一跳 `_pending_stop_loss`（**只认 messages 尾部**——标记只存在于自环交接瞬间，历史中部旧标记属已结束轮次，后台任务续跑等新 invocation 不能被误伤成纯文字模式）触发**解绑工具**强制纯文字收尾——解绑后物理上无法再调用，循环结构上终结。穿插异参/其它工具天然断档（validate→ops→validate 复检不算循环），跨轮 Human 断档。**宽限收尾**（`_step_budget` 读节点 config 的 `metadata.langgraph_step`——langgraph 1.2 实证步号从 1 起、节点跑到 limit 本身不炸、炸的是还想调度 limit+1）：距 recursion_limit 还有 8 个 superstep（`_CLOSE_MARGIN`，一次 chat+tool 往返=2 步，留约 3-4 次工具余量）注入软收尾指令；**最后 2 步（`_HARD_CLOSE_MARGIN`）硬收尾**——chat(limit-2) 再发工具调用则 tool(limit-1)→回 chat(limit+1) 必炸，此后解绑工具+「必须现在收尾」指令；收尾指令与 nudge 同范式**只进本次请求不落 checkpoint**，每跳按最新步数重注入。遥测：agent.step 加 `consec_repeat`，拦截/止损发 `agent.loop_guard`（level/tool/consec）。合成 ToolMessage 走正常交替（配对 tool_call_id），DeepSeek reasoning 回传不受影响（response 保留 additional_kwargs）。回归：`cd agent && uv run python test_loop_guard.py`（59 项：A 纯函数签名/计数断档/止损标记/步数预算 20 + B 图级真 chat_node 假模型 39——B1 同参×4 全链两拦一解绑、B2 limit=12 宽限收尾不抛递归异常、B3 异参零误伤、B4 前端调用不进账、B5 混合调用前端优先路由守卫不添乱、B6 多后端调用违规只拦首个其余 sanitize 占位、B8 止损 END 后同 thread 新 run 恢复正常绑定）；agui-client-test 冒烟通过
node scripts/chat-interrupt-queue-test.mjs      # 聊天引导/打断回归（运行中回车排队 chips 可撤回/本轮结束自动排水/停止落中断标记（中性气泡）/停止后排队接续/标记落 transcript；route mock SSE 无 LLM 约 60s，自删项目）
node scripts/chat-longcontent-test.mjs          # 长内容 UX 回归（助手消息复制/重新生成钮在位·复制写剪贴板/长回复折叠 480px（实测溢出 ≥1.6 倍才折）/展开收起/搜索计数·高亮·Enter 跳转·Esc 清高亮/轮次点命中区 24px 且让开滚动条/重新生成→fork 端点→截断重跑；浏览器段 mock SSE + E 组真跑 LLM 验 fork 语义，自删项目）
node scripts/turn-locator-test.mjs               # 对话轮次索引回归（juben TurnLocator：点数过滤系统通知/贴边/悬停面板/跳转滚动/闪圈/回底/scroll-sync 当前轮实心·末轮空心环/Alt+↓·Alt+End 轮次导航 27 项；自建项目+合成会话不跑 LLM，约 30s）
node scripts/attach-race-test.mjs               # 附件回归（① 13 张一次拖入不截断 ② 上传中 Enter 发送不丢图：route 延迟制造竞态窗，落库带 WS_PARTS envelope+缩略图；真跑一轮 LLM，自删项目）
node scripts/mention-gaps-test.mjs              # @ 体系补缺回归（分镜表/调研卡进候选/文档上传落资料卡/聊天上传进素材库/素材库拖入建卡+引用；无 LLM 约 40s）
pnpm dlx tsx scripts/chat-context-contract-test.mjs  # 附件/引用上下文段契约（显示文本+界标+人话段+manifest/存量迁移幂等；纯函数无 LLM）
node scripts/chat-attachment-display-test.mjs   # 附件在对话记录里显示为 chip 回归（气泡只出 chip·payload 仍带全文·搜索/复制/轮次轨/编辑重发；mock SSE 无 LLM，REAL=1 追加真跑 LLM 验模型侧没丢正文）
pnpm dlx tsx scripts/episode-inherit-test.mjs   # 分集回归（一张剧本卡=一集：归属继承/摘要⟨集名⟩/按集打包 zip；纯函数+浏览器 16 断言，自删项目）
pnpm dlx tsx scripts/episode-plan-test.mjs      # 分集集号回归（集号自动排号/重排归一/复制粘贴重排号/脏号清理/分集面板聚焦与↑↓；纯函数+浏览器 19 断言，自删项目）
cd agent && uv run python ../scripts/chat-alternation-guard-test.py   # 聊天历史交替守卫回归（混合调用毒历史/陈旧前端调用哑火；纯函数无 LLM）
cd agent && uv run python test_loop_guard.py    # 确定性循环检测 + 步数宽限收尾回归（A 纯函数 20 + B 图级假模型 39：同参×4 两拦一解绑/limit=12 宽限不抛递归异常/异参零误伤/前端·混合·多调用违规不误伤/止损后新 run 恢复；无 LLM 约 10s）
cd agent && uv run python test_turn_state.py    # turn 状态机补完回归（C 轮中压缩续跑：超限识别/force 折叠保 5 条/图级超限重试续跑/压不动原样抛 + G 完成闸门：锚点计数轮界/载荷判别散文不误触/全链拦下补 canvas_ops/一轮一次/hard_close 不拦；42 项无 LLM 约 10s）
cd agent && uv run python test_subagent.py      # 隔离子代理回归（干净上下文无主对话泄漏/只读白名单工具链/未知工具拒给白名单/步数耗尽解绑收尾交替合法/报告超长显式截断/递归防护结构断言/画布真值 digest 全字段/图级经 ToolNode 真执行；32 项，子模型打桩无 LLM，projects 指临时库）
# 隔离子代理（2026-09-12 P2 完结，doc/harness-gap 第三档 #9 的轻量落地——不做 subgraph、不动工具桥）：`delegate_task` 后端工具 = **干净上下文 + 只读工具白名单 + 有界内部循环**，只把最终报告带回主对话——冗长的手册阅读与中间推理不占主上下文。**执行留在主循环**（canvas_ops 是前端工具只能主循环答）：正确分工是「子代理产出 ops 计划 → 主对话 canvas_validate_ops 干跑 → canvas_ops 应用」，工具 docstring 明写何时用（长材料通读出计划/干净视角审查比稿）何时别用（改画布/一句话能答/后台任务查询）。三个价值点：长阅读隔离 / 干净视角审查 / 干跑-执行分离。**白名单只有 read_skill + read_canvas**（`_build_subagent_tools` 按委派闭包项目上下文；read_canvas 是子代理专属——`_subagent_canvas_digest` 服务端权威副本：节点一行一卡 id/类型/标题/有无图/分镜行数 + 连线 + 画风题材时代，节点 id 以此为准防幻觉）；**递归防护是结构性的**（白名单无 delegate_task，子代理生不出子代理）。内部循环 `_run_subagent`：`_SUBAGENT_MAX_STEPS=8` 步 + `_SUBAGENT_DEADLINE_S=240` 死线，**死线检查必须在调模型之前**（拿到带 tool_calls 的响应才断会留下无应答调用、收尾 ainvoke 必 400）；超限收尾与主图 hard_close 同范式（解绑工具 + 收尾提醒再要一次纯文本）；报告 cap 6000 且**超长显式标记截断**（不静默）；空报告给明示文案。子代理走独立 ChatOpenAI 通道（temperature 0.2、非流式、timeout=120——工具内阻塞主图，openai 默认 600s 会让 240s 死线后的末次调用再挂 10 分钟、thinking 档同标题生成；**非流式 ainvoke 的 reasoning_content 保留完好**，子代理多轮工具往返不触发 DeepSeek 回传 400——真跑实测）。遥测 `agent.subagent`（chars/tools/forced_close）。**真跑验证**（DeepSeek）：主模型自构自包含 task（连 context 平台背景都自己想到补）→ 子代理真读手册 → 报告含手册关键细节（fromId=剧本卡id/正文不截断）→ 主模型据报告作答，全链 10s。**探针手法**：直连 graph 的脚本必须①load_dotenv(agent/.env + 根 .env.local，否则 AGENT_API_KEY 空 → openai Missing credentials）②MemorySaver 编译（别与活 agent 争 checkpoints.db）③结果写文件 + os._exit(0)（_LazyAsyncSaver 从不关连接，aiosqlite 非守护线程会把优雅退出挂死、stdout 缓冲全丢）④用 run_in_background 跑（Bash 调用结束会收走 & 的子进程）。回归：`test_subagent.py` 32 项 + 既有 59/41/8 组不回归 + agui 冒烟
# turn 状态机补完（2026-09-12 P2 续，turn 的三个出口至此都有守卫——步数耗尽→宽限收尾（见上）、上下文爆掉→压缩续跑、模型说完成→确定性对账）：**轮中压缩续跑**：滚动压缩是阈值预防，救不了「单轮内工具结果暴增、两跳之间越线」——chat_node 的 astream 捕获 provider 超限错误（`_is_context_overflow` 认 maximum context length / context_length_exceeded / prompt is too long / exceeds the context / input is too long）→ `_compress_history(force=True)`（**无视阈值、keep_budget=0 只保最近约 5 条**，折叠边界不切开 tool 配对）→ 原地重建 trimmed 重试（**不烧 nudge 轮**、最多 `_CTX_OVERFLOW_RETRIES=2` 次）；消息 ≤8 折不动（空增量）则原样抛出——单条巨型工具结果不是压缩能救的，别死循环。强制折叠的 comp_update 随本跳 Command 落 state，后续跳走常规阈值 no-op。**完成闸门（stop-hook，091101「口播已落卡、画布全空」事故形状的通用解）**：纯文本收轮前 `_unapplied_canvas_ops` 确定性对账——本轮工具结果带稳定标记「原样应用整批 ops」**且连体 `{"ops":` 载荷**（generate_asset_images 分镜/资产两段 + generate_look_images 共用形状；**只匹配标记句会被散文引用误触**——script-to-assets 手册里就有这句原话，子代理读手册后的报告引用它、主助手正常收尾会被白拦，故锚点必须带真载荷；子代理报告真带 ops 计划时命中是期望行为）但之后没有任何 canvas_ops 调用 = 宣称与画布现实不符 → 拦下一次自环重跑给第二次机会；闸门通知以 **SystemMessage 落消息流**（模型每跳可见直到被滚动压缩折叠 / `_turn_gate_fired` 按轮扫描天然去重**一轮只拦一次**不与模型拉锯 / 前端 RenderMessage 对 system 角色 `default: return null` 不渲染已核实 / ChatPersistence 只存 user·assistant·reasoning 刷新即净）。闸门只管「调没调」不管应用成败（前端结果与模型自行处理）；hard_close 不拦（步数只够收尾；闸门→canvas_ops→前端 END 开新 invocation，应用链路预算重置）；闸门 SystemMessage 会断 `_consecutive_repeat` 尾部计数（罕见交叉场景，步数预算仍兜底）。遥测：`agent.ctx_overflow`（retry 序号）、`agent.turn_gate`（pending 锚点数）。回归：`test_turn_state.py` 41 项（C 纯函数识别/force 折叠语义含配对边界/图级 5 轮工具后超限一次强压续跑文字收尾/≤8 条压不动只试一次原样抛 + G 锚点计数五种位形/闸门标记轮界/全链拦下补 canvas_ops 且模型入参可见/无视闸门一轮一次放行/无标记零误伤/hard_close 跳过）+ test_loop_guard 59 + 交替守卫 8 组 + agui 冒烟
cd agent && uv run python test_topic_pool.py    # 选题池单测（store/生料管线/深挖复查 + 并发流水线：全量喂入/信号量限流/断点续跑/发散失败不喂 + 平台热榜/豆瓣口碑/标杆拆解知识库；fake flow 无 LLM）
cd agent && uv run python ../scripts/auth-smoke-test.py   # 认证冒烟（FastAPI TestClient 直测，临时库不污染 data/wingsight.db）
./scripts/setup-langflow.sh                     # langflow 环境重建/首个部署（装 venv → 起 7860 → 导 flows → flow id 回写 .env.local）
./scripts/update-flow.sh <flows/xx.json>        # flow 内容更新回写实例（setup 按名字幂等跳过，改 flow 后用它 PATCH）
pnpm dlx tsx scripts/era-prompt-test.mjs        # 调研发起前问年代（纯函数判据：era 空则跨项目考据复用永不生效）
node scripts/era-prompt-ui-test.mjs             # 拆完资产也问一次年代（拆解走 route mock，答了落进画布 meta）
node scripts/mention-inline-test.mjs            # 输入条 @ 内联引用（候选弹层/chip 整颗删除/图N 编号契约；出图 mock；需 dev 模式）
node scripts/instruction-compose-test.mjs       # 智能编排 KEEP/OPTIMIZE（真跑指令合成 flow + 真出 2 图）
node scripts/chat-tool-cards-test.mjs           # 聊天工具卡 + 审批内联（真跑 LLM：列技能 / 建卡删卡走审批）
node scripts/chat-revise-image-test.mjs         # 聊天侧改图闭环（regenerate_card_image 三语义 + 覆盖主图前归档；mock 出图）
node scripts/look-chain-e2e-test.mjs            # 造型图全自动链（真出 1 图：落卡 + 角色连线 + looks 回填幂等）
node scripts/look-card-unify-verify.mjs         # 旧 Look 小卡 → 标准图卡尺寸迁移一次性验证（自建项目自删）
node scripts/shot-continuity-test.mjs           # 分镜连贯/近景造型闸（同场上一镜进参考、跨场不配；需 dev 模式）
node scripts/ref-batch-unmount-resume-test.mjs  # 调研参考图跨卸载续链（平移画布后进度/终态面板恢复；route mock）
pnpm dlx tsx scripts/ref-group-collapse-test.mjs # 考据参考组框（sanitize 存量收组 + 默认展开/组框尺寸契约；纯函数）
pnpm dlx tsx scripts/ref-group-layout-test.mjs  # 考据参考组内布局（卡高预估公式/行距按该行最高卡/组框包住末行/自愈重排与幂等；纯函数+store，无 LLM 无浏览器）
node scripts/context-summary-test.mjs           # 画布摘要读通道（媒体 URL/调研卷宗 id/超预算降级不硬切；需 dev 模式，走 __summarizeCanvas）
node scripts/research-api-journey-test.mjs      # 深度调研 API 全链（发起→确认→卷宗→gap 补研，含各阶段耗时）
node scripts/research-chat-loop-test.mjs        # 深度调研聊天链路（真 LangGraph 四轮：发起/确认/落卡/查询）
node scripts/research-ui-shot-test.mjs          # 调研卡 + 卷宗阅读器 UI 截图（产出 /tmp/research-ui-*.png）
node scripts/thread-switch-regression-test.mjs  # 切项目不带旧会话 id 打 404（自动挑两个有会话的项目 A/B）
node scripts/telemetry-test.mjs                 # 操作埋点（/api/v1/events 落库聚合 + data-track 全局点击捕获）
./scripts/audit-flows.sh                        # langflow 实例 vs agent/flows 漂移审计（--fix 逐个 PATCH；退出码可作部署闸门）
node scripts/cleanup-test-projects.mjs          # 清 E2E 残留项目（按名字前缀统一回收；--dry-run 只列不删）
cd agent && uv run python ../scripts/prune-checkpoints.py   # 裁 checkpoint 孤儿线程（只清孤儿，绝不按天数删活会话）
python3 scripts/fetch-dmx-docs.py               # 抓 doc.dmxapi.cn 文档转 Markdown 存 doc/dmxapi/（一次性工具）
```

## 架构铁律

- **不做 fallback、不兼容历史版本**：除非用户主动提出，失败就报错让用户决策，不要静默降级/兜底方案；改数据结构、API、字段时直接改到位并迁移，不保留旧字段/旧格式的兼容读取（历史遗留值一律清除而非静默叠加）
- **画风闸（juben image_style_required 范式）**：出图类操作（分镜批量出图 / 资产卡 AI 出图 / 拆解自动出图链）要求画风已选——唯一入口 = 底部坞「画风」（全局）。无画风只拦视觉产物，文字流程（拆解/分镜表生成）与聊天自由出图不拦。前端三入口拦截 + `start_decompose_job` 兜底（visual_style 为空记 images_note）。分镜表卡原 visualStyle 字段已无 UI，遗留值静默叠加不参与闸。**画风选择回传（2026-09-05「选完 agent 不知道」事故）**：`open_style_picker`（CanvasAgentBridge）是**等待式工具**——handler 派发 OPEN_STYLE_EVENT 后挂起，等三态收尾：用户实际选完（store projectStyle 变化后稳定 2s——自定义输入逐键保存须等停笔）/ 关面板（CanvasView 四处关闭路径统一 `closeStylePanel()` 派发 `STYLE_PANEL_CLOSED_EVENT`）/ 超时 4min；返回值带所选画风，agent 同轮接着干。此前 handler 立即返回固定文案「等用户选择后继续」，用户选完画风后 agent 一无所知、待办任务也不续跑
- **图生图参考（输入条直连管线 directImagegen）**：参考序列单一事实源 = `lib/canvas/refSequence.buildRefSequence`——@ 引用 → 本卡原图（未 @ 自己时作图生图锚点，孝庄太后项目踩坑）→ 上游连线卡，带图才收、按图 URL 去重；PromptBar 的「参考 N/4」计数与 chips 上的 图N 位次同源读取（口径漂移曾致「参考 0/4 实发 3 张」的罪案实录事故）。图N 编号**覆盖全部参考**（numberingNote 注入正文，连线参考也有编号可指代）。**上传图派生改图（2026-09-04，novanova/open-ai-canvas 范式，7/8 竞品共识）**：无生成谱系（`genShot`/`genPrompt` 皆无）的有图图片卡（上传图/素材库图）提交**不在本卡原位覆盖**——PromptBar 右侧避让建新图卡+连线（标题取提示词开头、继承卡级 `gen`），经 `editOf`=源卡 对新卡发事件；桥接层把源图当「本卡原图」角色（EDIT 最小模板改图锚点，`buildRefSequence` 的 self 条目挂**源卡**——图N 编号与 visualNotes 报源卡标题；referenceLabels 名「原图」）。模式标签「派生新卡」（2026-09-04 底栏术语说人话：派生新卡/改图/参考生成/文生图，解释进 tooltip）、占位文案/chip 提示随切换；「按设定重掷」在无谱系卡上同样派生；@ 引用随迁新卡（连线不随迁）。已生成的谱系卡保持原位+版本档案（锚定控制/快照重跑整套语义），**以谱系为界互不越界**——测试夹具代表已生成卡时须带 `genPrompt`（ref-pipeline 的 shot/editCard、mention-inline 的 n_tgt）。回归：ref-pipeline §13。显式引用（@/连线）存在却**既无可用图也无可用正文**时明报拦下（不发任务，卡上出错误说明），不静默降级文生图；空卡无引用的纯文生图不受影响。**连线文本参考（2026-09-04 白骨精项目事故）**：连进来的卡没图但有正文（剧本/文本/调研…）→ 正文以 `连线文本《标题》：正文` 全量注入 visualNotes，与 @ 无图卡同待遇——此前这类卡被丢弃且触发参考闸，「剧本卡连线空图卡生成角色图」被拦死，用户没有「从剧本建缺失角色」的直连路径（PromptBar 连线 chip 的「参与本次生成」承诺与管线行为矛盾）。noRefs 重掷连文本参考一并忽略。回归：ref-pipeline §16（K 组）。**版式显选 v3（2026-09-07 空镜事故定案，8 竞品调研共识）**：生效路径 = 用户显式点选（版式 chip 经 detail.assetType）> **声明上下文**（资产卡自身类型 / 分镜表派生 → shot / Look 卡父类型，`genContract.declaredAssetType`——卡片类型是建卡时声明的意图，不算猜）> **none 原话直传**——不注入任何版式命令段，最终提示词 = 用户描述（置顶）+ 画风段 + 参考职责 + 文字守卫（flow `_FREE_TEMPLATE`；agent 侧 "none" 原样透传不落 scene 缺省；genShot 快照记 assetType:"none"，补出/字面重试原样往返）。关键词推断只活在展示层（`recommendAssetType` 供版式 chip「推荐」标记），**永不自动采用**——旧 v2 关键词白名单（场景|空镜|环境）把「树木/草坪/全景」纯风景修改猜成 shot，shot 版式「呈现描述中的人物」命令句把无人空镜拍出人（宝马山 4e2df837 项目三连：无参考版式逼模型编出长椅读书人 / 连线场景卡剧情描述注入七个围观抢劫者 / 用户手补「不出现任何人物」才压住）；8 家竞品无一在自由出图路径做类型推断+模板注入——意图声明靠显式动作（ai-moive 角色卡三视图按钮 / open-ai-canvas 工作流确认门 / novanova KEEP 且 edit 强制 KEEP），自由路径是传话筒。改图仍走 EDIT 最小模板（promptTemplate 整体替换，含标题行）+ assetType=none。回归：ref-pipeline §4-§7（带图参考 none / 改图 none+模板 / 场景关键词失效 / 场景资产卡声明 scene）。存量占位标题（hint 默认文案/hex 文件名）由 sanitize 装载时剥成空名（`strippedTitles`）。@ 候选弹层（2026-09-04 分组化）：顶部搜索框（组内过滤标题+正文）+ 分组 tab 行（本卡/已连线 N/角色/场景/道具/服饰/图片…，ai-moive 上游分组+open-ai-canvas 分区范式），←→ 或点击切组、↑↓ 组内循环、Enter 拾取、高亮 scrollIntoView；有过滤词时空组隐藏。旧「分桶轮转截 24」弃用——后建卡被挤出列表靠分组+搜索根治（罪案实录事故不再靠配额缓解）。**版式契约显性化（2026-09-07 报纸事故四件套）**：宝马山项目「根据图1 生成报纸道具图…纯白色背景」出成人物读报剧照——①映射层黑洞：聊天工具 type 传了缺省 shot，关键词只认场景词，「道具」没有分支，shot 版式（人物+环境+剧情）注入后压死用户原话；②提示词黑箱：flow 渲染的最终提示词（版式+画风+描述+参考职责）不落库，任何界面看不到。四件修复：**P0** `generate_asset_images` 工具描述教「单件物件道具图（报纸/文件/告示）必须用 prop，不要用 shot」+ `final_prompt` 入参（完整提示词整体替换，原样出图）；**P1** flow `record["final_prompt"]` 回传渲染结果 → `_generate_single_image` 解析 → 落卡 `genShot.finalPrompt`（分镜行 ops 自动带；非分镜卡经返回文本 genShots 附录教 agent 写卡）→ 「节点信息」弹窗「实际发送提示词」块展示（差分范式：与 genPrompt 原文不同亮「含版式契约」徽标）；**P2** 该块可编辑 + 「按此提示词重跑」（GENERATE_EVENT `finalPrompt` → 桥接层 payload final_prompt 整体替换 + 跳过指令编排 + genShot 快照回填服务端渲染版）；**P3** `lib/canvas/genContract.ts` 共享解析（桥接层与 PromptBar 同源，PROP_KEYWORDS 道具|设定图|结构图|海报|报纸|文件|告示|信件|传单|标牌|图鉴 → prop）+ PromptBar 底栏版式 chip 事前可见（**原话直传**（默认）/电影剧照/道具结构图/空镜场景/定妆照/服装结构图，说人话 label+tooltip）；同日空镜事故升级版式显选 v3——推荐只做菜单标记不再自动采用（详见「图生图参考·版式显选 v3」）。**导航键的 keyup 不重算 trigger**（`onKeyUp` 跳过 ↑↓/Enter/Esc）——keyup syncTrigger 会 setHi(0) 把高亮弹回首项，「方向键没反应」根因。**方向键整颗跨过 chip（`skipChipCaret`，2026-09-07「光标跑到 @xxx 实体里」事故）**：chip 是 contenteditable=false 复合元素（头像/徽标+label），浏览器默认 ←/→ 会把 caret 钻进 chip 内部（label 文本/组件之间），之后打字插进 chip、正文打不出字——编辑器 onKeyDown 在 弹层切组（多组才消费 ←→）之后接管：①caret 已在 chip 内（任何深度）→ 原地弹到 chip 前/后 ②文本节点末/首紧邻 chip → setStartAfter/Before 整颗跨过 ③宿主 childIndex 档位指向 chip 前后 → 同跳。E2E 探针要点：无图卡的 chip textContent 是「角@关关」形态（badge 取类型名首字），断言别按 "@标题" 硬等；打字落位断言要数清 ← 按了几次（多按会到行首）。正文 @ImageN 式死文本有软提示（不拦截）。聊天出图工具资产 JSON 支持 `reference_images`/`reference_labels`（snake_case，`_generate_single_image` 双键名都收）。**参与清单显性化（2026-09-04，八家竞品共识「所显即所发」）**：输入框上方 chips 行=本次生成的完整参与物——本卡原图（图1）/本卡设定/全局画风/@引用，全部可 × 摘除（当次生效，切卡恢复默认；novanova「显式动作才进参考」范式）。`selfBodyOff`/`styleOff` 经 GENERATE_EVENT 透传桥接层：设定摘除=不进 visualNotes/compose setting/空指令描述兜底；画风摘除=visualNotes 去掉全局画风段。**设定统一注入通道**：chip 开且非空 → `本卡设定（《标题》）：正文` 必进 visualNotes——修掉「有指令+扩写关+无参考→设定静默丢弃」暗坑；refEntries 的 self 图条目不再携带正文（防与设定项重复），正文职责归设定项独占；noRefs「按设定重掷」无视摘除强制带设定。卡上资产设定默认折叠一行摘要「设定 · 出图自动带上 · N 字」（点开才是 inline 编辑；空设定保持 placeholder 直编辑）——「图是主角、设定是附录、指令在输入框」三层分离。**智能编排（默认开，卡字段 `composeOpt:false` 关；UI 名「AI 扩写」留底栏（高频开关不藏弹窗）；候选张数收进 ImagegenChips 模型弹窗——底栏只留一个参数入口不堆术语）**：出图前经「指令合成」flow（`LANGFLOW_COMPOSE_FLOW_ID`）做 KEEP/OPTIMIZE 判定——短指令结合设定文本扩写成完整提示词，完整描述/改图指令 keep 原样（提示词搬运自 novanova agent-image.md/optimization-image.md）；合成失败明报不降级直传；结果落 `composedPrompt` 随任务回传，PromptBar 只在扩写真实发生时出一条折叠对照行（「AI 扩写了你的提示词」可载入修改）；输入框**不预填卡上正文**（只回显 genPrompt——正文在卡上可见，抄进输入框是两份重复，用户反人性反馈；空提示词=按标题+设定生成，placeholder 已讲明）；**Enter=提交生成（2026-09-07 用户拍板，画布面板与聊天侧统一 enterToSubmit 语义）**：Shift+Enter 换行、Ctrl/⌘+Enter 仍提交（兼容肌肉记忆）、@ 弹层打开时 Enter 归拾取、IME 组合中不抢键；生成按钮 tooltip 同步改口，mention-inline 回归提交键已改打裸 Enter；注入设定清单（visualNotes）是审计信息，看「节点信息」弹窗，不进生成面板**锚定控制（渣男「改了没变化」事故的解法）**：①「本卡原图」chip 可 × 移除（当次生效，`selfRefOff` 经 GENERATE_EVENT 传桥接层，切卡恢复默认带上）②模式标签（Storyboard-Copilot modeLabel 范式：文生图/参考生成/改图/派生新卡，提交前可见是否被锚定）③「⟳ 按设定重掷」（chips 行尾，`noRefs` 忽略全部参考 + 标题/设定正文纯文生图全新渲染，做减法立即可见）④字面「重新生成/重试」= genShot 快照原样补出重跑（novanova RETRY_MESSAGES 范式，改了文字才是新创作）。**原文抓取四通道（2026-09-04 双通道起家；2026-09-11 两跳：补官方 API 层治「抓取经常失败」，同日用户拍板改 Jina 优先 + 文路并行化）**：`fetch_page_text`（research.py）= 知乎专栏专项 **TikHub** 排最前（`_tikhub_zhihu_article`：`zhuanlan.zhihu.com/p/{id}` → `fetch_column_article_detail` 按 article_id 取正文，`TIKHUB_API_KEY` 已配——登录墙内唯一稳定通道，别让 Jina 白跑；注意 shell `cut -d=` 取 key 会被键内 `=` 截断造成 401 假象，dotenv 全量加载即有效——实测 15683 字）→ **本地 Jina 主路径**（`jina_reader.fetch_local`，docker `ghcr.io/jina-ai/reader:oss` @ `127.0.0.1:3000`，`JINA_READER_BASE_URL` 可覆盖；无头浏览器+主内容提取，正文最干净——直抓的正则剥 HTML 会带导航残渣；实测解锁 academia.edu 与学术 PDF）→ httpx 直抓回退（快、免费；**Jina 失败 ≠ 直抓失败**——有些站拦浏览器指纹不拦裸 httpx，本地实例没起时也靠它秒级顶上）→ **官方 API 收尾**（`jina_reader.fetch_api`，`https://r.jina.ai`，**配 `JINA_READER_API_KEY` 才启用**、`JINA_READER_API_BASE_URL` 可覆盖；按输出 token 计费 $0.05/百万、新 key 送 10M ≈ 数千次，`X-Retain-Images: none` 丢图省 token；无头浏览器集群+住宅代理，**本地判定不算终审**——本地过不去的反爬官方常常能过；生产服务器没部署 docker 就靠这层）。全链 4xx=永久不可达不重试、验证码/登录墙内容识别（WebSourceContentError）语义来自 juben lib/web_search/jina.py；key 失效/无额度静默跳过。**考据文路抓页 4 路并发**（`imgresearch._PAGE_FETCH_CONCURRENCY`，深度调研同款信号量范式）：原串行 for 是直抓时代没人疼过的惰性（全库三条抓取路径唯一串行的一条），Jina 优先后单页 2-10s、串行 4 页 8-40s，并行化摊平到 1 倍；帽不放大——本地 Jina 每页真开一个无头浏览器，批量调研 20 路资产一波；pages 按 merged 顺序组装（gather 不保序）。**本地 jina 运维（2026-09-11「抓取经常失败」根因复盘）**：当时 colima 停了容器跟着停、生产从未部署 docker，整条回退链等于裸直抓。三层保障：①容器名 `jina-reader`（内部端口 8081，`-p 3000:8081`）、`--restart unless-stopped` 随 colima 自启；②**容器代理地址必须是 colima 网关 `192.168.5.2:7897`**——宿主代理只听 127.0.0.1，容器里 127.0.0.1 指自己、旧容器烘的 `172.17.0.1:7898` 是死地址，症状 = wikisource 等境外源全 `ERR_PROXY_CONNECTION`（修法 = 删容器按 start 脚本重建，代理值从 colima VM `/etc/environment` 自动推导）；③`start_wingsight.sh` 的 `ensure_jina`：3000 没人听时自动 colima start + docker start/run（镜像本地有才建，**绝不隐式 pull 2GB**，任何失败只警告不阻塞——jina 是可选依赖，没有它链路自动降级直抓+官方 API）。
**文字考据（fork-join 文路，2026-09-02）**：批量调研 plan flow 一次产出 图词+文词（`text_queries`，手填词不跑文路）；文路（`imgresearch._run_text_research`：Serper 网搜→复用 `research.fetch_page_text` 抓正文≤4页→`ref-research-brief` flow 提纯）与图路搜索下载并行，join 在终选——select 的 asset 载荷带 `research_brief` 字段纠错配（错年代/错形制降权）；简报同时落卡 `data.researchBrief`（条目完成即写、幂等，资产卡设定下方折叠块），喂「AI 写设定」context；**出图链路 2026-09-10 起改为服务端统一注入**（`skills._inject_research_briefs`：`start_storyboard_image_job` 按 rid（含 `#序号` 剥离）/资产名、`generate_asset_images` 按资产名，从画布取 `data.researchBrief` 并入 visual_notes——覆盖补资产图/资产卡直出/图片卡/聊天出图四条路）。此前只有「AI 扩写开 + 输入框有指令」才经 compose setting 注入，其余路径全漏：图照着搜来的真实参考图画、却没有年代形制的文字约束（用户反馈「生成资产图不够尊重史实」，2026-09-10 定案）。回归：`cd agent && uv run python test_asset_research_brief.py`（107 项，含端到端断言简报送达出图载荷 + 跨项目复用 + 主题分发注入）。**题材开关与补考据（2026-09-10，用户口径「主做历史/罪案纪录片 + 动画片，除了动画片都是真实题材」）**：画布 meta 加 `factuality`（缺省 `real`）；真实题材出图前对**没有 researchBrief 的资产**自动补一次文字考据（`skills._ensure_research_brief`：出词 `run_ref_plan_flow` + 搜抓提纯 `imgresearch._run_text_research`，复用调研文路原语），拼接格式「考据依据（真实形制与年代，优先遵循）：…」；`_BRIEF_CACHE` 按 `项目:资产名` 进程内缓存（同资产重出不重复搜）、`_BRIEF_CONCURRENCY=6` 限流、逐资产预热与出图流水线重叠，失败软放行不拦出图（落 `[考据]` 日志）。虚构题材（动画/架空/穿越）跳过考据——只给 agent 一个前端工具 `set_project_factuality`（用户说做动画片时声明；**不加 UI 开关**，摘要「题材：」行让 agent 可见当前口径）。**豆包搜图死代码已清（同日）**：`graph.py` 资产 JSON 契约删 `search_query` 字段、`skills.py` 删两处「key 空则静默剥 search_query」降级逻辑（违背「不做 fallback」铁律）与 `VOLC_SEARCH_API_KEY` 常量、`.env.example` 删条目——该通道从未真正工作（拆解 flow 不产出该字段），出图联网考据统一走 Serper 调研 + 补考据。注意 `langflow/.../batch_asset_sheet.py` 里的豆包组件代码保留（无害、不可达），彻底删除需改 flow 内嵌代码。**考据跨项目复用与 era 询问（2026-09-10）**：`canvases.meta.era`（如「北魏·平城时期」）是复用的作用域键——服务端按 (era, 资产名) 命中同题材项目的历史条目（`skills._reusable_brief` → `imgresearch.lookup_entries`），era 为空一律不复用（名字像而年代不同的资产，「朝服」在唐宋与北魏是两回事）。生产 23 个项目实测**无一设过 era**：`set_project_era` 是 agent 前端工具，而调研是用户点按钮发起、不经过 agent，复用能力因此从未生效、每次调研都重搜。现改为**调研发起前问一次**（`startBatchResearchForCard` 里 `shouldPromptEra` 判定 + `window.prompt`，可跳过；跳过记会话级 `eraPromptSkipped` Set、不落库，刷新后重问）——真实题材才问，架空/穿越不设 era 免污染复用域。回归：`pnpm dlx tsx scripts/era-prompt-test.mjs`（9 项）。**报告卡/大纲卡删除语义（2026-09-10 用户拍板「删了就不再来」）**：与参考卡「删卡=取消采纳」同语义——用户删掉考证报告/大纲卡后，对账不再重建。报告卡没有服务端采纳凭据，故标记记在项目 `meta.dismissedReports`（`store.deleteNodes` 落、`saneDismissedReports` 装载整形、`refReconcile.upsertDocCard` 读）。注意两类「不再打扰」语义别混：era 询问的跳过是**会话级 Set**（临时意愿、刷新即重问），报告卡删除是**持久化 meta**（用户决定、跨会话生效）。回归：`pnpm dlx tsx scripts/report-dismiss-test.mjs`（8 项纯函数）+ `node scripts/ref-report-reconcile-test.mjs` F 组（F1-F3 浏览器端：删除 → meta 记 kind → 重载不重建）。文路全程软失败（errors["考据"]），不拦搜图采纳。回归：`scripts/ref-pipeline-test.mjs`。**调研产物落点与考证大纲（2026-09-10，用户「调研结果不该只是一堆图片和文本，应该形成一份可复用的报告」）**：诊断——调研产物此前只有两个去处：候选图落 `ref_candidates`，考据简报只活在 agent 进程内存的 job 字典里（重启即蒸发，库里没有存它的表）；用户唯一能看见的形态（画布卡 `data.researchBrief` / `refSource` 参考卡）**写入端全在前端轮询回调里**（`nodes.tsx` `useBatchRefJob`：卡带 `refBatchJobId` 锚 → 轮询 → 条目 done 时落卡 + `autoAdoptTopRecommendations` 建参考卡）。四个条件（锚在、卡挂载、轮询跑到、agent 没重启）任一不成立产物就永久留在库里；而 `refBatchJobId` 全仓只有一处写入（`nodes.tsx` 前端圈定资产发起批量调研），**agent 从聊天调 research_asset_references 发的调研没人写锚** → 没有任何卡去轮询。生产库实测：23 个项目画布 JSON `refSource` 零命中、资产卡 `researchBrief` 全零，而 `ref_candidates` 里躺着 152 张已采纳的图——钱花了、产物在库里、画布上什么都没有。修复三层：①**服务端权威落点** = 新表 `research_entries`（`upsert_entry`/`list_entries`/`lookup_entries`）——简报一产出即入库（`_run_research` 文路 join 后、`skills._ensure_research_brief` 补考据后），与谁发起/画布开没开/进程活没活无关；`_run_text_research` 改为返回 `(简报, 来源底账)`（来源=真正抓成功的页面 title/url/domain，条目落库时存底账）；`_inject_research_briefs` 注入源改为**条目优先 + 画布卡存量兜底**（历史项目有只有卡没条目的）。②**画布对账**（本架构下画布写入都在前端——agent 用前端工具/ops，服务端权威落在**数据**上、画布是它的视图）= `lib/canvas/refReconcile.ts` `reconcileRefResearch(projectId)`：读 `GET /refs/report` + `GET /refs/outline` → 简报写进资产卡（按 `cardBriefs` 内容比对，幂等）、已采纳图物化成参考卡+连线（按图 URL 去重，复用 `adoptRefRows` 的参考带布局）、报告与大纲落成文本卡（各自单例，按 `data.reportKind` 认领）；触发点 = TaskEvents 打开项目时（等 hydration，20×500ms 有界重试）+ `ref_research` 终态事件（**先对账再自动续跑**，让 agent 汇报时卡已就位）；打开项目即自愈，不再依赖轮询窗口。③**考证大纲（调研单位是时代不是资产，用户「人会先定调研计划再逐个实施」）**：新表 `research_topics`（`replace_topics` 整份替换——大纲是一份判断，增量叠加会攒出互相矛盾的多版；`build_outline(_report)`、`topic_briefs` 按服务范围分发、`lookup_topic_entry`、`run_topics` 并发执行）+ 后端工具 `get_research_material` / `propose_research_outline` / `run_research_outline` + 端点 `GET/POST /refs/outline`、`POST /refs/outline/run` + 画布大纲卡（`reportKind="ref-outline"`，工具条「执行主题」，客户端 5s 轮询刷状态）。判据（`real-documentary` 手册新增「考证大纲」节）——**影响面最大的那类旧流程里没人做**：已有条目内容重叠的一组卡收成一个主题；**没有任何卡认领的时代共有事实必须单独成主题**（「不属于任何单个资产」所以无主）；只影响一张卡且无时代共性不进大纲。`replace_topics` 的 nodeIds 照 `research_asset_references` 防幻觉口径（不存在则报错并列出可用卡清单）；「服务哪些卡」是优先级依据（按服务卡数排资源，不是按资产顺序）。**文字考据走主题级，参考图仍走资产级**（参考图必须像那个资产）——所以 `ref_candidates` 模型未动。主题事实按 `topic_briefs` 分发给成员资产：`_inject_research_briefs` 合并「本资产条目 + 服务它的主题条目」注入，`build_report.cardBriefs` 是同一合成结果（**卡上显示的 = 出图发出去的**）；执行状态写在主题行，**有事实即已完成**（条目可能来自直接写入/补考据，状态还停在 planned 时显示「待执行」会误导）；「待补」口径含**被主题覆盖**的资产（不催用户做已经做过的事）。**跨项目复用（era 作用域键）**：条目按 `era` 归档（画布 meta.era + 前端工具 `set_project_era`，空=不复用，架空/穿越不设），`lookup_entries`（同 era + 同资产名归一相等）与 `lookup_topic_entry`（同 era + 同主题键）**严格相等**——名字像而年代不同（「朝服」在唐宋 vs 北魏）宁可重搜不可错用；资产类型只做负向护栏且**两侧都是资产四类时才比**（分镜载荷类型是版式 shot，拿它比会把该命中的全漏掉）。`_ensure_research_brief` 查表顺序：本项目条目（注入已覆盖）→ 跨项目条目（命中即用，正文标「复用《X》同题材考据条目」）→ 现搜并落库；主题执行前也先查跨项目同主题（命中记 `status=reused`，注入时按**活引用**取源项目条目、不拷贝）。**产物两张卡不重复**：大纲卡=计划与进度板（主题/服务范围/检索词/状态），报告卡=事实（逐主题与逐资产事实 + 来源底账 + 参考图底账 + 待补清单；段号随有无大纲浮动，`_CN_NUM` 按序取）。**参考卡删除=取消采纳（同日补，用户拍板「删了别自己长回来」）**：物化时把候选 id 落卡（`data.refCandidateId`），删除参考卡时 `store.deleteNodes` 广播给 `lib/canvas/refDismiss.ts` 注册的处理器（**store 不碰网络**，故走注册钩子；钩子必须挂在 deleteNodes 这个唯一入口上——工具条/键盘/批量删都走它，逐个 UI 路径挂钩必漏），由 TaskEvents 调 `POST /refs/unadopt`（保留候选行、只摘 adopted，面板里可重新采纳）。对账同时做**反向修复**：画布上有参考卡而服务端未采纳（删卡撤销回来后就是这状态）→ 补采纳，两个方向都自愈。不这么做的话对账只认「这张图 URL 有没有卡」，分不清「用户删过」与「从没建过」，删掉下次打开又长回来。回归：`cd agent && uv run python test_ref_report.py`（70 项：归属键与重跑覆盖/报告分节/cardBriefs 合成/大纲整份替换与防幻觉/主题分发/reused 活引用/被主题覆盖不计待补）、`uv run python test_asset_research_brief.py`（107 项：注入优先级/跨项目复用/主题分发/失败软放行）、`node scripts/ref-outline-api-test.mjs`（27 项，真 agent HTTP 契约，无 LLM 无浏览器）、`node scripts/ref-report-reconcile-test.mjs`（25 项浏览器对账：简报落卡/参考卡物化/报告卡与大纲卡单例幂等/era 不被前端保存周期抹掉/删参考卡后取消采纳且重载不长回来）。**调研失败的可感知·可恢复·可自愈（2026-09-11 冯太后项目「为啥那么多调研任务都失败了」产品化解决）**：诊断实况——09-07 那轮 70 资产批量调研 52 成 18 败，失败全是人物类候选图下载 403（20 路并发 × 32 路下载同时压向百科/知乎/搜狐几个大 CDN 的防盗链风暴 + 110s 下载死线把排队队尾整批掐掉；403 不在重试白名单）；而用户打开项目看到的恐慌来自两层假象——报告卡头部「已有考据 1 · 待补 70」（52 个参考图调研成功的资产也被计成待补：文字简报 09-10 才开始落库，旧调研的简报没存档）+ agent 只在聊天里报过「18 项失败」且**没有任何恢复入口**。四层修复：①**口径重构**（`build_report`）——头部改三口径「资产 N ｜ 参考图已采纳 X 张、覆盖 M 个 ｜ 文字考据 K 个 ｜ 缺参考图待补 Y 个」（文字考据只数画布卡、孤儿条目不进头部算术）；待补清单分级——「缺参考图」（真待办，行内带 jobstore 镜像的**上次失败原因**）与「另有 N 个参考图已采纳、文字考据未存档」（次级说明+指路考证大纲，**不进重跑清单**——重跑它们是浪费搜索）；`missing` 保持全集供旧消费方，新增 `pendingAssets`（真待办结构化清单）。②**恢复回路**——报告卡（reportKind=ref-research）工具条「补调研 N」按钮（`nodes.tsx` TextCard，与大纲卡「执行主题」同范式）：读卡上 `data.reportPending`（对账随报告同步落卡）→ `promptEraIfNeeded()` → `startBatchRefResearch` → 锚 `refBatchJobId` 在报告卡上 `useBatchRefJob` 续链 → 终态清锚 + 对账刷新报告（N 随之减少）+ toast 汇报。③**自愈**（`_run_batch`）——批内启动抖动（`random.uniform(0, min(12, 0.4×N))` 摊开下载风暴）+ 主轮结束后 error 项（未产出任何候选的）**隔 45s 自动补跑一轮**（`retried` 标记；只补 error 不重跑 done——萧燕燕计费原则同精神，且调研按搜索计费、error 项没花出采纳成本）；`done` 计数改按 items 状态重算（补跑回退计数，不再递增越界）。④**失败原因落库**——batch 状态镜像 jobstore（kind=`ref_batch`，create 于开跑/finish 于终态，items 含 per-item error；重启丢内存任务表后报告还能读到「上次失败/中断」），`jobstore.latest_states(kind)` 新增。下载层同步：**403 进退避重试白名单**（瞬时防盗链挑战退避后再来就过）、下载死线 110→150s（与文路 `_BRIEF_WAIT_S` 同档）。回归：`test_ref_report.py`（79 项：三口径头部/分级待补/pendingAssets 排除有参考图项/上次失败原因/镜像不串项目）、`ref-report-reconcile-test.mjs`（31 项，A3b/B5b 断言 pendingAssets 落卡）。**历史项目救法口径**：缺参考图的资产=报告卡「补调研」一键；只缺文字简报的=跑考证大纲或等出图自动补考据——都不要整轮重跑。**参考卡收「考据参考」折叠组（同日下午，用户反馈「调研结果图直接显示在画布里，乱七八糟」）**：09-10 的对账物化把已采纳参考图全量铺成画布卡，旧落位「资产列下方各占一条横带」是给单资产小批量设计的——冯太后 52 资产 × 3 张一次性物化抻成 2350×4100px 的 sprawl。重做落位（`refAdopt.adoptRefRows`）：全部参考卡 parent 进项目级「考据参考」组框（`data.refGroup="research"` 单例），**建组即折叠**（172×40 胶囊、子卡 hidden；追加式 4 列网格落在组内已有内容 bbox 之下，折叠态 prevSize 同步扩张）——出图参考链路零影响：前端 `buildRefSequence` 与服务端 `_canvas_ref_cards` 都按 nodes/edges 收参考、**不过滤 hidden**（改前实测核实）；点胶囊展开看全部。存量散卡由 sanitize 装载迁移（`regroupedRefs`）：保位收进组（相对排布不变、默认折叠）、幂等；**守卫**——组已存在时只收 ≥6 张的批量散卡（用户从组里拖出来的个别卡是刻意安排，不塞回）；组不存在时任意张都收（首次迁移）。折叠/展开复用既有 `toggleGroupCollapse`；删组框=子卡提升回画布层（≠取消采纳，下次装载 sanitize 会再收进新组——想真不要就删卡=取消采纳）。回归：`pnpm dlx tsx scripts/ref-group-collapse-test.mjs`（16 项：迁移/幂等/守卫/新采纳落组/追加不叠压）+ `ref-report-reconcile-test.mjs`（33 项，B8c/B8d 断言组框折叠与 parentId，E 组删除走「展开分组→点卡」路径）。E2E 注意：折叠组的子卡不在 DOM（hidden），要交互先点 `[aria-label="展开分组"]`；组框落位在可见内容 bbox 下方 +120，常规视口内可点。

**调研主体库与显式复用（2026-09-11，用户三连问「为啥要二选一 / 人做调研应该先规划再实施 / 应该形成能复用的报告，下个项目直接用」）**：诊断——调研的**主体**（时代共有事实 / 具名个体）被建模成了项目内的副产品：文字条目键是 `(project_id, asset_key)`（跨项目靠「撞名」隐式命中、没有任何地方能看见库里有什么）、参考图键是 `(project_id, node_id)`（**完全不能复用**，而图是最贵的产物：搜索+下载+视觉终选+存储）、报告卡是 `build_report(pid)` 的一段文本（项目的视图，不是主体的陈述）、终选挑图时看不到时代事实（「文字定边界→图像做选择」缺的最后一步）。四件修复：
①**主体化**（`research_entries` 重建表，不做旧格式兼容）：加 `subject_key`（`asset:{名归一}` / `topic:{主题键}`，分 kind 前缀——顺带消除旧模型「主题条目与资产条目共用一列键、同名互相覆盖」的隐患），唯一约束改 `UNIQUE(era, subject_key)`，`project_id` 降级为「最近产出者」（溯源展示用），删 `asset_key`。**era 为空 → 键加 `local:{项目}:` 前缀**（空不是错误态，表示该主体只在项目内成立：用户跳过年代询问、或题材就是架空）。
②**主体图集**（新表 `research_subject_refs`，键 `(era, subject_key, url)` 去重）：调研采纳的参考图挂在**主体**上而不是项目节点上——这才是「同一个时代同一件事不必每个项目重搜一遍」在图上成立的前提。图与事实各自独立：手填检索词的调研不跑文路、没有事实条目，图照样入库（`_run_research` 终选后 `_archive_adopted_refs` 回流）。
③**项目引用**（新表 `research_uses`，`UNIQUE(project_id, target_kind, target_key, entry_id)`）：`list_entries` 改读引用表——「本项目有哪些考据」的答案不能是「project_id = 本项目」（主体全库唯一，谁产出的不重要、用了才该出现）。`upsert_entry` 落主体时同时记产出方引用；库命中时 `record_use`；同一主体可同时挂「自身主体」与「时代主体」到同一张卡。
④**显式复用读面**：`GET /projects/{pid}/refs/library`（本项目 era 下全库主体：事实 + 图集张数 + 出处项目 + `used`）、`POST /refs/import`（挂到某张卡/某个主题，校验目标存在、防幻觉 id）；agent 工具 `list_research_library` / `import_research`，`get_research_material` 增第五节「同题材可复用主体」；画布入口 = 报告卡工具条「同题材库」（`RefLibraryDialog`，按名称归一匹配画布卡，匹配不到明说）。**活引用**（用户拍板）：引用方不存副本，正文与图集每次从主体读——主体被任何项目改进（更准的年代形制）所有引用方下次出图就跟着变；主题执行命中库即 `status=reused` + 记引用、零搜索。
**解析层收口**（`skills._entry_index` 一次给出「按节点 id / 按资产名 / 按主体键」三个索引，记录带 `album` 与 `source` 出处）：`_inject_research_briefs` 合并顺序 = **主体事实（权威，含复用来的）> 画布卡 researchBrief（存量兜底）> 主题考据（按服务范围分发）**；`_inject_canvas_refs` 改为双来源——**本项目自己采纳的参考卡优先占席，主体图集补剩余席位**（上限 `_MAX_REF_IMAGES=4`，标签写「复用《X》」，用户与模型都看得出这张图是哪来的判断）；`_ensure_research_brief` 的跨项目分支改用主体库检索，命中即记引用并把图集一起补进参考序列（首次出图就吃到，不必等下一轮）；**分镜行/资产卡直出路径也接上 `_inject_canvas_refs`**（此前服务端不读 edges + 前端 genLook 不传参考 = 已采纳的考据参考图连在卡上也白连）。
**两个顺带修掉的既有 bug**：`_project_scope` 返回 `(项目名, era)`，而 `_run_topic` 写成 `_, src_name = ...` ——「复用自《X》」一直显示的是**年代**（如《北魏·平城时期》）而不是项目名；`generate_asset_images.one()` 把画风硬写进 `shot["visual_notes"]`，若载荷用 camelCase `visualNotes`（`_attach_brief` 会沿用调用方键名）就会新建一个键，而 `_generate_single_image` 优先读 `visual_notes` → **考据依据被画风整段顶掉**（隐性丢失）。
**run_topics 重跑语义**：缺省全量时跳过已有事实的主题（done/reused 或已有条目——状态机只记过程、产物为准，重跑是白花搜索）；**显式点名（`topic_keys` 非空）一律跑**（点名就是重跑意图，静默空转比花钱更糟），`_run_topic(force=True)` 跳过「已有事实」短路但**仍复用库命中**。
**前端**：报告卡/大纲卡正文改**只读**（此前是可编辑 textarea，而 `refReconcile` 每次对账按内容差回写——用户手改的正文在下一次对账被静默覆盖，改了个寂寞；事实权威不给人手改，与调研卷宗卡同款语义，改写走聊天）；报告卡正文增「复用自《X》——本项目未重搜，源更新此处跟着变」与「—— 参考图集：N 张（同题材项目可直接复用）」行，头部增一行「复用同题材考据 K 条 · 主体图集 N 张可跨项目复用」；**era 可见可改**——此前唯一入口是发起调研时弹一次 `window.prompt` 和 agent 工具，没有可见入口库就一直是空的（生产 23 个项目实测全空），现在报告卡工具条有「年代：X / 年代未设」按钮（事实性开关仍不加 UI，只给 agent 工具——用户既有拍板不变）。
**迁移与回填**（`init_ref_research_db` 内一次性、`ref_maintenance` 打标记防重跑）：同 `(era, 主体键)` 多行归并——`updated_at` 最新者为正文权威、来源底账取并集、`created_at` 取最早，**每个原属项目都补一条 uses**（不补的话旧项目合并后报告整段消失）；存量 `ref_candidates.adopted=1` 按 `(era, 画布资产名)` 回填图集。本地真库实测：42 条 → 39 条（3 组归并），四个真实项目迁移前后报告事实**逐条一致**。
**测试污染警示（本次踩到）**：主体按 `era` 全库共享，所以 E2E fixture **必须用当次唯一的 era 串**（`e2e-era-${Date.now()}`）——用固定 era（原 reconcile/outline 测试写的「北魏·平城时期」）会与真实项目同名同域：既覆盖别人的事实，清理时按 `project_id` 删还会把共用的那条一起删掉。已修两个测试并在清理里加 era 维度 DELETE。
**已知取舍**：同一主体重跑 = 覆盖全库共享的那份（活引用的对称面：谁改对都算改对）；era 后设不会把已有的 `local:` 主体迁进库（要进库得重跑一次）；参考图跨项目复用的口径是**全部可复用**（用户拍板，含具名个体的脸——标签写明出处供改选）。
回归：`node scripts/research-library-test.mjs`（27 项：库读面/引用幂等/报告复用标注/出图载荷事实+图集+标签/主题库命中不重搜/无 era 空库/幻觉目标 400/迁移幂等 + F 组浏览器：报告卡只读无 textarea + 同题材库入口在位）、`cd agent && uv run python test_ref_report.py`（89 项，含主体唯一/归并/uses/图集）、`uv run python test_asset_research_brief.py`（132 项，含库检索语义/图集进参考/席位）、`node scripts/ref-outline-api-test.mjs`（29）、`node scripts/ref-report-reconcile-test.mjs`（34）、`canvas-read-channel-test.mjs`（79）。

**主题指挥图路（2026-09-11 续，用户「先规划再调研 / 同框形制不能各挑各的」）**：上一轮把「主体」做成了一等实体，这一轮让**考证大纲的主题真的去指挥图路**——规划产出文字 + 实物参考两层，且两层都按「服务哪些卡」分发。
①**终选看时代事实**（`imgresearch._select_brief`）：挑图的模型此前只看见「这个资产是什么」（本资产简报），看不见「这个时代什么形制才对」——只能判像不像、判不了对不对。现在 `research_brief` = 本资产简报 + `〈主题名〉事实`（合成口径与 `skills._inject_research_briefs`、报告卡 `cardBriefs` 三处同源：**卡上显示的 = 挑图依据 = 出图发出去的**）；select flow 的 SYSTEM_PROMPT（`agent/flows/ref-research-select.json`，已 PATCH 实例）新增第 7 条判据——考据是**判错的依据**不是参考：错年代/错形制（明清补服混进唐代、后世彩画琉璃混进北魏木构）画面再干净也不选。用户消息里也点一句「asset.research_brief 是考据结论，判形制对不对用它」。
②**时代参考池**（`imgresearch._topic_images` + 新表用法）：`run_research_outline` 执行一个主题时，除文字事实外还用主题检索词搜图（→ 下载 → 模型终选）归档成**主题图集**（`research_subject_refs` 按主题主体键），`TOPIC_REF_KEEP=6`/`TOPIC_CANDIDATE_CAP=12`（每批 50 张一次终选，别把额度烧在长尾）；**已有图集的主题不重搜**（幂等，重跑只补文字）。这是「十二个大臣戏里同框、官服形制却各挑各的互斥」的解法：同一批实物参考发给该主题的全部成员卡。**成本变了**：主题执行从纯文字变成 每主题约 3 次搜索 + ≤12 张下载 + 1 次选图调用——工具描述与手册都明写，汇报要如实说量级。
③**分发反查**（`imgresearch.topic_serving`）：卡片节点 id → 服务它的主题主体记录（带 subjectKey/era/出处）；只列**有事实**的主题（未执行的主题没有图可分）。`skills._topic_album_refs` 据此把主题图集变成第三来源，`_inject_canvas_refs` 优先级定为 **本项目自己采纳的参考卡 > 该卡主体图集（跨项目复用）> 服务该卡的主题图集（时代参考池）**，输出前统一受 `_MAX_REF_IMAGES=4` 钳制（测试锁定「自身来源打满时主题图集不挤进来」）。标签写「{主题名}（时代参考·本项目/复用《X》）」——模型和用户都看得出这张图是哪来的依据。
④**图路收检索词**（上次按住的那件，补完）：`research_asset_references` 的 assets_json 每项可带可选 `queries`（≤5），逐资产透传 `_run_batch → start_research_job`；端点 `/refs/batch-research` 同步收。**给了 queries 就不为它跑文字考据**（与面板手填词同语义）——工具描述明写这条，避免 agent 以为「给词更保险」而白白丢掉考据。
⑤**产物可见**：`build_outline` 的主题行与结构化字段带上 `refCount`（图 M 张，一次批量取图集不 N 次查询），大纲卡上「这个主题除了文字还有几张实物参考」一眼可见。
**测试注意（本次踩到的第二个坑）**：主题执行现在会连带搜图，所以**任何走「显式点名执行主题」的 E2E 都不能让它真跑**——`ref-outline-api-test.mjs` 的 D 组改成**库命中路径**（先用同 era 的另一个项目落一条主题事实，执行即 `status=reused`、零搜索），既保住「无 LLM/不烧额度」的契约又顺带验了复用；该测试的 era 也早已改成当次唯一串（主体按 era 全库共享，固定 era 会污染真实项目）。
回归：`cd agent && uv run python test_ref_report.py`（107 项：M 组锁 主题图路 mock 全链（搜→下→终选→归档）/已有图集不重搜/服务范围反查/大纲 refCount/`_select_brief` 合成两层边界 + M6 逐资产 queries 透传）、`uv run python test_asset_research_brief.py`（142 项：T 组锁 主题图集进参考序列 + 席位优先级（本项目卡 > 自身主体 > 主题图集）+ 非成员卡不沾 + 打满时主题让位）、`node scripts/ref-outline-api-test.mjs`（31 项，D2b 库命中零搜索、D2c refCount 字段）、`node scripts/research-library-test.mjs`（27）、`ref-report-reconcile-test.mjs`（34）、`canvas-read-channel-test.mjs`（79）、`scripts/shot-card-ops-test.py`（11，真出图）。

**时代参考池物化（2026-09-11 续，用户「可以」）**：主题图集从「只在图集里」落到画布——`build_report` 新增 `topicRefs`（有图集且有服务范围的主题：图带 `id`（`subject_refs` 行 id，删除凭据）、`servedNodeIds`=大纲声明的成员卡），对账（refReconcile ②c）经 `refAdopt.materializeTopicRefs` 物化：**一张图一张卡、连到该主题的全部成员卡**（「同一批时代参考发给所有成员」在画布上可见），收进同一个「考据参考」组框。关键设计：时代参考卡带 **`topicRefId` 而不带 `refCandidateId`**——主题图不在候选表里，混进 refCandidateId 通道会打错表（unadopt 广播/反向修复全按候选行工作）；删除它 = store 记 **`meta.dismissedTopicRefs`**（按图 id、上限 200、`saneDismissedTopicRefs` 整形、setProject 重置、ProjectManager 持久化/装载），对账按 id 跳过（「删了就不再来」，同主题其余图照常物化）。物化去重：任一成员已有同 URL 参考卡就不重建（`materializedUrls` 并集）；成员卡全删的主题不建。幂等由内容比对天然保证（重载不重复建卡，E2E C4 锁定）。
**考据参考组框默认展开 + 组内按真实卡高排版（2026-09-11 用户三点反馈，091102 项目事故）**：用户原话「考据参考这个分组为啥默认是折叠的呢？我觉得直接展开就行啊。而且这个分组的卡片间距有点小，容易重叠。还有为啥总共是 100 个，是只有 100 还是被截断了？」——三个问题两个是真 bug，一个是数对了：
 **默认改展开**：建组即展开（`ensureRefGroup` collapsed:false、style 按内容实算），sanitize 存量迁移同改（`hidden` 也不再置 true）；需收起时手动折叠成胶囊（`toggleGroupCollapse` 既有机制，prevSize 契约仍是 `{w,h}`——store 展开时按它还原）。**只改新建/迁移口径，不动已存在的组框**（用户手动的折叠意愿不被覆盖）。
 **重叠与「卡挂框外」两个根因**：①组内网格行距写死 footprint 高 + gap（224），而图片卡媒体自适应后高度 **121~454**（竖图是横图三倍）→ 高卡压下一行；②`refGroupSize` 返回 `{w,h}` 却被 spread 进 `style`，而 **xyflow 只认 `style.width/height`** → 组框尺寸**从未生效**（建组当刻的估算值一直挂着），末行于是伸出框外（实测一张连「盛加垟村」的参考卡越界 190px）。这就是用户看到「生成的（场景）图没放到分组里」的真相——14 张场景卡本身一直在「场景」组框内（数据与 DOM 双向核实），挂在框外的是一张**场景参考图**。
 **修法**：`packRefGrid`（纯函数）行式流入、行距 = **该行最高卡 + gapY**（gapX/gapY 24→28、padX 16→20 顺手给点呼吸）；`refCardHeight` 建卡当刻按候选图宽高比预估卡高（公式 `53 + 254 × 图高/图宽`，浏览器实测标定：26 是卡身 chrome、254 是媒体盒宽、另 27 是媒体盒来源行），图片载入后的重排再校准到像素；`refGroupSizeFor` 按**已落位包围盒**算并返回 `width/height`；`relayoutRefGroup({force})` 是唯一重排入口（force = 刚建卡整组重排；非 force = 只在同列重叠时重排自愈，否则仅同步组框尺寸）——触发点三处：采纳/物化收尾（force）、成员卡高度变化（`installRefLayoutWatcher` 防抖 400ms）、**打开项目时对账（refReconcile ②d，非 force）**，所以旧画布打开即自愈。091102 实测：41 处同列重叠 → 0、越界 1 张 → 0、组框 1152×7876 → 1148×8541、阅读顺序不变。
 **「100 个」是数对了不是截断**：81 张 = 27 个资产 × 3 张自动采纳（`AUTO_REF_TARGET=3`；单资产手动上限 `MAX_ADOPT_PER_NODE=10`）+ 19 张时代参考（主题图集物化）＝ 100 张卡；服务端 `ref_candidates` 采纳行 81、`research_subject_refs` 里 topic 行 19，条条对得上，全链路没有总数上限（组框标题的「N 卡」是子卡实数）。**排查手法**：组框徽标数 ↔ 两张服务端表分组计数对账即可判定截断。
 **采纳的撤销是整批原子**：`adoptRefRows`/`materializeTopicRefs` 只在批次开始 `commitHistory()` 一次，建卡与连线期间一律 `history:"skip"`，收尾整组重排一次（重排走 `setState`，本就不入栈）。逐张 addNode/connect 各自压快照的老写法有两个病：Ctrl+Z 只能一张一张吐、且快照里的卡还停在「组内左上角」临时坐标（撤销后几张卡并排叠在同一格上，实测 `20,44 / 304,44 / 20,44`）；逐张重排还会带来 O(N) 次 store 写入 = O(N) 次画布重渲（87 张批量采纳明显卡）。**系统路径一律 skip**：对账物化（`refReconcile`）与调研跑完的自动采纳（`autoAdoptTopRecommendations`）都不进撤销栈——用户没点过「采纳」，要退就删卡（删卡=取消采纳）。回归：`ref-group-layout-test` G 组（整批回退 + 位置不叠格 + 连线一并回退）。
 回归：`pnpm dlx tsx scripts/ref-group-layout-test.mjs`（28 项）+ `ref-group-collapse-test.mjs`（17 项，默认展开口径）+ `node scripts/ref-report-reconcile-test.mjs`（45 项浏览器对账，B8c/B8d/B8e/G4 断言展开可见与组框包容）。**别信「组框写 style 就生效」**——改尺寸一律走 `refGroupSizeFor({width,height})`。
**E2E 纪律（本组装测时踩到）**：①插在 `browser.close()` 之后的浏览器段永远跑不到（page 已关）——新增浏览器断言必须放 close 之前；②折叠组的展开按钮 aria-label 是**态变的**（collapsed→「展开分组」，展开→「折叠分组（隐藏子卡）」），前一组测试展开过并落库后，后续 reload 里它是展开态——别硬等「展开分组」，按存在与否分支；③测试崩溃（playwright 启动失败/断言抛错）会跳过尾部清理，主体按 era 全库共享、残留会污染真实复用域——`research-library-test` 与 `ref-report-reconcile-test` 都挂了 `process.on("exit")` 兜底清研究行（实测清掉 6 批崩溃残留）。
回归：`ref-report-reconcile-test.mjs` 44 项（G1-G4 物化形状/连线到双成员/不带候选 id/进组折叠、C4 幂等、H1-H3 删卡→meta 记忆→重载只少一张）、`test_ref_report.py` 113 项（M7 topicRefs：凭据 id/servedNodeIds/复用主题图集同样分发/无图集不进）、`research-library-test.mjs` 27、`ref-outline-api-test.mjs` 31、`test_asset_research_brief.py` 142、`canvas-read-channel-test.mjs` 79。
**参考图采纳自动化（2026-09-06「你帮我选啊」被拒 +「为啥没自动选」双事故）**：LLM 终选本来就产 recommended/rec_rank（`_apply_recommendation` 注释白纸黑字「自动采纳按它取 top-K」——设计本意如此、只是一直没接线），现在 `_run_research` 终选完即 `auto_adopt_top` 自动采纳推荐（只挑未采纳的，用户手动改选优先）——不再等用户逐张手勾把模型判断抄写一遍，面板随时可改选。**采纳张数由终选判断（2026-09-12 用户拍板「按参考图实际情况定——一张够就一张、图间矛盾的是噪声」）**：select flow 输出 `adopt_count`（1-3），`skills.run_ref_select_flow` 清洗透传（非正整数当缺省、多批相加作缺省、**全局精排给出有效值则覆盖**——精排是全局视角比批相加准），`_run_research` 跨轮累计 `adopt_quota`、调用点 `min(AUTO_ADOPT_PER_NODE, quota or AUTO_ADOPT_PER_NODE)` 钳到留 1 席上限（`AUTO_ADOPT_PER_NODE=3`，各模型参考上限最小 4；模型全程没给 → 回退固定 3）。提示词判断标准（flow 已 PATCH）：一张高质量参考已锁定形制 → 1 就够；图间高度一致互为补充 → 2-3；**图间形制/年代/对象互相矛盾 → 只数最可信的几张（矛盾参考一起采纳就是噪声）**；近似重复（同画面/同来源截图）只占 1 席。起因：bf32f21a4f3f（屈原与端午）实测 SC1 汨罗江畔自动采纳的 rank1/rank2 是同一篇文章的两张图——近似重复占双席。措辞同步：graph.py 五处 + script-to-assets/asset-aware-generation 两本手册「top-3」改口「1-3 张（张数由终选按参考图一致性判断）」；`ref-adopt-tool-test.mjs` 断言放宽 1-3（auto_adopt_top 一次性落库，adopted>0 即落完）。回归：`test_ref_select.py` 96 项（A9-A12 透传/清洗/精排覆盖/精排失败回退、C12 adopt_count=1 只采一张、C13 跨轮累计 2+2 钳到 3、C1 锁缺省回退 3）。`adopt_asset_references` 后端工具保留作**调整**用途（「每个资产带 5 张」类补齐请求，按 rec_rank 补到 N，上限 10=MAX_ADOPT_PER_NODE），get_reference_research_status 收尾提醒同步改口（换图→面板改选/调张数→adopt 工具），asset-aware-generation 规则 3 明写「不要以没有勾选能力回绝」；research_asset_references 的返回串与防幻觉校验同步——node_id 不在画布时报错**并列出可用卡清单**（模型曾自拟占位 id「待确认」发起，靠错误里的清单下一轮自纠，不必再查画布）。**langflow 连接池打崩事故与三层修复（2026-09-12 下午，屈原 51 资产 + 多项目批次同跑实测）**：症状——langflow SQLAlchemy `QueuePool limit of size 20 overflow 30 reached`（池满后 flows API 全 500/挂起，正在跑的调研终选整批失败，报错只剩「终选：第1批：」空串无从排查）。三层根因与修复：① **默认池给 PostgreSQL 调的，SQLite 该调小**（`lfx/services/settings/groups/database.py` 注释原话：SQLite 写并发有限、锁竞争应减池）——SQLite 写锁串行，每个 flow run 都写 build/log，池越大写等待者越多；`setup-langflow.sh` 启动行加 `LANGFLOW_POOL_SIZE=10 LANGFLOW_MAX_OVERFLOW=10`（经根 Settings 的 `model_fields_set` 路径生效，已验证 env 映射；组配置 `DatabaseSettings` 本身不读 env，别单测它）。② **agent 侧 flow 调用无闸**——`BATCH_CONCURRENCY=20`/批、多项目批次叠加时并发 plan/select flow 调用 40-100；`skills.py` 加 `_FLOW_CALL_CONCURRENCY=10` 全局闸（`_flow_gate()` 按事件循环惰性重建——asyncio.Semaphore 绑首个循环，跨 `asyncio.run` 会炸，测试多循环安全；`run_ref_plan_flow`/`run_ref_select_flow` 改为「公共入口=闸内调 `_impl`」，imgresearch 四个调用点与测试 fake 全走公共名，fake 替换=绕闸，主流程不受影响）。③ **`_resolve_tweak_keys` 每次终选前 GET 一次 flow、15s 超时、httpx 异常空串裸抛**——51 资产批 100+ 次纯浪费 GET，拥堵时这些调用还占池等待位放大雪崩；加 **TTL 300s 缓存**（`_TWEAK_KEY_CACHE`，flow PATCH 后最多 5 分钟旧 id，langflow 对未知 tweak 键静默丢弃→组件报「payload 为空」自愈）+ httpx 异常包成带类型名的 `_TweakKeyError`（超时/连接错误不再空串）+ 超时 15→30s。**运维教训**：langflow 手动重启必须清 SOCKS 代理变量（`env -u ALL_PROXY ...`，否则 httpx 启动即 ImportError socksio）；事故批次里拥堵窗口跑终选的资产零采纳，用报告卡「补调研」补。回归：`test_ref_select.py` 105 项（G 组 9 项：TTL 命中不 GET/过期重取/新键合并/httpx 异常带类型/并发闸峰值≤10）。**setup-langflow.sh 全量重跑会在启动后卡在后续阶段（本机实测 18 分钟无进展，原因未查）——用它重启 langflow 时起服务就够，卡住可 kill，flow 导入阶段没到也无妨（库里 flow 已是最新）**。**实测定论 v2：召回真凶 = 查询形状 + 预筛阈值，不是 Google 没图（2026-09-12 修正，用户质疑「Google 按理说效果更好」推动复测）**：上一轮「中文互联网没有可索引影视美术图」的结论是错的——对照实验：**「温州一家人 剧照」（2 词）能搜到官方剧照页**（电视指南网/新浪娱乐/百度百科），而 planner 同款复合查询「温州一家人 乡村外景 村道 石桥 空镜」（5-7 词）只能搜回自驾游与旅游盘点。两层真凶都已修：① **查询形状（主凶）**——旧 planner 提示词要求每词带美术角度限定（美术设计/置景/幕后），堆出长复合查询触发 Google 松相关退化。planner 提示词已重写（flow 内嵌代码，已 PATCH；**ref-research-plan.json 含并行会话 staged 基线改动，留工作区不提交**）：**锚查询优先**（资产能关联到剧时首轮必含「剧名+单角度」，剧照池=同剧同年同形制是最稳来源）、每查询 **2-4 个普通词**、角度词（剧照/定妆照/取景/空镜/服饰）**只用一个**、首轮 3-4 词；幕后特辑类内容靠终选看来源语境分辨，不靠搜索词硬凑。② **预筛阈值（帮凶）**——漏斗测量「温州一家人 剧照」10 条被 400px 硬线拦掉 7 条：新浪娱乐官方剧照全是 **550x367**（2012 年老剧通稿标配），活下来的反而是百科与旅游页。`_MIN_IMAGE_SIDE` 400→320，终选提示词分辨率条款同步（≥320 可用、<320 原则上不选；已 PATCH）。**端到端四跑对照**（雪湾村，真实链路）：采纳从「武汉黄陂/论坛配图」变为「温州一家人官方剧照（模型确认空镜镜头）+ 乐清渡头村实拍 3442x2581 + 浙江民居形制图」——同剧、同地域、有形制，参考图语义第一次成立；12 词 67 候选 288s，判停与推荐达标双出口都工作。gl/hl 地域参数实测无差别（不加）。遗留：下载层仍有损耗（搜狐 SSL/防盗链、抖音百科空响应，~20-30%）可接受；终选对虚构资产偏严（旅游取景地图「形制不匹配」被拒）是刻意的宁缺毋滥。回归：`test_ref_select.py` 62 项（B 组含 550x367 放行断言）。**终选来源页语境佐证（2026-09-12 P4，治「文章题图被当参考」）**：终选此前只看 512px 缩略图+标题+宽高——SC_5 采纳的三张全是搜狐/知乎/澎湃的报道题图（画面正常、语境错），看图看不出来、看页面聊什么一目了然。现在 `_run_research` 每轮终选前并发抓候选来源页语境（`_fetch_page_context`：httpx **直抓** HTML → `research._strip_html` 剥正文开头压一行 ≤200 字；**不走 Jina/TikHub 重通道**——佐证只要「这页在聊什么」，导航文字就够，别跟考据文路抢无头浏览器；8 并发、40s 整体死线、失败空串软放行），`_select_payload` 带 `context` 字段入载荷；flow `_meta` 显示「页面语境：…」、提示词新增第 2 条「来源语境」（正文聊剧情/收视/八卦→题图降权；美术设计/置景/幕后→优先；语境为空≠图不好）。**实测修掉两个实现缺陷**（132 个来源页：乱码 34、只剩标题重复 59 —— 信号 84% 作废）：① **GBK 解码**——中文老站（新浪/网易 2000s 页面）不声明编码、内容实为 GBK，httpx 的 `resp.text` 默认 UTF-8 整页乱码；`_decode_html` 按 content-type charset 解、未声明则 utf-8 严格试失败退 gb18030（乱码 34→1）。② **取正文不取导航**——取页面开头 200 字拿到的是导航与标题；`_page_context_from_html` 摘要优先（meta description / og:description，页面作者写的「这页在聊什么」），无摘要才退到正文 `<p>` 段落并跳过短行。改的是 flow 内嵌代码（运行时真相），**改后须 `./scripts/update-flow.sh ref-research-select.json`**（注意脚本自拼 `agent/flows/` 前缀，只传裸文件名）。回归：test_ref_select D/E/F 组。
**终选排序即采纳序 + 先搜后判再补（2026-09-12 双修，091102 项目实锤「检索一两百张、选出来的很差」的根因）**：① `run_ref_select_flow` 曾返回 `sorted(set(recommended))` 把终选模型按适配度排好的顺序销毁成搜索位次序——`_apply_recommendation` 按列表顺序排 rec_rank、`auto_adopt_top` 按它取 top-3，实际采纳的是「推荐集里位次最靠前的 3 张」（SC_11 采纳了「千万工程」2020s 新农村照与现代美术馆，模型想要的 80 年代图排在 rank4+ 全没被采纳；库里签名=全部节点 rec_rank 与 idx_total 完全单调——模型不可能按索引排序，那是 sorted() 的确定性产物）。现**保序去重返回、模型序=采纳序**；多批（>50 候选）时把推荐集（≤18 张）再跑一次**全局精排**（业界 reranker 范式——Contextual AI 实测 reranker 是全链最大杠杆，去掉 nDCG@5 掉 56%；批间序只是搜索位次的回声，不精排 top-3 永远出自前 50 张），精排只定序不砍推荐（排落的名次垫尾）、失败软降级为批序合并并在 note 明说。② `_run_research` 重构为「搜→下→终选」逐轮推进：AI 模式首轮 3 词广撒网，每轮下载完**就本轮新增做终选**，推荐累计 ≥`_SELECT_ENOUGH_RECS`(4) 即停（auto_adopt 取 3 留余量；终选提示词每批推荐上限 6），不足才带「终选推荐 N/M」证据补搜——planner 只看标题判不了候选质量，旧结构按数量想象补搜成每资产 17.6 词、136 张候选（510 次 Serper/项目）；planner 的 enough 退居次级判停，终选失败不盲搜（保住已入库候选收工、errors 明报，用户面板手动采纳）；文路简报 join 提前到首轮终选前（文字定边界→图像做选择的时序不变）。**逐轮插入必须给 `_insert_candidates` 传全局 start**——idx_total 按插入批次从 0 编号会撞号，面板按 idx_total 排序会让两轮候选交错，且位次就是终选的 index 空间。③ 搜索层下载前预筛 `_prefilter_candidate`：短边 <400 拒（终选本来就拒 <480「除非信息独特」，中间地带留给模型裁量；宽高元数据缺失照收）、旅游电商/素材站域名黑名单拒（子串匹配 sourceDomain——Serper 的 source 字段带中文前缀如「人民图片- 人民网」；Pinterest 等图库不进黑名单）——明显不够格的图不再占下载带宽。回归：`cd agent && uv run python test_ref_select.py`（40 项：保序/去重/多批精排/精排降级/预筛/首轮即停/证据补搜/终选失败不盲搜/手动词路径）。**判停必须有无进展出口（2026-09-12 端到端实测补）**：`_NO_PROGRESS_ROUNDS=2`——「推荐不足」有两种含义（还没搜够 / 搜索源根本给不出），只按前者判会一路补满 5 轮烧额度：实测雪湾村每轮终选都报「候选均缺乏片场美术、置景设计语境」，推荐恒 3 张够不到判停线 4，白跑 23 词 110 张；加「连续 2 轮零新推荐即停」后降到 2 轮 8 词（-66% 耗时）。顺带修 `job["note"]` 只在有推荐时赋值——无进展判停的原因用户看不到。**缺口台账回流（2026-09-12 A 档，业界 deep research 的「看缺口再搜」范式）**：对照四个开源项目后确认的业界共识——每轮检索后必有一个评估节点，以结构化 JSON 产出「学到什么 + 还缺什么 + 下一步搜什么」，下一轮由缺口驱动（而非轮数驱动）；agent 只是外壳之一（dzhng 纯递归、Firecrawl while 循环都没 agent）。我们此前的反馈信号是**数量**（「终选推荐 N/M」）而不是**内容**——planner 知道这轮不行却不知道为什么不行，而原因一直躺在终选的 note 里没回传。现已打通：终选 flow 输出加 `covered`/`missing`（维度名要具体到可据以出检索词，「巷道近景与铺地」而非「细节不够」）→ `run_ref_select_flow` 透传（_str_list 清洗/保序去重/形状异常当空不拦终选）→ `_run_research` 跨轮累积经 `_gap_clause` 写进下一轮 planner 输入的 found → planner 提示词「定向补缺」段。**实测**：终选报「仍缺：土路接石桥的村口关系/窄巷与红砖围墙/门楼院落/俯瞰全景」→ 下一轮出词「乐清 老村 石桥 村口 剧照」「温州沿海村落 窄巷 红砖围墙 空镜」「雪湾村 航拍 空镜」，逐条对应；此前只会出「温州一家人 剧照」这类泛角度。**已知下一层缺口**：定向补缺能命中维度但不保证画面可用（缺「石桥村口」搜回桥梁档案照，形制对但是资料图，终选仍推荐）——B 档覆盖度判停需区分「有图」与「有好图」。研究报告：`doc/deep-research-search-loop-research-2026-09.md`（含四个项目的回路机制对照 + B/C 档方案 + 「不建议做查询扩展」的反例）。回归：`test_ref_select.py` 72 项（A6-A8 缺口透传/异常形状/条目清洗 + C7 缺口必须进下一轮 planner 输入）。注：planner 提示词改动在 `flows/ref-research-plan.json`，该文件压着并行会话 staged 改动，留工作区未提交。**缺口追补判停 + 实景/物料判据分界（2026-09-12 B 档，commit 7301303）**：① 判停从「推荐数 ≥4 就停」升级为**缺口参与决策**——推荐数只回答「有没有能用的图」，缺口回答「有没有对路的图」；推荐够但 `missing` 非空时定向追补，上限 `_GAP_CHASE_ROUNDS=2` 轮且要求缺口在缩小（`missing_now < prev_missing`，补不动止损）。② 终选 `covered` 门槛收紧为「只有能当生图画面参考的才算覆盖」（档案照/线稿/图表/新闻配图/商品页不算），**但收紧后实测出反向问题：旅游游记里的实景村落照片对生图有用（真实砖木老屋、巷道尺度、材质），却因带「旅游」标签被一起拒 → 零推荐**（诚实但无用）。修正为「**纪实照片可用，只拒宣传物料**」：景区官宣海报/门票/攻略封面拼图/压文案的图才拒；优先级 影视置景 > 实景纪实 > 文旅物料(拒)；并明确「不要把『文旅街区是仿建』扩大成『与景区沾边就拒』」（区别在画面本身：仿建常有崭新仿古构件与商业招牌，不在来源域）。**实测**：修前 0 推荐 → 修后推荐 11 采纳 3（温州老村实景形制参考），covered/missing 均为真实维度，下一轮出词逐条对应缺口。**方法论教训**：收紧判据必然伴随过度拒绝，**判据改动必须真跑一轮看产出**——单测只保证契约、保证不了模型行为（本轮「资料图不算覆盖」收紧后没实跑，实跑才发现零推荐）。回归：`test_ref_select.py` 79 项（C8 追补后缺口清空即停 / C9 不收敛止损 / C10 收敛但达上限即停）。**终选判据按类型分化 + 地域分级（2026-09-12 四类实测定案，commit caf92f8）**：用真实项目 091102（1980s 温州抬会案，无影视剧对应）的角色/服饰/场景/道具各一个代表跑真实链路，暴露并修掉三处一刀切问题。① **类型差异必须建模**（代价最大）：scene/character 要「在场景中/穿在身上的真实影像」，costume/prop 要「这东西长什么样」——对后两类实物照/博物馆展陈/结构图/局部特写**恰恰最有用**；B 档「资料图不算覆盖」一刀切同时造成两个方向的错（prop 该收的没收→零推荐、costume 判据失效→形制图被放行）。现按 type 分化：画面类要实景影像、形制类收实物/展陈/结构/特写，共通拒绝项（拼贴九宫格/商品白底图/压文案宣传品/报纸版面）不变。**实测 prop 零推荐 → 3 张采纳**（温州夫妻记账/金融手写/民间文书）、costume 采纳形制图 + 警服历史实景。② **地域/年代约束分级**：硬拒会连「温州/浙南老街作苍南参考」一起拒（scene 零产出），改为跨省跨大区/现代仿古 → 一律不选；**同文化区/相邻地域 → 降级参考但必须标注**（note 写地域差异、missing 写「无本地照片，以同区域参考替代」）——**零参考不如标注清楚的近似参考**；实测 scene 恢复 6 推荐 3 采纳（含「苍南霞关老街」地域全对）。③ **planner 防编造**：实测它给「郑乐芬」（真实人物、无对应影视剧）出词「温州一家人 郑乐芬 剧照」（该剧无此角色，同地域误当同剧）——不确定就用「年代+地域+对象」出词，别编对应关系。**遗留**：零产出资产的用户引导（1984 钱库镇老街照片网上确实不存在，系统能精准报 missing 但用户在面板上能做什么未设计）；跨大区漏网率待观察（不为单例调提示词，防过拟合）。回归：`test_ref_select.py` 79 项（本次仅改 flow 内嵌提示词）。**两组对照实测：有影视参照 vs 无（2026-09-12，commit d9b8087）**：用两极各跑四类——对照结论：**有影视对应的资产，character/costume/prop 质量显著更高**（character 直接拿到同人物剧照、costume 拿到剧集服装解析、prop 甚至拿到博物馆实物如「上博绿釉烛台」）；**场景类是例外，两组都难且难因不同**——无影视对应（钱库镇老街）缺「那个具体地点的影像」（1984 年苍南小镇照片网上不存在）、有影视对应（唐代宫殿）缺「那个时代建筑内部的真实影像」（现存唐代木构仅个位数且少内部照，影视剧多为明清化搭景）。本轮另修两个缺陷：① **3D 模型渲染图被采纳**（「唐代宫殿大殿室内SU模型-草图溜溜网」）——planner 早有「不要用概念图/模型/素材这类词」的禁区，但**终选侧没有对应拒绝条款（上游禁区下游没兜住）**，现补「3D 渲染图/素材站预览图/AI 生成图一律不选」+ 判据「这张图是拍出来的还是画/渲染出来的」；② **来源可核实性缺判据 + 多轮下标准漂移**——scene 第 2 轮自述「仅有明清建筑影像，不能覆盖」，第 4 轮却采纳 Pinterest「伤感说说吧」的无信息标题图，现补「来源不可核实的候选只有画面含明确时代特征才可选」+「标准每一轮保持一致，推荐为空的代价远小于给出错年代的参考」（复测 scene 推荐 4→1 收紧生效，其余三类仍 3 张）。**核心结论：提示词能收紧标准，但变不出不存在的资源**——场景类资产（尤其历史建筑内部）的参考图获取是结构性的难，出路在 ① 剧集视频抽帧 ② 形制文字考据 + AI 生成 ③ 产品上明确告知「此类资产网上无合格参考」并给替代路径（系统已能精准报出 missing 清单，缺的是面板侧引导设计）。**无参考图时考据升级为「唯一形制依据」+ 画布侧参考核查（2026-09-12 路径 2，commit ef69459）**：资源稀缺资产（场景类尤其历史建筑内部）走「形制文字考据 + AI 生成」。**通道早已存在，缺的是强度分级**——`_inject_research_briefs` 一直把简报并入 visual_notes 并标「考据依据（真实形制与年代，优先遵循）」，但这个措辞不分「有参考图时考据是辅助」与「无参考图时考据是唯一依据」（091101 武则天事故：52 张资产考据全到、参考图 0 张，agent 一路出图都没觉得不对）。三处补齐：① `_attach_brief` **按有无参考图分级措辞**——无图时「考据依据（**本资产无可用参考图，以下文字是形制与画面的唯一依据，必须逐条落实到画面**）」，**哨兵词「考据依据」两种措辞都保留**（`_ensure_research_brief` 靠它去重，改措辞别把它弄丢）；② **注入顺序 refs → briefs**（`generate_asset_images` 与 `start_storyboard_image_job` 两处调用点）——分级要先知道参考是否落地，两注入互不依赖所以换序安全；③ **画布侧参考核查**：聊天侧早有 ref_gap 明报但**画布直出不经过 agent 用户看不到**，现落 `job.refGap` → `GET /storyboard/images/{id}` 返回 `ref_gap` → `pollShotImageJob` 首次拿到时 toast（只弹一次，7 个批量出图调用点共用此入口），只提示不拦。验证：真实注入链路上无参考资产走「唯一依据」、有参考卡连线资产走「优先遵循」。回归：`test_asset_research_brief.py` 146 项（新增 I2 分级四断言）。**缺口台账矛盾修剪 + 第三轮多场景（2026-09-12，commit 90d543c）**：① **上下文审计真 bug**——终选每轮只看本轮候选，会把前轮已覆盖维度换个说法再报进 missing，planner 同时收到矛盾信号（「已覆盖：巷道近景」+「仍缺：村落全景航拍的俯视构图」）；修 = 累积时**双向子串匹配修剪**（自由文本维度变体视为同维），C11 锁行为。② 单卡出图（资产卡直点生成）的独立轮询循环补上refGap toast（与 pollShotImageJob 同口径）。③ 五新场景实测：**调研链路无题材真伪意识**（imgresearch 不读 factuality——虚构项目照跑史实文路与年代判停，实测行为可接受：planner 自发映射「庆余年/九州缥缈录 剧照」、采纳故宫藻井元素图，记录为已知不修）；中文检索西式内容完全可行；重跑语义完全正确（37 行 = 新 34 + 旧采纳 3 保留，采纳 3+3）。④ **B 档判停的实测代价**：缺口追补几乎总触发（终选几乎总报 missing），常态 = 推荐够 + 追 1-2 轮，词数耗时约 ×2，`_GAP_CHASE_ROUNDS=2` 兜底；成本不可接受时可收紧为「missing ≤2 才追」。回归：`test_ref_select.py` 83 项。**壁画/人俑与古画判据（2026-09-12 看图专项，commit e6245c9）**：用户反馈「很多图是壁画/人俑，作生图参考效果不好」。专项实测（唐代女性人物/服饰）+ 看图验证：壁画/人俑已被治住（planner 禁区 + 终选拒展陈，43 候选 0 命中）；但**传世古画钻了空子**——《簪花仕女图》被 character 采纳（绢本古画，看图确认），「拍下来的画册图」骗过「拍出来的还是渲染出来的」判据。修复分级：**画面类拒绘画作品**（古画/壁画/绢画/工笔/线描/画像砖/俑像——再权威也是画，作参考带偏画风；优先序 剧照/定妆照 > 装束复原真人实拍 > 纪实照片）；**形制类画作降级**（无摄影时代的合法形制证据，池内无实物/复原/剧照时才可选，note 必须标「源自画作（有画风差异）」）。复测：唐代侍女采纳从古画×3 → 妖猫传摄影专访/长安十二时辰服饰解析/杨贵妃剧照。**方法论：判断图的质量必须看图**——标题与来源域只够粗筛，本轮三张关键图的标题都看不出实际内容（候选图在 agent/static/assets，可直接 Read 看图验证）。回归：`test_ref_select.py` 83 项。**调研产物使用链路验证 + 风格前缀重复修复（2026-09-12，commit 2634f6f）**：真出 1 图读落库 finalPrompt 验证「调研→生图」最后一公里：参考图进生成（逐图职责段按位次列出）、考据简报全文进提示词（含常见误用判错依据）、每张参考图有专属职责（juben build_reference_usage 范式，refSource=research → type=reference → 「不继承背景、展陈环境、拍摄角度与光影」的考据参考职责）。**实锤并修**：finalPrompt 渲染成「全局视觉风格（…）：全局视觉风格：写实影视质感」双重前缀——前端 4 处与聊天工具都先带前缀、flow 模板 {visual_notes} 槽位外层还有同名标题；修在 `_generate_single_image`（全路径汇聚点）构建载荷前剥开头一次，**刻意放在 compose 之后**（指令合成按该前缀提取画风行，顺序换了会破坏扩写）。回归：`test_asset_research_brief.py` 150 项（I3 三断言含 compose 路径）。**注意候选入库≠调研完成**：total>0 只说明搜索阶段落库，模型终选跑完 batch 才 done（E2E 轮询要打 batch-research 端点或直接等 adopted 出现，不要解析 agent 措辞取任务号——「任务 ID：」/「batch_id：」说法不定）。回归：`node scripts/ref-adopt-tool-test.mjs`（真调研 1 资产→断言完成即 adopted=3→「带 5 张」补齐到 5 落库，约 5 分钟）。**调研范围默认全量（2026-09-06 090602「55 只调研 16」事故）**：用户说「给资产做调研」不点名范围时，agent 曾自挑 16 个「重点资产」发起而用户以为全做了——工具描述 + asset-aware-generation 规则 3 现都明写「画布资产卡**全部传入一次调用**，不自挑子集；点名了才收窄；>60 个或用户在意时间先问一句带默认」。「N 个全部纳入」要说在**回复文字里**——曾写成「发起时明说」，模型把说明塞进 assets_json 尾部（`]}` 后跟一句话）把 JSON 弄非法；现在工具报错文案点名「JSON 到 ] 即止、修正后原样重发」教下一轮自纠。并发口径如实：`BATCH_CONCURRENCY=20`（每 20 个一波、每波约 4 分钟），返回串估时按它算（曾虚报「100 路并发」把 55 个估成 4 分钟、实际 ~12 分钟）。回归：intent-routing-test S5（mock 画布 6 资产→断言一次调用带全部 6 个 node_id；E2E 断言读工具 args 注意 `\"node_id\"` 是转义内嵌字符串，regex 前先反转义——JSON.parse 整串也会因尾部偶发说明文字炸，宽容提取即可）。**上下文工程口径：正文注入不截断（2026-09-04 用户拍板「全站都不要字数截断，容易出问题」）**：`CONTEXT_BODY_LIMIT` 已删除——visualNotes/行出图/聊天 @ 引用行/文本撰写/AI 写设定 context/模板弹窗（运镜·灯光·图片模板）/调研补研全部全量注入正文，截半句设定曾让生成丢关键信息；展示层截断（chip 标签/tooltip 预览/CSS truncate）不受影响照旧；画布摘要 v2 的预算是「索引+按需拉取」架构不是截断，另论；**画布摘要 v2 = 索引+按需拉取**（影策 canvas-context 范式，2026-09-03）：头部「计数/类型分布/⚠警告（生成失败·生成中）/版本 rN」任何规模恒在，锚点卡（剧本/分镜表/调研）置顶永不丢，连线列清单上限 20 条，超预算尾部明示走 `canvas_query` 检索（query/types/status/resourceOnly 过滤，返回媒体 URL——聊天出图 reference_images 的查询源）、`read_node` 读全文（分镜行 rid + 邻接连线）；媒体标记⟨图/视频/音频:URL⟩仍随节点行。配套前端工具 `canvas_validate_ops`（干跑校验，顺序敏感：占位符同批引用、猜 id/重复连线/自连/删后再引用报错、rows 资产名告警；复杂批量先干跑再 canvas_ops）。**applyOps 每条 op 实时取 state**——循环外快照曾让同批「建卡即连线」必报不存在（27 卡事故连线全灭的另一半根因）。**agent 批量建卡自动分组排版（2026-09-05 c3e3e1c94d66 事故）**：canvas_ops 未带 position 的 add_node 由 `planBatchLayout`（ops.ts）在现有内容下方开「资产带」——角色/服饰/场景/道具各收进同名组框（**服饰紧跟角色**，见下）、组内 √n 列网格（与剧本卡拆解链路同款）、组框整块 findFreePosition 避让，单项不套框，非资产卡带尾普通网格；显式 position 尊重坐标不参与分组（canvas_ops 工具描述教「建卡一律不传 position」「资产不建 note 加前缀」——事故里 agent 因描述缺 scene/prop/costume 枚举把 15 张资产建成「场景：/道具：/服饰：」前缀 note 摊成 8700px 横排）。旧 `autoPosition` 恒返新列（tooClose 判定是永真式）已删。**groupNodes 曾双垫组节点**（set 数组首尾各一份、同 id 重复被 React Flow 静默吞）已修。回归：`scripts/canvas-read-channel-test.mjs`。**资产带顺序：服饰紧跟角色（2026-09-11 用户口径「服饰模块是不是应该和角色挨着」）**：此前顺序是 角色|场景|道具|服饰，091001 实测组框 x 为 -36/1028/2092/3156——角色组右缘（1020）到服饰组（3156）隔了 2136px（正好是场景+道具两组），要找「某角色这身作案装长什么样」得横穿两组框。服饰不是与场景/道具平行的「又一堆资产」，**它有主**：造型计划把服饰绑在具体角色上（角色卡 `looks[i].costume` → `costumeId`）、造型图出图的「参考图2=服饰结构图」就是为那个角色锁形制、画布上另有 服饰→造型卡 连线；场景/道具是环境与物件家族，与角色无绑定。改法=**两处顺序常量同改**（`ops.ts` `LAYOUT_KIND_ORDER` 与 `nodes.tsx` 拆解链 `KIND_ORDER`，都成 `character, costume, scene, prop`）。拆解链的自然结果是 **角色|造型图|服饰|场景|道具**——「造型图」框本就是贴角色组右缘放的（角色迭代时在 groupLeft 里给预留位），服饰排在角色后一位正好落在预留位右侧，两条派生边（角色→造型、服饰→造型）都不横穿别的组框；反过来排成「角色|服饰|造型图」则造型图要另找锚点、且角色→造型的线会压过服饰组，不划算。**只影响新建批次**（新拆解 / agent 新批量建卡），存量画布不重排（想对齐得重拆或手动拖组框）。菜单类顺序（类型选择器 `NODE_TYPE_ITEMS` / @ 候选分桶 `TYPE_ORDER_KEYS` / 底栏版式 chip）**不动**——那是候选与菜单顺序，与画布布局无关。回归：`canvas-read-channel-test` 同批补 3 项（四类各成一框 / 服饰框紧挨角色右缘且先于场景道具 / 场景道具退到外侧，实测 x：角色 -72 · 服饰 644 · 场景 1360 · 道具 2076）。**系列卡跨轮成列 + 整批收框（2026-09-09 罪案策划0909 项目散射事故）**：用户反馈「画布节点太乱」——agent 跨轮逐张建的 11 张策划 note 卡斜跨 2700×2800、零组框、集序视觉错乱（诊断注意：**ChatPersistence 只存 content 纯文本、不存工具调用**，查聊天记录没有 add_node 不代表 agent 没调 canvas_ops——建卡路径只能靠位置形态反推，别把「聊天无记录」当证据）。两层修复：①**机制层** `planBatchLayout` 纯 note 批次（批内全部 positionless add_node 都是 note）锚点从「全局内容左缘下方」改为**最近一张 note 卡正下方同列**（`absolutePosition` 沿父链取绝对坐标，组框内的卡也能续接；findFreePosition 只纵移保 x）——系列跨轮逐批建卡自成一列、集序自上而下可读，不再沿内容左缘斜向漂移；混合批次（含资产/媒体）保持全局锚点，资产带语义不变。②**行为层** canvas_ops 工具描述加「系列/分集策划卡」段：整批一次落卡（数组顺序=阅读顺序）、标题带序号（「第 01 集《XX》·单集策划」）、同批 group_nodes 收『<主题>·分集规划』框（同批占位 id 引用已支持，回归锁定）；**group_nodes 已摘出审批名单（同日用户拍板）**——分组可逆（解组即回）、只挪位置不毁数据，走审批卡会卡住「整批落卡+收框」的流畅度，审批门收窄到只有 delete_nodes（chat-tool-cards E2E 只测删除审批，不受影响），applyOps 纯函数层不设门（测试直连不受阻）。回归：`canvas-read-channel-test` 48/48（同批系列网格序=数组序/占位 id 收框/跨轮续列 x 不漂/混合批次全局锚点）。**文本卡建卡尺寸（2026-09-09 090803 项目事故，同日三轮迭代，终态=按正文实算）**：用户反馈「画布上弹出来的卡片偏小」——浏览器实测 1302 字策划卡是 280×170（标题行 20 + 卡面 146，正文 12px/19.5px 行高 ≈ 7 行可见），根因是 `NODE_FOOTPRINT.note` 仍是 2026-08-29「便签→文本节点」时代的尺寸，而 note 现在承载策划案/选题池候选卡/上传文档全文。**固定分档先后两版都被推翻**：首版三档（<300→280×170/≥300→480×360/≥2000→560×480）把 102-261 字候选卡关在便签框只露 ~6 行；二轮四档（<100/100-499/500-1999/≥2000）仍把 500 字卡截在固定高度里——固定档总会截住某段字数，用户口径「**尽量能显示全文字**」。终态 `store.noteFootprintFor(body, linkCount=0)`：宽度按内容量选行宽档（<100 字 280 / <400 440 / <1200 480 / 其余 560），**高度按正文实算行数**（汉字全宽 12px、半角折半、8% 换行余量、逐 \n 段计算；固定开销 74 = 标题行 24 + 卡内边距 24 + 字数行 ~26；links 来源行每条 16px 计入——候选卡带出处行也放得下），钳在 [170, 760]：短卡仍是便签、中长文全文可见、超上限才滚动。落点两处——① `ops.ts` add_node（agent 建卡），**布局格子必须用同一 footprint**（`planBatchLayout` 的 per-kind 网格、others 网格与 `placeGrid` 都走 `footprintOf`/`maxFootprint`，否则长文 note 压到邻卡上）② `ingest.addCardAt/addDocCard`（聊天上传文档落卡，此前 150KB 剧本全文也塞便签框）。只影响**建卡初始尺寸**：手动缩放、agent update_node 都不动。顺带修 add_node 的 style 三来源（分组默认/stagger CSS 变量/实算尺寸）合并成一个对象——分开 spread 会互相覆盖，实算尺寸会被 stagger 打回便签框。**存量归一走 sanitize**（research 卡 320×220→480×560 同款先例）：`resizedNotes`——style 精确等于**历史默认档全集**（280×170 / 440×340 / 480×360 / 560×480，含两版分档期产物）且**顶层无 width/height**（xyflow resize 只写顶层，有值即用户手调过）才按正文实算归一（可升可降），算出来与现值相同不计数——归一结果不在历史档集合，天然装载幂等；首版「存量不追溯」口径作废（用户看着旧项目还是小卡=没修）。回归：`canvas-read-channel-test` 43/43（三档宽度/实算高度/760 钳制/links 计入卡高/布局不叠卡/级联变量共存/sanitize 归一连：升档·手调不动·同值不计数·分档期策划卡 560×698·幂等）。**候选落卡 links（2026-09-09，共创契约的画布载体 P1）**：用户反馈「候选要在画布上展示、能点链接看故事背景」——note 卡新增 `data.links:[{title,url}]`（上限 6 条、title 60 字、仅 http(s)）。四层：① `ops.ts` add_node/update_node 白名单 + `sanitizeLinks` 整形（危险协议 javascript: 等剔除）；② `sanitize.ts` 装载边界再兜一道（非数组/缺字段/危险 scheme 整条清掉，`fixedLinks` 计数）；③ `nodes.tsx` TextCard 正文下方渲染可点「来源」行（样式同调研卡来源底账，`target=_blank`+`nodrag nowheel`+stopPropagation）；④ canvas_ops 工具描述教「候选池落卡」口径——出候选清单时一个候选一张 note 卡（背景+关键事实+材料底数）+ links 出处 + 整批 group_nodes 收「候选池·<主题>」组框，推荐在聊天里说、落卡的是全集；画布摘要候选卡行带「（来源 N 条）」标记。E2E 注意：links 无 UI 入口，浏览器验收走 PUT 画布落库→打开页面断言 `<a>` 渲染。回归：`canvas-read-channel-test`（links 透传/危险协议剔除/sanitize/摘要标记，36/36）。**长对话滚动压缩（2026-09-04）**：chat_node 每轮估字符量（≈token 上界），超 `CHAT_COMPRESS_THRESHOLD_TOKENS`（默认 400k）触发——保留最近 ~40% 预算消息原文、其余并入 `history_summary` 滚动摘要（LLM 折叠 ≤1500 字保主角/决策/偏好），`summary_count` 记边界（不切 tool 配对），均存 checkpoint、系统提示注入「更早对话摘要」节；**替代旧 messages[-14:] 硬截断**（阈值内全量原文可见）。注：LangChain SummarizationMiddleware 绑 create_agent 栈不可直接用，按 LangGraph summarization 范式自实现

- **深度调研卡契约（researchId 是卡面的命，2026-09-04 白骨精事故）**：调研卡（nodeType=research）一切前端行为——进度轮询/终态显示/卷宗内容——只认 `data.researchId` 数据字段。事故：agent 建调研卡把 jobId 写进正文文本，卡成死卡（状态永远「…」、卷宗按钮点击无效），40 来源 103 事实的卷宗躺在库里用户什么都看不到，问了 agent 才在聊天框倒出长文。三层堵死：① `canvas_ops` 工具描述 + `ops.ts` 契约声明 `researchId`（add_node 必带、update_node 可补挂）② `validateOps` 干跑 error + `applyOps` 硬拒（缺字段整 op 不落，明报教学文案）③ sanitize 存量回填（正文「深度调研任务：{12位hex}」→ researchId，幂等）。**调研卡=文档型卡（2026-09-04 用户拍板：卷宗全文直接上卡、不折叠、不藏弹窗——viedeo「节点即编辑器」范式）**：任务头（状态行+计数+⤢全屏钮）常驻，正文区 `nodrag nowheel flex-1 overflow-y-auto` 滚动渲染卷宗五段+来源底账全展开；运行中显示观看问题+日志尾部（调研过程可见不是黑盒）；S 编号引用 chip 上卡（`ResearchCite.tsx`——悬停来源卡**必须 portal 到 body**，卡内滚动容器会裁 absolute 弹层）；卡正文只读（卷宗是事实权威，改写走聊天/后续「转文稿」）；`NODE_FOOTPRINT.research` 480×560，sanitize 精确匹配把旧默认 320×220 升档（手调尺寸不动）；ResearchReader 降级为可选全屏（复制/导出/补研在里面，aria-label「打开调研卷宗」保持——research-ui-shot-test 靠它）。**终态可感知（AG-UI 会话无服务端主动推送——后台任务跑完 agent 不会主动说话，用户必须再发一句才被理）**：调研卡轮询观察到 running/planning → 终态翻转时广播 `RESEARCH_TERMINAL_EVENT`（装载时早已终态的不通知，防每次刷新都弹），`ResearchNotice`（挂 app/project/[pid]/page.tsx）消费——聊天流插 `progress_*` 前缀瞬时消息（ChatPersistence 不落库；会话内留 assistant 上下文，助手下轮自知任务已终态）+ 左下浮条「查看卷宗」（`FOCUS_NODES_EVENT` 运镜定位 + `OPEN_RESEARCH_READER_EVENT` 直开阅读器）。`getResearch` 对 404（任务不存在）停轮询明报（Error 挂 status 字段），网络闪断照旧 8s 重试。画布摘要对带 researchId 的调研卡附「（调研卷宗 {id}）」锚点
- **后台任务事件流（2026-09-06 方案 B 常开推送通道，根治「任务跑完 agent 不会主动说话」）**：AG-UI 是请求-响应协议——runAgent 的 SSE 流随轮次结束关闭，后台任务跨轮次跑完无处可推（轮内工具如 generate_asset_images 靠 `_emit_progress` 实时播报，不受此限）。补一条进程内事件总线 `agent/eventbus.py`：任务终态 `publish_job_event(kind, project_id, job_id, status, …)` → `GET /api/v1/events/stream`（SSE 常开，15s 心跳防 Next 代理 10min 空闲超时，`CurrentUser` 认证）→ 浏览器 `lib/agent-events.ts`（**fetch 流式解析，不是 EventSource**——后者带不了 Authorization 头；单例连接+指数退避重连；事件不重放，错过的终态由卡面轮询兜回）。已接线六类：deep_research（research.py `_emit_terminal`：非终态不发防 agent 关停误报、补研成功发父 id、补研失败 status_override="error"）/ ref_research（imgresearch 批量翻转处）/ shot_images / decompose（skills.py 两 job 增 `project_id` 参数，端点从请求体透传、lib/shotlist.ts 已带）/ script_review / image_review。消费端 `TaskEvents`（挂 page.tsx）：深度调研事件翻成 `RESEARCH_TERMINAL_EVENT` 复用 ResearchNotice（`seenRef` 按 jobId:status 去重——卡面轮询与 SSE 双源同达只响一次）；**调研类完成自动续跑**（Devin/Manus 范式：以「（任务通知）」前缀的用户消息让 agent 主动汇报并问下一步；Sidebar 据前缀把气泡渲染成中性系统样式、摘掉编辑重发钮）；出图/拆解/审查类只浮条通知不插话（UI 触发的任务，结果落在卡上）。**自动续跑的发送必须经 `AutoRunBridge`**（渲染在 ChatInput JSX 里、走槽位 `onSubmitMessage(string)`）：侧栏内外 chat context 分裂——`useCopilotChatHeadless_c` 在侧栏外 sendMessage 会 resolve 但消息进另一套 context、可见聊天收不到（实测踩坑）；且 `appendMessage` 吃裸对象在 `gqlToAGUI` 炸 `isResultMessage is not a function`（要 Message 类实例）。闸门在桥：页面可见（hidden 挂起等 visibilitychange）/ `isLoading` 忙时 8s 重试≤3 次；每 job 去重在 TaskEvents（`ranJobs`）。E2E 浏览器侧无法伪造 SSE 帧：TaskEvents 暴露 `window.__wsJobEvent` 测试注入口（与 SSE 帧同一条处理链路）。回归：`node scripts/agent-events-ui-test.mjs` + `scripts/ref-adopt-tool-test.mjs` 的 SSE 终态事件断言
- **任务持久化与重启恢复（2026-09-08 P1，novanova Redis Stream 队列的单机等价物）**：长任务状态一律落 SQLite，agent 重启不再 404/丢产物——三张表各司其职：`image_jobs`/`video_jobs`（item 形状，出图/出视频逐张落库，imagejobs.py 表参数化）、`async_jobs`（jobstore.py，单结果+阶段进度型：拆解/分镜表生成的整状态 JSON 镜像，7 天懒清理同款）。**拆解自动出图链逐图 checkpoint**（`_auto_asset_images` 的 `persist` 回调，每张设定图落卡即写库）：重启后已花钱生成的图随 partial assets 收回——`get_decompose_job` 内存 miss 回退 jobstore、端点 done+error 分支也带回 `assets`（曾硬置 None 掐断回收）、前端 `decomposeAssets` 返回 `interrupted` 让部分产物照常落卡并如实转达（不是全有或全无）。**孤儿语义三处一致（萧燕燕计费原则）**：已完成项保留、未知状态在途项标「生成中断」**绝不自动重跑**（重试即重复计费）；轮询端懒终态化（`load_job`/`load_job(table=)`）+ **启动清扫**（main lifespan：`imagejobs.finalize_running_orphans()` + `jobstore.sweep_orphans()`，与 research/script_review/image_review 的 `report_interrupted_jobs()` 同排——进程都换了 running 行必是孤儿，不留僵尸行装活）。jobstore.load 的 status 列权威展开在镜像 state **之后**（镜像里残留 status 键不得盖真实终态）。回归：`cd agent && uv run python ../scripts/job-recovery-test.py`（A jobstore 语义/B item 表清扫/C skills 回退 partial/D HTTP 轮询，10 断言无 LLM）
- **服饰是一等出图类型（2026-09-06 修复「9/9 服饰全败 500」）**：imagegen flow（asset-imagegen.json，运行时真相=内嵌 code）的 `LAYOUT_SPECS` 原本只有 character/scene/prop/shot 四布局，收到 `costume` 直接 `raise ValueError("未知资产类型")` → langflow 整请求 500——090602 项目 15 角色/12 场景/20 道具全成、9 服饰全败即此根因（聊天 generate_asset_images 与资产卡直连出图同害；拆解自动链 `one_costume` 一直是 **costume→prop 绕行**才没炸）。已补齐三层：①flow `LAYOUT_SPECS["costume"]`（纯净浅灰背景服装结构图：正/背/侧三视图+材质细节，人台或平铺不出人脸）+ `_TYPE_LABELS`/`TYPE_ASPECT`（16:9），改后必须 `./scripts/update-flow.sh asset-imagegen.json` PATCH 运行实例；②`one_costume` 与前端契约推断改回本名（曾 `costume→"prop"` 4:3 绕行）；画布「补资产图」批量路径同日发现仍残留此绕行，一并改回本名；③generate_asset_images 工具描述枚举加 costume（明写「服饰卡出图用 costume 不要改成 prop」）。类型枚举四处同改：flow LAYOUT_SPECS/TYPE_ASPECT、graph.py `_GEN_ASSETS_DOC`、store.ts genShot、shotlist.ts 两处 union——加新资产卡型时全链对齐。**资产设定图默认画幅一律横版 16:9（2026-09-07 用户口径「设定图/场景图不要竖屏」）**：①flow `TYPE_ASPECT` prop 4:3→16:9（五类型全 16:9，已 PATCH 运行实例）；②**桥接层设定图语义不吸附参考图比例**——`resolveAutoAspect`（跟随首位参考图真实比例）只保留给 shot 剧照/改图（改图跟原素材走），资产卡本尊（targetAssetType）与资产入边 Look 卡（isLook）空画幅时直接回空走 flow 类型默认——考据参考常是网络竖图，吸附曾把「横版 16:9 四格构图」定妆照带成竖版；卡片级 `data.gen.aspect` 显式选择仍最优先（用户点名竖版不受限）；③graph.py 工具描述、models.py 注释同步（graph.py 改后须重启 agent）
- **全景环视（2:1 球形 pano，2026-09-04）**：seedream 系 2:1 出图（gpt-image-2 无 2:1、gemini 探针 E 组 9:16 倒置出局——spec §4）+ photo-sphere-viewer 球形查看（Lightbox 懒加载）+ 前端矫形（crop 2:1 + 48px 接缝羽化，模型画不出真等距柱状几何）。**分辨率取最高档**（2026-09-04 清晰度反馈：panoGen 此前钉 2K 2880×1440 投球面观感糊，改 4K 优先——seedream 4-0/4-5 4K 4320×2160 实测严格 2:1，5-pro responses 4.19M 上限自动落 2K）。**环视手势（Figma/Maps 业界标准）**：裸滚轮/触摸板双指滑=平移环视（yaw/pitch，pitch 钳 ±85° 防过极点）、捏合/Ctrl+滚轮=缩放（触摸板捏合天然 ctrlKey wheel）——PSV 内建 mousewheel（滚轮=缩放）关闭自接管 host wheel（capture），触摸板双指滑曾疯狂误缩放；`moveInertia: true`（false 拖起来像拖木头）；FOV 30–90；查看器底部常驻手势提示条。回归：image-node-ops P 组（P4 断言 4K）
- **浮层跟手双订阅（2026-09-04 拖画布面板滞后事故）**：NodeInputPanel（卡下输入面板）/SelectionToolbar（多选工具条）的位置直写必须**同时订阅两个 store**——拖卡片时节点位置走 `useCanvasStore`（每帧）；**拖画布平移/缩放时 xyflow 直写视口 DOM、手势结束才落 `useCanvasStore.viewport`**（onMoveEnd 才 setViewport，缩放零重渲优化），只订阅自家 store 的面板会全程钉住、松手瞬跳。修法 = `useStoreApi().subscribe` + `useCanvasStore.subscribe` 同调一个 write()，坐标读 `rfStore.getState().transform`（每帧更新）；React 只随选区结构变化重渲染。探针法：CDP 派 wheel 序列逐帧采样 `panel.left − tx` 偏移量（恒定=完美跟随；注意面板有视口夹紧 clamp，测试卡要在视口内、pan 幅度要小；wheel 要打在空白 pane 上，卡上会被 nowheel 吃掉）
- **媒体比例自适应（2026-09-04，全比例贴合）**：图/视频到位后卡高按自然宽高比贴满媒体区——`useMediaFitHeight`（宽不动只调高；换算必须用 offsetWidth/Height 布局单位——getBoundingClientRect 会把 fitView 动画中的 transform 缩放混进来，动画中量一次就锁死垃圾高度且 fittedFor 记账后不再重贴，卡片永久畸小）；接入图片/资产/视频三卡（img onLoad / video onLoadedMetadata）。`fittedFor=url` 记账：同一媒体只贴一次，用户手动缩放保持到换图，`freeResize` 不抢；存量卡首载 fittedFor 为空→自动纠正历史尺寸（免 sanitize 迁移）。落库走 `store.fitNodeHeight` 原子写 fittedFor+卡高且**免撤销栈**——自适应是视觉归位不是用户操作，入栈会把「撤销生成/裁剪」截断成只撤销卡高（image-node-ops D4 事故）。空态默认按主流 16:9 档（image 256×200 / video 320×220 / 资产 288×214）。E2E 夹具注意：720×1280 竖图夹具会触发卡高 ~490，网格行距（560）与视口 zoom（0.35）要给足防层叠遮挡/边缘卸载；select 助手**必须校验选中 id**——旧卡面板开着时「生成按钮可见」恒真，假成功会把字打进旧卡面板（ref-pipeline §6/§7 假绿事故）
- **生成过程可见性 + 卡面常驻信息（2026-09-04 卡片 review 五项落地）**：①loading 任务卡（novanova image-node 范式）——GenProgress 整卡态显示 提示词摘要（line-clamp-2）+ 模型目录 label·画幅 元数据 + 进度条 + 取消，模型名经目录换 label 不吐内部 id；②编辑重生成**保留旧图**（viedeo 范式）——`LoadingOverMedia`：旧图 opacity-75 + 压暗 + ws-loading-scan 光带 + 底部渐变进度横条（overlay 态），接入图片/资产/视频三卡（视频用 poster=imageUrl，无 poster 退整卡态）；③工具条全平铺（2026-09-04 用户裁决二次回退：收纳版让五项生成能力无人知晓，功能可见性优先）——ToolBtn 缩小（h-7/图标 3.5/文字 11px/间距 gap-1 px-2.5）后 17 钮 842px 放得下；顺序按操作心智：生成类（裁剪/多视角/环视|三视图）→调整类（打光/质感/多功能/自由缩放）→卡动作（版本/下载/复制/重生成/大图/评审）→画布类（复制/锁定/信息）→删除；④版本常驻角标——有 versions 时媒体右下 `V{n}` 黑底 chip（点击开版本弹窗，未选中也可见——版本入口不再只藏在选中后的工具条里）；⑤RetryPanel 加醒目「重试」按钮 + 错误文案 line-clamp-2；图片卡补设定折叠行（与资产卡同款「设定 · 出图自动带上 · N 字」——图片卡 body 语义=出图注入的本卡设定，此前无处可写）。**卡面动作单一出口=工具条（2026-09-05 盘点收口）**：文本卡（note/纯 text）的 生图/生视频/调研/导出 也上浮工具条（`TextCard` 的默认 `extraTools`，调用方传了则不注入；footer 只留字数——曾是 footer 小 chips，低缩放 LOD 整档消失且与剧本卡范式不一致）。**卡上工具条只在单选时显示（2026-09-10「多选满屏工具条」事故）**：CardShell 曾写 `isVisible={selected}`——xyflow 的 `isVisible` 一旦显式传布尔值就**整体覆盖它自带的多选守卫**（源码默认 `isActive = nodes.size===1 && 该卡 selected && selectedNodesCount===1`，注释原文「If multiple nodes are selected it will not be visible to prevent overlapping toolbars or clutter」），于是用户「已选 102」时画布上并排浮出 102 条工具条、全被盖住。修法=**不传 isVisible**（要改显隐语义就改默认判断，别再传只看 selected 的布尔值）；多选的批量动作归选区工具条（SelectionToolbar：复制/对齐/依次连线/成组/整理/按连线整理/下载 N/删除）。回归：`node scripts/node-toolbar-select-test.mjs`（8 项：单选恰 1 条且按钮可点、全选清零、多选工具条仍在位、点空白清空、回落单选复现；断言只走 DOM——`.react-flow__node-toolbar` 非 active 时根本不渲染、选中数数 `.ws-node.is-selected`——不依赖 dev 调试钩子，生产实例也能跑）。E2E 坑：**节点信息弹窗只认背板点击关闭、没有 Esc 路径**，Esc 关不掉它，浮层会吃掉后续所有点击（本测试首版被坑出一次假通过——D 组「点空白清空」在弹窗没关时也过了，故 D 现在同时断言选中数归零）。**卡片全屏文档模式（2026-09-08，导演策划工作载体第一步：用户裁决「先内核后页面」）**：`DocFullscreenEditor`（nodes.tsx 内，紧挨 Editable/ExportMenuButton——Editable 是文件私有且 IME 敏感，独立文件需导出+循环引用，不搬）= 文本/剧本卡工具条「全屏」钮（Maximize2，`data-track="doc.fullscreen"`）开的 z-1300 全屏编辑器——头部标题+实时字数+导出（ExportMenuButton bare）+关闭，正文 = `Editable always fill` 大编辑面（15px/行高 8/剧本卡 font-editorial 衬线），**与卡内正文同一数据源**（store body + 打字流实时落库），双入口零版本冲突；portal 到 body 免疫 LOD/视口卸载（万字长文实测 345ms 插入）；打开即聚焦光标落文末（续写姿态）；关闭只走 Esc/×（写作模式不吃背景板误点）；Esc 有 IME 组合守卫。配套「成稿落卡」宪法口径：策划案/文稿/成片稿写进 note/script 卡、聊天只给摘要与指向（含教用户「点卡上工具条的全屏」）——聊天是对话、卡是产品，不给聊天加下载/导出避免双真相源（结构化提问控件同判：观察项不建，见宪法讨论）。后继留档：文稿台第二视图（ActivityBar `script` 坑位已预留 enabled:false）= image-studio 壳 + 文档列表（画布卡过滤视图）+ 本内核平铺，触发条件=文档清单/专注写作需求真实显形。E2E 夹具注意：**nodeType 没有 'text'**（文本卡是 note），填错会让画布摘要 nodeLine 的 NODE_META 查 undefined 直接崩整页。回归：`node scripts/doc-fullscreen-test.mjs`（10 断言）。E2E 注意：工具条贴左缘时前段可能伸出视口（xyflow NodeToolbar 无水平钳制，存量现象），测试开编辑菜单用 `evaluate(el => el.click())`；卡被搜索框等画布层控件遮挡时 `:visible`+hasText 定位会空过——断言按钮存在用 toolbar `innerText`
- **图片卡动作分层（"看图干活"进大图）**：缩略图悬浮条只留 版本V/复制提示词/重新生成/放大——标注重绘、九宫格切图、候选「设为主图」、版本「恢复」在灯箱里做（`Lightbox` 的 `actions` 注入，gallery 快照带 meta，动作只对本卡图片出现；他卡图片与 PromptBar 引用预览无动作区）。openZoom 快照顺序 = 主图→候选→版本→他卡主图。恢复版本 = 当前版入档 + genPrompt 回滚（与 `NodeMediaHistory.restore` 同逻辑，两处改一起改）。版本历史弹窗（`NodeMediaHistory`）是 master-detail（左 contain 大图点进灯箱 + 右版本列表/A·B 滑杆），**必须经 `OverlayModal` portal**——节点树内裸 `fixed` 会被 viewport transform 劫持钉死在卡宽（事故：弹窗缩略图 144×96 不可用）。MaskEditDialog 画布自适应大屏 `min(92vw,1400px)`（入口在灯箱）
- **分镜行→资产引用三通道**（`lib/canvas/shotRefs.ts` 共享解析，sanitize 存量迁移同源）：① 结构化 refIds（改名不失联）② 行内 `@完整标题` ③ **全名兜底**——无 @ 的完整资产标题也算引用（≥2 字、最长优先、区间不重叠；ai-moive-studio 按名解析范式），出图时活解析——**分镜先于资产生成时行文本天然含资产名，资产后建/后出图自动挂上，无需回填**。生成链路另注入名单并要求 flow 每行输出结构化 `assets` 名单（novanova referenceKey 范式的名字版）：agent `_parse_shot_rows` 解析 + 按名单二次校验（幻觉名剔除），前端转 refIds 后即剥离 assets 字段（聊天侧写回走 canvas_ops rows，`ops.ts` `normalizeRows` 按资产卡精确标题匹配 assets→refIds，lighting/sound 一并透传）。出图软闸点名具体镜头（`镜2、镜4 未引用已出图的资产设定图…行内 @资产名 可绑定参考`），不再笼统报数。参考穿线：镜头图卡落 refIds + 「资产→镜头图」连线（open-ai-canvas materializer 范式）。**聊天侧整表分镜**（2026-09-03「27 张分镜卡空转」事故的解法）：`generate_storyboard` 后端工具（`graph.py`，核心 `skills.run_storyboard_flow` 与 HTTP job 共用）生成 rows → canvas_ops 写回**已有**分镜表卡（没有才 add_node shotlist）；系统提示分镜路由明确「整表分镜不铺独立 storyboard 卡」。配套：画布摘要 [剧本]/[分镜表] 行永不丢 + 分镜表带行数（96 节点画布上曾因预算丢行让 agent「看不见」分镜表）；read_node 对 shotlist 返回行清单（含 rid）。**聊天侧分镜图落卡（2026-09-07 用户拍板「也落图卡」）**：`generate_asset_images` 的 shot 项带行绑定（`shotlist_id`+`rid`）时，出图完成由 `graph.py _build_shot_card_ops` 生成落卡 ops——每镜一张 image 卡（**分镜表右侧 √n 网格**、粗碰撞整块下移；title=「镜头NN 图」、genShot 快照、refIds 按 reference_images URL basename 反查资产卡）+ 分镜表→图卡与资产→图卡连线 + 行挂 `imageNodeId`（**行不写 imageUrl**——行缩略图读卡上的图，重跑随卡刷新），返回串教 agent「canvas_ops 原样应用整批 ops、不要手写行 imageUrl」。与前端出图按钮同语义（此前聊天侧只回填行 imageUrl、无图卡无操作层——两路径语义漂移）。skills.generate_asset_images 返回改结构化 `{lines, results}`（graph 唯一调用方）。配套：ops.ts add_node/update_node 补 `status/genPrompt/genShot/refIds/styleSnapshot` 透传 + update_node row 补 `imageNodeId`。回归：`scripts/shot-card-ops-test.py`（python 直连工具，真出 1 图断言 ops 形状）+ `scripts/chat-storyboard-test.mjs`
- **视频生成层（2026-09-08 定案：RunningHub MiniMax H3 参考生视频唯一渠道，用户拍板）**：分镜行→参考生视频→一键成片链路闭环。**协议与节点映射移植自 juben `lib/video_backends/runninghub.py`（实战验证）**：ComfyUI 工作流协议——上传 `POST /task/openapi/upload`（form: apiKey+fileType=input+文件→fileName）→ 建任务 `POST /task/openapi/create`（{apiKey, workflowId, nodeInfoList}→taskId；**421=账号并发满，任务未创建不重复计费，30s 重试至多 30min**）→ 轮询 `POST /task/openapi/outputs`（code 0+data[].fileUrl 取 video 型产物；804/813 进行中；805 failedReason）→ 下载落盘。工作流 2088888684010622977（9 图槽+3 音频槽，**无独立主图槽——首帧占槽 0、参考图最多 8 张依次占后续槽**）；时长开关 select=秒数−4（5s→1…15s→11）、兆像素开关 1=540p/2=720p、画幅走 ResolutionSelector 枚举（"16:9 (Widescreen)" 等**必须精确匹配**）；恒出音频。**架构**：`agent/videogen.py` 直连客户端（compose.py 同范式——视频 API 调用不是 LLM 文字生成不走 Langflow）；目录 `models.py VIDEO_MODELS` 唯一条目 rh-minimax-h3（5-15s/540p·720p/8 画幅/参考上限 8）+ `GET /models/video`；`RUNNINGHUB_API_KEY` 在 `.env.local`（juben 同款 key）。**分镜视频 job**（`skills.start_storyboard_video_job`）：video_jobs 表持久化（imagejobs.py 表参数化）、并发 4、批量上限 60、prompt+首帧图必填校验、终态 eventbus `shot_videos`；端点 `POST/GET/DELETE /storyboard/videos`（params：model/duration/resolution/aspect 逐镜头合并预校验，非法 400 点名）。**前端**（分镜表卡）：工具条「出视频·N 镜」「补缺视频·N」（资格=行有 ready 镜头图卡）；**参考图收集与出图同源**（行 refIds→preferLookRefs→带图资产卡，上限 8），提示词带**图N 编号行**（juben PROMPT_REFERENCES 范式：图1=首帧构图基准、图2+=《名》身份/形态一致，composeShotVideoPrompt）；画幅跟随分镜卡 d.aspect；**视频卡落图卡正下方**（首帧血缘连线+分镜表→视频卡，一键成片按连线收集）；行挂 `videoNodeId`、`videoJobId` 断点恢复、VideoGenSettings chip（时长/清晰度，恒带音频无开关）。**历史选型实录（2026-09-07 探针留档）**：DMX /v1/videos 海螺系取件链坏（双面 artifact_gone，完成即刻也取不到）、wan/kling/vidu 适配器 fail_to_fetch_task、seedance/sora 无渠道；ark REST 契约 juben 已验证但 key 未开通 seedance；BigModel cogvideox flash/v3 双档全通（coding key 走官方 paas，coding 网关无视频路由）——2026-09-08 被 RunningHub H3 取代，扩渠道时参考 videogen.py 头部注释与 git 历史。回归：`node scripts/shot-video-test.mjs`
- **聊天引导/打断（2026-09-09，对标 Claude Code 的排队与中断告知）**：此前 v2 框架原生支持「运行中提交→`waitForActiveRunToSettle` 排队」，但自定义 ChatInput 的 submit 遇 `inProgress` early-return 把它拦死——运行中发什么都被静默丢弃，用户只能干等或停止后重新解释（「比 codex/claude code 差」的根因）。两层修复：①**排队引导**——运行中回车不丢不发，进排队 chips（⏳ 行、可 × 撤回，placeholder 提示「生成中…回车将排队」），`inProgress` true→false 跳变沿自动排水发出下一条（跑完或被停止都排水——停止后排队的引导恰好作为新一轮指令接上）。**排队项必须带 `threadKey`=入队时的 `agentThreadId`，可见与排水都按它派生过滤**：不能用 threadId 做「切会话清空」的信号——新会话首次落库会把 threadId 从 null 变成真 id，误判成切会话把刚排的队清掉（首跑实锤；agentThreadId 在首存前后稳定，只在真正切换时变化）。②**打断告知（Claude Code "[Request interrupted]" 范式）**——停止按钮 = `cancelChatRun`（杀在途后端工具）+ v2 `stopAgent`（abort SSE）+ **落一条用户消息「（用户中断了这一轮生成）」**（addMessage 不跑 run；ChatPersistence 的 `onMessagesChanged` 照常落库，下轮合并进 checkpoint 模型自知）；Sidebar UserBubble 对「（用户中断」前缀走中性气泡（同任务通知：无编辑重发、不盖轮次锚）。宪法执行纪律配套一条：见标记则上一轮是残篇，不续写不自动重跑、按最新消息行事。**abort 不弹错误横幅**：v2 core 的 `onError` 对 AbortError 系（`Fetch is aborted`/`signal is aborted without reason`）静默，无需自己拦。E2E 要点：agent run POST 打在**精确的 `/agent-service` 路径**（无后缀），Playwright `page.route("**/agent-service")` 恰好只拦 run、`/agent-service/chat/jobs` 等照常穿透；mock SSE = 标准 `data: {...}\n\n` 帧（RUN_STARTED→TEXT_MESSAGE_START/CONTENT(delta)/END→RUN_FINISHED 即可），「忙窗」用 route handler 里 delay 再 fulfill 制造，abort 后 fulfill 落空要 try/catch。回归：`node scripts/chat-interrupt-queue-test.mjs`（14 断言无 LLM）。真 mid-run steering（当前轮进行中插话）AG-UI 请求-响应协议做不到，排队是等价形态
- **聊天长内容 UX（2026-09-09 用户 review「关闭/拖宽/长内容浏览复制体验不好」后的三件修复）**：①**助手消息零操作按钮**（P0）——Sidebar 把 `copyButton` 换成 `NullSlot`，而 v2 的工具栏是**条件渲染**（`onRegenerate`/`onThumbsUp` 没传 handler 的按钮根本不渲染，`renderSlot` 里 `(onX || xButton) && bound`）→ 工具栏只剩一个空容器（实测 `btns: []`、h=0），长回复只能手动拖选。修法 = 新增 `components/copilot/AssistantMessage.tsx` 自绘助手消息槽：直接渲染框架的 `CopilotChatAssistantMessage`（复制钮由框架接好 `onClick=copyToClipboard(原文)`）+ 传 `onRegenerate`，labels 里早已配好的「复制/重新生成」文案从此不是死配置。②**重新生成必须服务端 fork**（关键坑，实测三次）——只截断本地历史重跑**不能**让旧答案离开模型上下文：ag_ui_langgraph 的 `is_continuation` 把「客户端消息是 checkpoint 子集」判定为续跑（补全工具调用场景），不触发它的 time-travel 分叉，旧回答留在 checkpoint 里、下一轮能逐字复述（实验：首轮答「AAA-111」→ 截断重跑 → 只发首问追问，模型照样答出 AAA-111）。修法 = 新端点 `POST /chat/regenerate {threadId, messageId}`（main.py，复用 `agent.get_checkpoint_before_message` + `agent.graph.aupdate_state`，与框架 `prepare_regenerate_stream` 同一手法）把该消息之前的 checkpoint 分叉为会话当前头；前端顺序**先 fork 成功再截断本地历史再 runAgent**（fork 失败明报不截断）。注意 fork 的 `messageId` 传**上一轮用户消息**的 id（不是被重生成的助手消息）。③**长回复折叠只裁正文**——v2 把工具栏渲染在**正文之后**（`[prose, toolbar]`），给整条消息包 `max-height` 会把复制/重新生成一起裁掉；改由 `data-ws-collapsed` 属性 + CSS 只裁 `.cpk:prose`（480px + 底部渐隐，与 `AssistantMessage.COLLAPSE_LIMIT_PX` 同源），展开钮挂在 `additionalToolbarItems`（工具栏行内，不另占行）。**折叠判据 = 实测溢出**（2026-09-11 改口径）：正文 `scrollHeight > 480px × 1.6` 才折——旧版按「700 字」字数触发而限高 340px 只装得下 ~430 字，一折就砍掉近四成正文；**最新一轮的答复永不折叠**（折叠是翻历史的手段，刚拿到的答案要铺开，不止流式中不折）；展开态按消息 id 存 store（框架虚拟化卸载不丢）；搜索进行中自动展开（命中在折叠区里就高亮不到）。④**会话内搜索**（`components/copilot/ChatSearch.tsx` + `lib/chat/search.ts` store）：Cmd/Ctrl+F 或头部放大镜打开，高亮走 **CSS Custom Highlight API**（`CSS.highlights.set` + globals 的 `::highlight(ws-search*)`，不改 DOM、流式重排不打架），Enter/↓ 下一处、Shift+Enter/↑ 上一处、Esc 关闭清高亮；**当前命中必须从「全部命中」集里排除**——两个 highlight 覆盖同一段文字时绘制顺序按注册先后、先注册的压在上面，accent 色会被 dim 色盖住（实测「当前命中看不见」）；**Turbopack 对 `::highlight()` 会报 `'highlight' is not recognized as a valid pseudo-element` 警告但规则保留生效**（别被 logs/web.log 的警告误导去改写法）；**监听必须挂在常挂载组件上**——ChatSearch 若按 `open` 条件挂载，关闭态快捷键直接失效（实测 C1 挂）。>50 条消息框架虚拟化，只能搜到已挂载部分，无结果时文案明说「仅搜索已加载部分」。⑤**轮次轨命中区**——圆点此前就是按钮本体（10×4~6px），点不中；改成按钮撑成 24px 宽 ×(点高+间距) 的连续列、圆点退化为列内 span（`group-hover/dot` 仍负责悬停展宽），轨整体让开右缘 24px 避开 v2 滚动容器的 20px 滚动条（原先压在滚动条上）。⑥**助手工具栏常驻可见**（透明度 0.65→悬停 1）——纯 hover 浮现时那 26px 行仍占版面却不显示任何东西，长对话里就是一排空槽。回归：`node scripts/chat-longcontent-test.mjs`（A 复制/重新生成钮 + 剪贴板原文、B 折叠 480px（溢出 ≥1.6 倍才折）/展开/收起/工具栏不被裁 + 最新长答复不折、C 搜索计数/高亮/跳转/关闭清高亮、D 命中区 24px + 让开滚动条、E fork 端点 + **真跑 LLM 验 fork 后模型看不见旧答案**）+ `turn-locator-test` 27/27（点结构变更同步改断言）+ `chat-spacing` 10/10（**注意 ④ 段内间距偶发 79px 是模型输出形态波动，非回归**——同一构建重跑即绿）
- **@ 体系补缺（2026-09-08，用户问「上传的资料都能 @ 吗」后的盘点与修复）**：四条 @ 通道——画布卡输入条（`MentionInput`：本卡带图/已连线置顶/按类型分桶）、聊天输入条（同一个组件，无本卡/已连线）、生图台（只 @图N）、分镜行内 @资产名（只认资产四类+图片卡）。修复前「聊天上传的文档（doc/pdf/rtf/文本）只内联在当轮消息里」——不落卡、不进素材库、后续轮次无法 @；聊天上传的图/视频/音频同样是一次性的（只有画布节点的媒体会被 `AssetAutoRecorder` 自动入库）。四件修复：①**文档落卡**（`lib/canvas/ingest.ts` `addDocCard`，ChatInput 两条内联分支各调一次）——上传即建 note 资料卡（标题=文件名、正文=提取全文、位置走内容下方锚点不抢视口），toast 提示「已存为资料卡，可在画布 @ 引用」，此后 @/连线/跨会话/跨视图全通；②**`MentionInput.TYPE_ORDER_KEYS` 补 shotlist/research**——不在表里的类型只会在「已连线」组露出，普通候选永远 @ 不到（卷宗做写作依据、分镜表做上下文都曾够不着），**新增卡型必须同步此表**；③**聊天上传的媒体自动进素材库**（`saveAsset`，与画布上传对齐）；④**素材库项可拖入输入条**（`ASSET_DRAG_MIME` 载荷 + `parseAssetDrag`；画布面板 `NodeInputPanel` 就近建卡+`ADD_REF_EVENT`、聊天 `appendMention`）——库项不是画布卡，@ 的载体必须是卡，故拖入即建 image/video/audio 卡再引用（语义同「库图即资产」）。**同日 review 补两处**：①文本类文件直读上限 64KB→**2MB**（`TEXT_READ_MAX`）——旧上限把 100KB+ 的剧本 .txt 静默踢出「落卡+内联」两条路，落到上传分支后 agent 根本读不到内容（`/extract-text` 只收 doc/docx/rtf/pdf）；②**空/非文本文件明报错误态**——此前附件永远停在「上传中」，submit 三分支全不匹配被静默丢弃。回归：`node scripts/mention-gaps-test.mjs`（14 断言，含 docx 服务端提取落卡/150KB 大文本全文落卡/空文件失败态；注意 @ 引用 chip 是编辑器内联 `[data-mention-id]`，`定位到画布卡片` 那个按钮是**连线**引用、别认错；文本卡面板没有参考 chip 行，面板拖入要用图片卡测；画布 PUT 有 1.2s debounce，落库断言要等 ≥2.6s）。
**场景资产升级为多视角基准图（2026-09-10，用户反馈线上场景图是单张中轴对称大远景、分镜无法判断空间关系与大小）**：`LAYOUT_SPECS["scene"]` 从「中性勘景视角的单张空镜」改为**横版 16:9 四格**——主视图（空间全貌与光位）／反打视角（对面墙面、出入口与纵深）／四分之三斜角（相邻两面墙与地面关系）／关键区域近景（最常发生戏的区域陈设与年代细节），四格共用一套陈设与光位，并要求**建筑构件按真实人体尺度关系呈现**（门洞高度/台阶/栏杆/坐具比例经得起推算）——尺度锚点是这一轮的核心诉求：无人空镜里没有人物参照，只能靠建筑构件的常识比例让分镜推得出空间大小。配套同步三层：`_REF_DUTIES["scene"]` 加「不继承多格排版」（防分镜图复刻四格，与 character 定妆照同款防护）、场景拆解契约（asset-decompose-scene.json 与 legacy 的 asset-decompose.json）要求 description **交代到能支撑多视角复原**（主视/反打各看到什么、出入口位置、主要陈设相对关系）、asset-aware-generation 手册与 generate_asset_images 工具描述明写场景版式。改的是 flow 内嵌代码（运行时真相），仓库源码 batch_asset_sheet.py 同步；**改后需 `./scripts/update-flow.sh asset-imagegen.json` PATCH 运行实例 + ./scripts/update-flow.sh asset-decompose-scene.json**。存量场景卡不迁移——重出即得四格版。
**造型计划与造型图链路（2026-09-10 用户反馈「服饰图没有回填角色生成 look 图」）**：线上诊断实锤——24 个项目 19216 张图里 Look 卡 0 张、角色卡 looks 字段 0、costume→character 连线 0；而**实测拆解 flow 确实产出 looks**（冯太后拆出「朝服/常服」带服饰细节与 costume 名），是这条数据从拆解出来后一路被丢弃。五个断点：①`decompose_script` 返回给 agent 的清单只拼 name/description/visual_notes，**looks 一个字没提**；②`ops.ts` 无 `looks` 字段（agent 写不进）；③前端建卡只物化 `filter(l => l.image_url)` 的项，而 image_url 只在造型图出图后才有；④造型图出图链（`gen_one_look`/`look_backfill`）只挂在已停用的拆解自动链（`autoLooks` 恒 false）上；⑤服饰与角色的绑定靠 `find_costume_img` 名字模糊匹配、前端从不建关系。**修复：造型计划 = 角色卡的结构化数据**（`data.looks:[{label,description,costume,costumeId,imageUrl,nodeId}]`）——拆解即落卡（不再筛 image_url）、`ops.ts` 收该字段（label 必填、最多 8 项、**不收出图产物字段**）、`decompose_script` 返回清单带「造型：」并教 agent 写卡、canvas_ops 描述同步、拆解 flow 契约改为「description 只写不随造型变化的本体外观 + 最常出现那套造型，其余装束细节归 looks」（避免定妆照要同时表现几种装束互相打架）。**出图全自动**：`fillAssetImages` 完成后自动续跑 `fillLookImages`（参考图1=角色定妆照锁身份、参考图2=绑定服饰结构图锁形制；`assetType:"none"` 走 `_FREE_TEMPLATE` 原话直传 + protocol 自述单幅版式，避开四格定妆契约；`aspect:"9:16"` 显式传——`TYPE_ASPECT` 表里没有 none，不传会 KeyError；rid=`{角色卡id}#look{i}` 让服务端按 # 前缀取该角色的考据简报）；出图后物化成独立造型卡（`角色名·造型名`，收「造型图」组框）+ 角色→造型卡与服饰→造型卡连线 + 回填 `looks[i].imageUrl/nodeId`。手动入口：剧本卡工具条「造型图·N」（漏补/存量用）。**sanitize 迁移改幂等且不剥离**（原逻辑无条件 `delete n.data.looks`，会把未出图的计划整条清掉；现在按 nodeId 或同标题判「卡还在就跳过」，计划全量保留）。服饰绑定结构化：建卡后把 `look.costume` 解析成 `costumeId` 落卡（改名不失联），按名互含匹配降级为解析兜底。消费侧不变：`preferLookRefs` 按分镜行文造型词自动选造型卡（「脱下常服换上雨夜装」取最后出现的、显式引用不替换）。回归：`scripts/canvas-read-channel-test.mjs`（53 项，含 looks 透传/非法项剔除/物化+连线+回填/幂等/计划保留）。
- **手动建资产卡（空名优先 + 同名防重）**：所有建卡入口标题一律留空（`Editable` placeholder 引导「输入角色名」；历史 hint 占位文案当真名会污染资产名单/@引用/全名匹配——已清除，勿回退），建卡即 `dispatchFocusEdit` 聚焦命名（`Editable.editingOn` 直接参与 open 渲染条件，**不在 effect 里 setState**——React Compiler 禁；`focusWhenVisible` 支持等挂载重试）。`CardShell` 资产卡同类同名 onSave 即时 confirm 提醒定位（titleToId 后者覆盖前者、重名绑定随机）。空名资产不进名单/候选（title 过滤天然挡住）。资产卡「AI 写设定」= `/text/rewrite` 直连（`ASSET_WRITE_HINT` 类型模板 + assetSource 源剧本上下文；空设定直接落正文、已有设定预览采用才覆盖）；素材库图片条目悬浮「建为角色/场景/道具/服饰」= 库图即设定图落资产卡（viedeo-workflow 库项即资产范式）。回归：`scripts/asset-create-test.mjs`
- **剧本审查（三维度结构化 findings，不是文字报告）**：剧本卡 footer「审查」→ 弹窗勾维度 → 异步 job（`agent/script_review.py`，review_jobs/review_findings 两表，research.py 四件套同范式）→ master-detail 弹窗（`ScriptReviewDialog`，OverlayModal portal；左只读正文按锚点切 span 高亮+滚动定位，右 findings 列表逐条 忽略/应用建议/看来源）。维度：合规（敏感词表 `agent/lexicons/sensitive-lexicon.txt` 仅作 LLM 参考底料注入 flow——**不做代码层规则匹配**，语境判定归 LLM）/ 一致性（内部矛盾双引文）/ 事实核查（抽断言≤12 → Serper 号池取证 → 判定，属实不报只报问题）。**锚点与过期**：finding 写入时定位 quote 区间，job 记 body_sha1；前端按当前正文本地重算锚点（服务端区间是写入时的），指纹不符亮「剧本已修改」横幅，应用建议前校验区间仍指向引文防误改。单维度软失败明报（dims_json），取消 1s 粒度 cancel 在途 flow 任务（不等 240s flow 跑完）。卡锚 `reviewJobId`（useScriptReviewJob 续链，终态清锚自动弹窗，useBatchRefJob 同式）；文本模型吃卡上 `data.textModel`。4 个 flow（script-review-compliance/consistency/fact-claims/fact-verdict）**顶层保留 id 字段**（update-flow.sh 可用，research 系被剥 id 的亏不再吃）。回归：`scripts/script-review-test.mjs`（真跑 LLM+搜索）
- **操作埋点（数据分析）**：自托管事件表（agent/events.py，SQLite `events`，只记事件名+粗粒度属性、**不记正文内容**）。按钮埋点零接线——加 `data-track="area.action"`（可选 `data-track-props='JSON'`）属性即被 `TelemetryListener` 全局捕获上报；非点击流程用 `trackEvent(name, props)`（lib/telemetry.ts，fire-and-forget，绕开 apiFetch 的 401 重定向）。分析入口：`GET /api/v1/events/summary?days=30`（按名聚合）/ `GET /api/v1/events/recent?name=`（下钻）。新按钮想进统计只需加属性；键盘快捷键暂未埋（非点击）
- **一键下载全部结果（2026-09-07，竞品共识 = 前端 fflate zip）**：入口两个——右键空白「下载全部媒体（N）」（全画布，无媒体禁用）+ 多选工具条「下载 N」（只下选中卡，viedeo 框选范式；选中卡无媒体时不出现）。核心 `lib/canvas/bundleDownload.ts`：`collectMediaEntries` 扫节点 `imageUrls`（候选全收）→ 否则 `imageUrl`，再加 `videoUrl`/`audioUrl`，**跨卡按 URL 去重**；打包 = fflate `zipSync` **level 0**（媒体已是压缩格式，再压只费 CPU——novanova/open-ai-canvas 同款）+ zip 根 `清单.json`（文件↔卡片底账：文件名/卡片/类型/节点/来源 URL/状态）；**单个媒体跳过 zip 直走 downloadMedia 快路径**；4 路并发 fetch，个别拉取失败跳过不拦整批（toast 报「N 个拉取失败跳过」，清单里标状态）。命名 `{两位序号}_{卡标题}_{节点id前6}[_候选N].{ext}`：非法字符清洗（download.ts sanitizeName 单一来源）、标题截 60、空标题回退「未命名」、Windows 保留名加 `_` 前缀、扩展名按 URL 推断回退 kind 默认；zip 名 `{项目名}-媒体-{YYYYMMDD-HHmm}.zip`。埋点 `trackEvent("canvas.bundle-download", {count, scoped})`。注意：选区工具条挂在选区包围盒上方（-translate-y-full），选中卡贴视口顶时工具条出屏——真实用户平移即见，E2E 必须先 `__wsSetViewport` 平移再选中。回归：`pnpm dlx tsx scripts/bundle-download-test.mjs`（纯函数 5 断言 + 浏览器 zip 解包 9 断言，自建项目自删）
- **自由生图工作台（juben ImageStudioPage 移植，2026-09-07；同日升级为项目域第二视图）**：`/project/[pid]/image-studio`（左侧活动栏「生图」入口，与画布工作台**同壳互导**——ActivityBar 画布/生图双视图高亮切换、聊天线程按项目共享、agent 带画布上下文；页面组件 `components/image-studio/FreeImageStudio.tsx`，projectId 来自 canvas store）。与资产/分镜出图完全隔离（juben 同款约定）——不读项目画风、不注入版式契约（assetType="none" + final_prompt 原话直传，版式显选 v3 的 KEEP 语义）、产物不写画布不进资产库。后端 `agent/free_images.py`：SQLite `free_images` 表（行=批次×模型）+ `POST /free-images`（一次点击多模型并行，每模型一行任务各调 `skills._generate_single_image`，10min 超时兜底；逐模型预校验 模型/画幅/档位/参考上限，不合法整批 400 点名——`MAX_BATCH_MODELS=8`）+ `GET /free-images?project_id=`（画廊 3s 轮询，`_reconcile_orphans` 对账重启遗留：在途且进程内无任务 >60s → 标「生成中断」）；30 天懒清理。**@图N 注解照搬 juben**：`@图1 锁定脸部` → 注解抽离正文、并入 final_prompt 顶部「参考图编号：图1=《文件》：注解」行（final_prompt 通道不渲染参考职责段，编号行就是参考图的全部说明）；@图N 越界（N>参考数）400 明报。前端：`StudioPane key={projectId}` 重挂载复位+草稿惰性初始化（React Compiler 不许 effect 里 setState——模型默认选择/画幅档位交集校验全走渲染期派生值）；**参考图 dnd-kit 指针拖拽排序（juben 原版保真：PointerSensor 6px 阈值防按钮误触/拖拽跟手动画/触屏，按钮上下移并存）**；画幅/档位取所选模型**交集**（空交集明报「减少模型」，绝不静默换档）；参考上限 = 所选模型最小 max_references；`PromptMentionTextarea`（@ 弹层过滤 图N/文件名，**弹层锚在光标处**——高亮层在 caret 偏移埋零宽锚点 span、portal 到 body 定位，juben 同款；透明 textarea+同版式高亮层 overlay，高亮层禁止额外占宽否则光标错位；**Backspace 两次删实体**（第一次选中整颗 `@图N`）；已引用实体 chips 行置顶回显）。juben「发给 Agent」按钮未移植——项目域壳自带右侧聊天（agent 两头够得着：**`generate_free_image`** 后端工具提示词逐字直传可多模型并行出图进画廊、**`list_free_images`** 查画廊拿 URL，配上 canvas_ops 即「把自由生图结果送上画布」，孤岛缺口闭环）；大图弹窗含「实际发送的提示词」（finalPrompt 可见性范式）。**skills.py `_generate_single_image` 的 final_prompt 现也拍平单行**（此前只有 description/visualNotes 拍平——多段 final_prompt 的 \n 被 langflow tweaks 反转义成裸换行，组件 json.loads 炸「Invalid control character」，P2 按此提示词重跑同害已一并治好）；flow `parse_assets_payload` 过滤器已收 "none" 类型（此前只认 LAYOUT_SPECS 五类，none 载荷被静默丢弃报「资产清单为空」）。**加载性能三件套（2026-09-08 用户「图片一张张冒出来」反馈）**：①画廊列表轮询**不带 finalPrompt**（每行最多 3000 字×汉字 3 字节，102 行实测 560KB/3s——隧道下图片请求全排它后面；详情走 `GET /free-images/{id}`，Lightbox 打开时按条拉）；②**画廊首屏限量**：只渲染最近 2 批（`visibleBatches`，初始 2、「加载更早的批次」+5——首屏缩略请求从全量 34 批掉到首批 3 张）；③**Lightbox 渐进加载**：先秒显缩略（已缓存）、原图（2K/4K 数 MB）后台 `new Image()` 加载完换 src——派生式状态（`loadedUrl === url` 判就绪），不在 effect 里同步 setState。回归：`node scripts/free-image-studio-test.mjs`（B11）
- 前端与 agent 间一切流量走**同源代理**（`next.config.ts` rewrites：`/agent-service*`、`/api/v1/*` → 127.0.0.1:8123）。密钥（AGENT/LANGFLOW/DMX key）只存在根目录 `.env.local`（agent 经 dotenv 读取），**绝不下发浏览器、绝不提交**
- **聊天侧改图 = `regenerate_card_image`（前端工具，2026-09-10 审计缺口修复）**：图片出图本是**前端直连管线**（输入条 → directImagegen），agent 在聊天里收到「改一下这张图」时没有等价工具，只能退化成图生图近似、或让用户自己去点输入条；而 `canvas_ops` 手写 `imageUrl` 是纯覆盖、旧图不入档、无法回滚（与「版本档案就是回滚」的承诺直接矛盾）。现在三条语义与输入条逐一同源：改图语义（EDIT 最小模板）/ 版本档案（`ops.update_node` 覆盖主图前归档旧媒体，幂等）/ 无谱系卡派生新卡（判据带 `nodeType==="image"` 限定——资产卡/分镜卡是那张图的本体，无谱系也原位重出）。**agent 收到改图请求一律走该工具，不许用 canvas_ops 手写 imageUrl 交付**；工具卡渲染「正在改图（约 30-90 秒）」/完成/未成功。回归：`node scripts/chat-revise-image-test.mjs`（mock SSE + mock 出图，含资产卡原位重出不派生）
- 主 Agent 是**瘦编排者**：系统提示只放"宪法"（`agent/prompts/system.md`，`graph.py load_system_prompt()` 装载），任务知识一律放 Langflow 技能或工具 docstring。新增能力 = 新工具/技能，不是加提示词。**宪法正文外置 `agent/prompts/system.md`（2026-09-08）**：占位符用 `string.Template` 的 `$name` 语法（`$history_section/$canvas_summary/$skill_catalog/$camera_cheat`；JSON 示例直接写花括号，不必再数 `{{ }}`），`graph.py load_system_prompt()` 按 **mtime 热加载**——改提示词保存即生效、**不必重启 agent**（实测：运行中追加标记行，下一轮就读到；删除同样即时生效），启动时校验一次（缺文件/占位符写错直接拒启，不静默降级）。编辑提示词直接改这个 md 文件即可。**消息路由决策表（2026-09-06 收拢；2026-09-08 拆为「决策原则」+「任务路由」两节）**：宪法开头「## 决策原则」块——只给素材→带默认推荐问一句（不开放式菜单；**素材是真实历史/事件题材时问句顺带提一句「出设定图前可先做资产参考图考据调研」**——2026-09-06 用户两次纠偏：真实罪案剧本的问句只给制作链不对，且「调研」出口指资产图考据 research_asset_references、不是史实核查 start_deep_research，一句话带过仍不是菜单）、意图明确→直接执行（执行细节自己定不请示）、「调研」二义按上下文分（史实=深度调研/设定图上下文=资产考据，分不清问，启动错 running 态 cancel_research）、回复与提议不一致=改道不是确认、无关问题正常答；曾散在行为准则 7/8 与调研路由补丁句里的内容全部收进此块，行为准则瘦回 5 条执行纪律——**禁令列表会越长越不管用，新行为约束优先进路由块而不是加准则**。**策划阶段交互契约（2026-09-08 对照 juben 服务器真实会话补；用户反馈「juben 的 agent 会大量调研/问想法/找矛盾而我们的不会」——官渡/大秦/斑狐三标本实锤：全是技能条款+工具撑出来的设计行为，非模型自发；gongxm 重度用法另暴露「审稿比稿」角色与「用户指令 vs 历史决策矛盾对质」两个高价值场景）**：① `web_search`/`web_fetch` 两个轻量后端工具进聊天主循环（graph.py，复用 imgresearch Serper 号池 + research.fetch_page_text 三通道；正文 1.8 万字上限、失败明报中文原因）——此前主循环零搜索工具，策划时模型想核实一个事实都没有手可用，这是策划质量的地基；② 路由块新增四条：**方向类决策呈报拍板**（选题/讲法/结构/片名先 web_search 基础核实再出 2-3 个带理由编号选项请用户拍板、关键依据附来源 URL——与「意图明确直接执行」分流：执行类不请示、方向类必呈报）、**审稿/比稿是回答不是执行**（用户拿自己写的稿子让审/比较时按用户标准逐条评给结论，不转「我帮你重写」）、**矛盾对质**（用户前后矛盾或与已定决策冲突→停下指出请裁决，不默默二选一；流行说法与史料不符→核实后如实呈报差异）、**轻量核实不算调研**（顺手查证用 web_search/web_fetch，不为轻核实发起 deep_research）。验收：探针式真聊一轮（鄱阳湖策划请求→web_search+四方向各带叙事结构/优点/风险/推荐度+附《明史》wikisource 来源+明确推荐理由）三断言全过；多轮行为回归可扩 intent-routing-test 场景组（暂未做）。**宪法瘦身（2026-09-08 冗余禁令审计，用户批准）**：5498→4213 字符 / 85→66 行（-23%），删的全是「工具描述已有」或「代码已强制」的副本，路由与纪律一条未动——①**P0 修事实错误**：视频条目原写「当前没有视频生成管线…暂不支持 AI 生成视频」（H3 上线后已失真），改指分镜表卡「出视频·N 镜」且明写「不要声称产品不支持视频生成」；②**P0 修自相矛盾**：「不要开放式菜单让用户做选择题」与新增的方向类「编号列出让用户拍板」字面打架，改写为「问意图不要甩开放式菜单让用户自己猜你要干什么（方向类决策的带理由选项见下条）」；③**去重七处**：ops 清单（宪法版已漂移——update_node 缺 imageUrl、缺不传 position 布局与正文口径，全部收口到 canvas_ops 工具描述，工具描述同步补 imageUrls/rows 行字段形成超集）、分镜两类处理（generate_storyboard docstring 已有全套）、占位符规则、计划先行整节（propose_plan 描述已有）、cancel_research 与「不做调研直接走制作链」两处重复、行为准则 2「每轮只调一次工具」（`parallel_tool_calls=False` 机械强制 + 前端优先路由兜底，凤临天下事故已实锤模型会无视它）、行为准则 5「同一工具不超过 5 次」拍脑袋阈值（并入准则 2 改为「无进展就停下汇报」）。行为准则 5 条→3 条。回归：探针真聊两条 P0（视频请求不劝退且指分镜表 ✓ / 方向请求出 5 个带理由编号选项并请拍板 ✓）；无测试脚本依赖被删文本（已 grep 验证）。**矛盾对质补验（同日，含一次过拟合返工）**——原「矛盾对质」条款只写「发现矛盾就指出」，实测漏（用户说「单片 8 分钟做，另外每一集片尾留钩子」，luna 直接把「单片」和「每集」合并成「单集 8 分钟、共五集」照做）。第一次修法**过拟合**：把探针里的原话（「单片 8-12 分钟」「兵力数字不写死」）和台词模板写进了提示词——用户当场指出「太定制化/死板」（真实用户看到的是答案 key）。改为**抽象三查程序**（①这句话本身前后打架（时长/集数/数量自相矛盾）②与画布已定的策划、基调、决策是否一致（策划卡写了什么，这条要求就改了什么）③与已核实的事实边界是否冲突（说过不写死/不采用的东西，这条要求要写死/要用）；命中就点名冲突（哪一条、出自哪里）请裁决；用户明确改主意时按改道走但一句话说明影响），例句与模板全删。**泛化检验方法 = 用提示词里从未出现过的域测**：U1 画幅画风（策划卡横版水墨 vs 要求竖版赛博朋克）、U2 时长 vs 幕数（3 分钟 vs 五幕每幕 2 分钟）、U3 虚构桥段（策划卡禁演义 vs 要求草船借箭开场）→ **3/3 通过**，且模型会引用策划卡原文点名冲突。**三个探针硬伤（都已修，写测试必看）**：①**画布摘要必须走 AG-UI `context`（description 含「画布」），传 `state.canvasSummary` 到不了模型**——旧探针一直没传对，模型根本看不到策划卡；②旧提示词埋了例句，那次「通过」实为**背诵例句**不是检测（所以过拟合验证会自欺）；③三场景复用同一 HttpAgent 会串台（U3 看到 U2 对话），每场景要新建实例。判据侧：冲突词与裁决词**窗口共现**（±400 字，模型自然分句；同句判据会把「不同策略冲突…请把剧本发来」判成通过——假绿），措辞表要含「请拍板/需要你裁决/采用哪一种」等变体（否则假阴性）。结论：**抽象程序（查哪几处）对弱思考模型有效且可泛化；具体案例/台词一旦写进提示词就是过拟合**。行为仍是概率性的，**建议把拍板/审稿/对质三条扩进 intent-routing-test 场景组做常驻回归**（暂未做）。**工具描述是唯一真相源（2026-09-06 全量审计定的流程规）**：改产品行为（卡型/字段/枚举/模型清单/回填语义）必须同步 ① 工具描述 ② 相关 SKILL.md ③ 宪法路由句，三处矛盾以工具描述为准并当天修齐——审计实锤三例：script-to-assets 曾教「场景/道具建 note 加前缀」「设定图另建 image 卡」（与 canvas_ops、asset-aware-generation 矛盾，已重写）；宪法 add_node 枚举曾缺 scene/prop/costume/research（与 canvas_ops 描述矛盾，已同步）；generate_asset_images 手抄模型清单已漏 gemini-3.1-flash-image（改为从 `models.IMAGE_MODELS` 动态生成，f-string 不能作 docstring——正文进模块常量、def 后赋 `__doc__` 再 `tool()` 包装）。**标准链顺序依据（2026-09-05 竞品调研 8 家）**：有正经资产系统的 3 家里 ai-moive-studio/open-ai-canvas 资产先行（步骤硬依赖+分镜被资产版本门禁卡住）、novanova 混合（分镜文字与资产清单一次产出、资产图后置批量生成）；其余 4 家分镜先行但**没有资产系统**（靠 characterDNA 内联/拆镜规则保一致性）——wingsight 有资产系统且 generate_storyboard 吃 assets_json 名单（名单内引用、名单外幻觉名剔除），故资产先于分镜是默认；`script-to-assets` SKILL.md 已重写成标准链。**拆解契约 v2（2026-09-07 090701 项目「小女孩甲」事故）**：分类型拆解 flow 的 description/visual_notes 旧契约引偏——配角无外观事实时模型抄剧情交差（「在游乐园中与另一名小女孩手拉手奔跑的儿童」当角色描述）、visual_notes 收「光线、氛围」产出分镜级调色词（「争吵时光线略显压迫」），且描述带第二角色与 imagegen 尾注「不添加任何其他角色」正面打架。四 flow 契约改为：description=本体外观事实（剧本没写的按类型/年龄感**补全具体外观**，禁止剧情转述、禁止描述里出现其他角色）；visual_notes=本体色彩/材质/体态（scene 例外：光线氛围只写空间固有属性不写剧情情绪；prop 明确浅灰背景结构图不写光线）。配套防污染条款：generate_asset_images 与 canvas_ops 描述均明写「卡面正文是设定数据不是状态日志，出图成败用 status/聊天汇报，禁止把『已生成/出图失败』写进 body」——事故实锤：agent 把自己的汇报句追加进资产正文，残留句每次出图都被当事实注入提示词。存量清洗范式：正则剥残留（`[，,；;]\s*(已重新生成|本次出图失败|已将…标记)[^。]*[。.]?`，**只在真命中时改 body**——rstrip 尾句号会把全卡正文都判成变更）+ 配角经新契约 flow 重拆（decompose_script_assets 带 existing 名单保持原名，紧贴 读→改→PUT 两段式防 LLM 调用期间用户并发保存的 409）。回归：`node scripts/script-to-canvas-test.mjs`（模拟器已修占位符映射——connect_nodes 的占位符 id 未经解析时连线断言恒 0，对齐 applyOps 同批占位符语义）；路由回归照旧 `node scripts/intent-routing-test.mjs`（场景组 S1 上传无指令/S2 直接制作/S3 调研二义/S4 改道）。**技能手册层（2026-09-03，Agent Skills 规范渐进披露）**：`agent/skills/<name>/SKILL.md`（frontmatter `name`/`description`，description 兼作触发条件；影策 skills 底稿改写）——`graph.py` `load_skill_meta` 启动扫描把目录注入系统提示，正文经 `read_skill` 后端工具按需读（现有：asset-aware-generation / brainstorming（导演策划头脑风暴——obra/superpowers brainstorming（MIT）逐段忠实翻译：HARD-GATE 批准门/三路径当面宣布（快探/小改动/项目级）/反模式/红旗表/三清单/dot 流程图/终态绑定/概念图对比全部保留；仅四处硬适配：spec 文件+git 提交 → 画布《片名·策划》卡、writing-plans·TDD·elements-of-style 引用 → 交接 canvas-context/asset-aware-generation 执行、浏览器 visual companion → 概念图对比（出图花额度，提议单独成一条消息）；2026-09-04 补优先级豁免——明确执行指令（点名工具/「直接做」/计划卡流程）不走批准门直接执行、触发收窄到开放性想法，与宪法「计划展示后立即执行」对齐（曾拦下点名工具指令致 storyboard E2E 挂 3 项），勿回退、代码术语 → 创作对应物（repo→画布、unit→卡/资产）） / canvas-context（画布读写协议；2026-09-08 由 canvas-editing 合并而成） / de-ai-zh（去 AI 味改写手册，2026-09-08 自 juben 忠实搬运：24 种 AI 写作模式（Wikipedia:Signs of AI writing）+个性注入+五维评分；唯一硬适配=「在 wingsight 中的载体」节——文本卡/剧本卡经 read_node→改写→canvas_ops update_node body 写回原卡不另建卡，剧本台词保留口语特征只清叙述部分） / generation-recovery（novanova agent-recovery 决策树忠实移植——GPL 源已按我们错误形态重述，勿逐字回搬） / script-to-assets（剧本→资产链路工作流：拆解建卡/长镜头节拍/分镜卡字段/audio·compose 卡——2026-09-04 从宪法赎出，宪法只留路由句）/ revise-assets（改图分层路由：五通道=改设定重出／走造型 Look／**小步改图（默认优先）**／改行·genPrompt 重跑／改「实际发送提示词」原样重跑；一次只改 1-2 个变量、「其他不变」要写进提示词、失败迭代锚原图——2026-09-10 加，宪法留路由句；判据是「只搬了机制没搬路由」的补课，juben 五通道此前只活了机制层）；用户点名技能（「按技能「N」的规则处理」）同样先 read_skill 再执行，聊天流有 read_skill 工具卡——toolCards.tsx render-only 注册）。加手册 = 建 `agent/skills/<名>/SKILL.md` 一个目录，宪法只留路由一句；目录名与 frontmatter name 一致（spec 要求，read_skill 按名寻径）。**用户可见性（发现性）**：`GET /capabilities`（main.py，Claude Code 同构——**技能 = SKILL.md 操作手册**，仅此一类；Langflow 生成管线是工具不是技能，不进面板，入口 = 输入条打 `/`，数据源 `/skills`）喂聊天 header「技能」按钮的 `CapabilitiesDialog`（OverlayModal 单一列表：展开看手册全文 +「按此技能处理」点名插入输入条——对应 Claude Code 的 /技能名 显式调用；`CHAT_INSERT_TEXT_EVENT`；入口按钮 `data-track="chat.skills"`）。示例发现性归聊天空态建议词（SUGGESTIONS）。**技能管理（admin）**：面板内可直接编辑手册全文（raw SKILL.md，frontmatter 校验 name=目录名）与新建（name 小写连字符规范，自动生成 frontmatter）——`PUT/POST /capabilities/skills*`，仅 `role=admin`；保存后 `graph.refresh_skill_meta()` 热刷新目录进系统提示，**免重启 agent**（编辑/新建/点名按钮均有 data-track）。回归：`node scripts/capabilities-panel-test.mjs`。**宪法重排 + 三层去重（2026-09-08 第二轮，用户批准）**：结构从「消息路由」杂物抽屉（1639 字，混路由/协作原则/工具指向三类）改为「共创契约 → 决策原则 7 条 → 任务路由 → 读画布/写画布 → 执行纪律 → 操作手册 → 链路一/二/三」（2026-09-09 再加共创契约节）；「技能」两义分家（手册 = SKILL.md/read_skill；管线 = Langflow flow/list_langflow_skills）；「行为准则」改名「执行纪律」（语言口径并入身份行），操作手册节提到所有链路之前。**去重判据 = 按「离执行最近」保留**（契约/字段/枚举/id 格式 → 工具描述；流程/干跑/核对/失败处置 → 手册；路由 → 宪法；同条规则两层出现时删描述性复述、留动作性指令）——不是「哪处最强留哪处」，否则删掉手册副本反而削弱执行现场的行为。本轮收敛：猜 id 5→2（canvas_query 描述 + 摘要尾行，描述补「时间戳段不可推算，猜必错」）、正经卡型与不传 position 4→1（canvas_ops 描述）、绝不让卡片停在 loading 6→2（宪法链路三 + generation-recovery）、失败步骤不标完成 2→1（宪法）、调研二义 3→1（宪法任务路由）、范围默认全量（research_asset_references docstring 独占）；canvas-editing 并入 canvas-context（46 行 → 10 条，独有 4 条保留：版本/警告语义、error 卡先读、干跑 error/warning 分级、审批卡不重发）；script-to-assets 删默认话术/正经卡型/position/整表写回复述，asset-aware-generation 删二义性与范围复述。**read_skill 读文件实时生效，但技能目录 description 要 PUT 一次（refresh_skill_meta）或重启才刷新**。回归（修正后复跑）：intent-routing 14/14、script-to-canvas 2/2、capabilities-panel 17/17、chat-skill-invoke 4/4。**共创契约（2026-09-09，用户提出「还应该有个全局规则让 agent 知道自己要和用户共创」）**：宪法新增「## 共创契约（每轮先过）」节，三条可检查的动作——① **给材料与判断、决定权留给用户**（推荐给理由、放弃给原因；不替他拍板，也不空手把问题原样推回）② **先铺开再收敛**（需要用户在**内容候选**里做选择时先把符合条件的给全、数量诚实、查不全就明说；再给推荐与放弃理由——不要先筛成 2-3 个再让他选；**问意图不适用本条**）③ **判断带依据**（事实带来源/方向带材料底数/取舍带标准；「我考虑过了」不算依据），末尾注「共创≠事事请示」（意图明确照决策原则 2 直接执行）——**共创有两种失败模式，规则两头都堵**：A 自己拿主意（090803 项目实锤：先给 6 集选集表，用户追问「只有这 6 个可以讲的吗」才在聊天里补出十几条候选，候选全集与来源全被吞掉）/ B 事事请示把决策全推回用户。手册落地：real-documentary「入口」节加「候选全集先行」（盘点/筛选类请求先列全 + 每条一句背景/材料底数/来源，再给推荐与放弃原因；「只有这 N 个吗」的追问就是缺口报警）；原则 3 删掉与新契约重复的「带理由选项」括注、改指契约。**宪法加内容必须 A/B**（前科：多集口径曾把 intent-routing 打到 10/14，摘掉才恢复）。**A/B 实录（同日）**：首版契约（无适用范围）复跑 intent-routing **12/14**——S1「考据出口」被契约的「先铺开」挤掉（模型把「你这轮要做什么」铺成 A/B/C/D 四选一菜单，正好违反决策原则 1），S3「走调研了吗」被契约「判断带依据」推成抢答 start_deep_research（a768423e2069 老事故复发）；诊断用同构探针（带前端工具回传、打印轮1 全文）复现后改法：契约第 2 条限定「**内容候选**（选题/案例/讲法/结构/方案）才铺开，**问意图不适用**——仍是一句话带默认」，末尾加「本契约只管呈现方式，不改各链路已有路由与闸门（调研二义先问、轻量核实不算调研、执行类指令直接做照旧）」→ 复跑 **14/14**（S3 改走 research_asset_references）。**结论：全局契约必须写清适用边界，否则会压过已有的路由规则**。回归：`skill-routing-test` **14/14**（R1-R3 手册触发 + R4 目标陈述先问不出清单 + R5 brainstorming + R6 探索委托先铺开再收敛：清单项 37 / 有推荐 / 有取舍 / 有来源）+ `intent-routing-test` **14/14**。**借鉴 juben CLAUDE.md（2026-09-08，用户批准）**：juben 产品 profile 的 CLAUDE.md 248 行 / 55KB，**体量不搬**——大半是路径规范、项目目录树、project.json 字段、生产结构与 MCP 清单，因其 agent 直接操作文件系统；我们的 agent 操作画布卡与工具，等价信息已在工具描述/画布摘要/库里。**明确不借**：生产协调器门禁（`inspect_production` 单一真相源、逐步骤工具许可、`legacy_read_only` 冻结——与「自由画布 + 提案计划」的产品哲学相反）、静态「可用 Skills 表」（我们目录动态注入，写死必漂移）、dispatch 纪律（等真有子代理再借）。**只借四条行为纪律**：①**导演语言优先**（身份行：先说完什么/影响哪一步/怎么推进；工具名/字段名/路径不甩给用户；必须解释技术原因时先给结论再用一句括号说明是系统内部信息）②**边做边说**（执行纪律：>30s 任务在工具批次之间给 1-2 句中文进展，连续工具调用之间无可见正文视为违规，不伪造百分比）③**改前先读、改后重跑**（续写/修改/审阅前先 `read_node` 读正文——画布摘要只是索引、聊天摘要可能已压缩；改了 genPrompt/行 prompt 必须紧接着重跑出图/出视频，否则留「新提示词 + 旧画面」）④**考据按资产本尊判定 + 证据边界**（asset-aware-generation：资产本尊涉及真实朝代人物/地点/制度/宗教服饰/官服/器物，哪怕整片虚构也要考据，纯虚构资产不必；来源不足的视觉判断标「推断/艺术处理」不冒充事实）。另案（不在本批）：造型命题先行（juben L49 character-design，治「类型化描述出平均脸」，需新增造型简报产物 + 导演确认 + 出图门禁）、长期记忆（L76-82，先要有 save/list/delete_memory 工具）、个人技能（L35，我们只有 admin 编辑手册）、子代理/隔离审查（机制层第一缺口）。**验收**：探针两条——① 让 agent 改图片卡 `genPrompt` → `update_node` 落改且回复明说「提示词改了、但画面还是旧版」并提议重出（2/2）；② 多步拆解任务三轮各有过程正文、带真实数量（「12 张卡全部建好了」）。四套回归全绿：intent-routing 14/14、script-to-canvas ✓、capabilities-panel 17/17、chat-skill-invoke 4/4。**注**：混发工具仍偶发（R1 又出现 read_skill+decompose_script+canvas_ops 同条）——纪律只是降概率，真正兜底是 sanitize 占位 + 探针只答前端工具后的自愈；若要根治需图层面拆分 mixed 消息（未做）。
- **juben 技能搬运盘点（2026-09-08，用户批准）**：juben 产品 profile 有 25 个技能（`~/Desktop/juben/agent_runtime_profile/.claude/skills/`），全量评估后只搬 3 个——**real-documentary**（真实题材创作纪律：事实铁硬/呈现自由/推断克制、批准即授权包、修改路由分层、呈现策略门；阶段产物全部落画布卡，juben 的文件路径/author 工具/生产协调器/阶段五 JSON 全部删掉）、**storyboard-director**（只搬拆镜自查 8 条：禁止机械拆镜/声画对位与旁白唯一真相源/声音容量≈5 单位每秒/时长与镜头数/逐镜资产申报含剪影与换装/忠实正文/参考图承担一致性；JSON 与工具链不搬，改指分镜表 rows）、**documentary-promotion**（宣发事实卡单一真相源 + brief 只传原话等编排纪律；平台规则早已随 `promotion_platform_rules.py` + `promo-copy.json` 移植，不必再搬）。宪法只加两行路由（真实题材 → read_skill real-documentary；宣发 → read_skill documentary-promotion）+ 链路一整表分镜句尾加 storyboard-director。**不搬清单与理由（勿重复评估）**：free-image/generate-assets/generate-storyboard/generate-video/generate-grid/topic-scout/case-research/manage-project 已有等价物或更强；asset-draft-workflow/analyze-assets-workflow 思想已被 asset-aware-generation 覆盖；seedance-* 模型不同（我们走 H3）；short-drama 是另一个内容模式；video-workflow 是 juben 生产协调器（哲学相反）；de-ai-zh/character-design/revise-assets 已搬。**能力缺口（手册搬不动、缺的是工具）**：TTS 配音（generate-narration-audio；仓库有 `doc/dmxapi/minimax-speech.md`，路径 = 加 TTS flow/端点 + audio 卡回填 audioUrl）、个人技能（skill-author）、素材库智能入库（asset-library-import）、飞书文档（feishu-docs——组件已移植且接在 promo-copy flow 的 `FeishuDocComponent-Source`，但 `.env.local` 缺 `FEISHU_APP_ID/SECRET` → 通道休眠，配好即用）。回归：`node scripts/skill-routing-test.mjs`（自然语言触发三手册，3/3）。**入口层补课（2026-09-08，090803 项目事故）**：用户只给题材（「我想做一部历史纪录片，关于中国近现代的骗局」= 目标陈述）时 agent 直接 4 次 web_search 后甩出 3 个方向 + 推荐 + 选择题，用户反馈「应该先和我充分沟通，了解我的真实想法」——根因是搬运时丢了 juben 的**入口层**（`references/director-collaboration.md` §0/§1：加载技能≠授权开工、目标陈述≠生成指令、没锚点先问一个高信息量问题不给具体选题；`real-documentary/references/01-整部策划.md` 的「入口：主题导览对话」：先盘再讲→讲述而非答题→**对话收敛优先菜单兜底**），而宪法决策原则 3 把「只给题材」定义成「核实→出方向选项」、任务路由又让 real-documentary 压过 brainstorming（宪法全文从未提它，具体路由压过目录描述）。修法：手册补「入口：主题导览对话」节 + 三类委托分流（目标陈述→先问意图不出清单 / 探索委托→直给 / 执行委托→直接推进），宪法决策原则 3 改「先分流委托类型，再呈报拍板」并把「用户只给题材要方向清单时」改成「用户点名要方向清单时」（原文歧义正是本次踩点）。**brainstorming 保持原样管非真实题材新片**——不设成总入口：它的项目级终态是《片名·策划》卡，与 real-documentary 的《整部策划/单集策划》双真相源打架，且它是逐段忠实搬运版，不为纪录片改流程。诊断法留档：checkpoint 反序列化看工具调用序列（本次全程只有一次 read_skill=real-documentary）+ 画布 0 节点 = 没建卡、纯聊天输出。回归：`node scripts/skill-routing-test.mjs`（R1-R3 手册触发 + R4 三判据：无方向清单标记/有反问意图/无执行类工具 + R5 虚构开放想法→brainstorming；判据是措辞型、概率性，看失败形态）。**导览深度下限（2026-09-11 用户反馈「调研不够深入、结论没法用」）**：现象——目标陈述进来后 agent 用 2 次 web_search 摘要给出六块「维基目录」，每块只写「材料：厚且硬——社科院近代史所…」，一个能进片子的钩子都没有，还把「我没搜到」写成「横跨百年的骗局史我没查到有人做过」（播客《半拿铁｜商业沉浮录》就做过系列）；用户点名的两个料（1986 温州乐清抬会/李启峰、蚁力神赵本山代言）一个没盘到。根因**不是模型偷懒而是规格**：导览节把「做**一轮轻量**题材盘点」「**每块一行定位**」写成必做，而「高光与反差」是「按题材取用」的可选项——强调点反了，必做的恰好是导演不要的 TOC，有价值的被降级成可选。修法（`agent/skills/real-documentary/SKILL.md` 导览节四条）：① **盘要读到原文**——「轻量」只指不发起深度调研任务/不建来源卡，骨架每块至少 `web_fetch` 一篇正文（搜索摘要只有标题加一句话，钩子全在正文里），前人作品**点名到作品**（片名/期号/播客/书名，「央视做过专题」这种笼统话等于没盘）；② **骨架是框、钩子才是货**——每块一条具体钩子（人名/物证/反差/名人关联/当事人下落）+ 可核实出处，钩子总量少于 8 条通常说明盘得不够，材料底数改导演口径（可拍素材 + 前人做到哪一步）；③ **空白判断必须写明检索范围**（「我没搜到」≠「没人做过」）；④ **按时代逐段检索、不许跳段**（记得的案子决定视野，漏掉的是整整一个十年——1980s 前两轮都没出现，补上这条第三轮才盘到乐清抬会；确实没料的段要明说，不要让它在列表里消失）。实测（`node scripts/tour-depth-test.mjs`，真 LLM 无浏览器约 1 分钟）：同一题材 2 搜/1531 字 → 22 调用（6 工具步、83 秒）/2852 字 → 14 调用/3400 字 → 18 调用（14 搜 + 3 读全文、4 工具步 55 秒）/2177 字（字数一律**最终答复**口径——探针初版把过程叙述混进字数，与旧基线不可比，已分开量；过程叙述分别 0/352/153 字，「会不会边干边说」是波动、不是修好了）。**其中一轮没达到「每块至少一篇原文」（7 块只读了 3 篇），探针判读因此分三级（硬失败：没读手册/一篇没读 → 警告：块覆盖欠账 → 达标），别把绿灯当全能**。骨架块 6→7 且**显式标出「1949—1978 这一段我没盘到东西」**，钩子具体到「会主李启峰嫌万元以下的票子不收、会款用尺量用秤称」「丁宁把 200 多台服务器及 1200 余册证据埋进 6 米地下、警方挖 20 多小时」「陈相贵在《刘老根 2》里演廉政官员」「汪振东当庭质问『王奉友不死，偏要我死？』」，前人作品点名到《半拿铁｜商业沉浮录》系列 + 央视《经济半小时》；赵本山代言的「谁用谁知道」也盘到了。**关键因果：这 18 次调用只用了 5 个模型步（步内 1/6/6/5，实测 55 秒）——深挖是靠并行工具调用换来的**，而并行正是「一条消息只发一个工具调用」禁止、`parallel_tool_calls=False` 又没挡住的那件事（DeepSeek 忽略该参数）。**深挖与串行在成本上互斥**：真按一条消息一个调用跑，同样 18 次要 18 个往返，模型会像旧版那样自己把预算砍到 2 次搜索。手册改动即热生效（`read_skill` 每次读磁盘、免重启），但深度是概率性行为（三轮跑分 2/14/18 次调用、每轮新发现的钩子不同），改完必用探针复跑判读形态、不看单次分数。**brainstorming 生效边界（2026-09-08 探针实测）**：非真实题材的开放性想法（「我想拍一部古装短剧，还没想好具体拍什么」）→ 读 brainstorming、反问意图、不动手（多选澄清是它项目级流程第 4 步允许的形态，别当成「又甩菜单」）；真实题材 → 归 real-documentary 的导览对话（同一个入口门的两个分支，避免两套策划产物双真相源）；点名执行指令 → 按它自己 description 的豁免直接执行；画布为空时说「重组画布」→ 它先问你素材在哪（无可改之物不构成小改动）。**capabilities-panel-test 定位器修正**：行按钮可访问名=技能名+描述，描述互引（character-design 描述提到 asset-aware-generation）会让裸文本/regex 撞多行 → 改锚定行首 `^name\b`。
- **LLM 生成类能力一律走 Langflow**（做成 flow，不在 agent 代码里直调模型/写死提示词）；唯一例外是聊天主循环本身（`graph.py` LangGraph 直连 DeepSeek）。约定与清单见下节「Langflow 工作流」
- **前端工具调用优先路由**：模型消息里混有前端/后端工具调用时必须 END 等浏览器执行（含后端调用的消息进 ToolNode 会返回 invalid tool 破坏交替）；`graph.py` 的 `_unanswered_frontend_calls` 与 `_sanitize_messages_for_model` 是历史交替守卫，勿删。**混合调用毒历史（2026-09-07 凤临天下项目连环 400 事故）**：模型无视宪法规则 2 与 `parallel_tool_calls=False`，一条 assistant 消息混发 canvas_ops（前端）+decompose_script（后端）——前端优先路由 END 后浏览器只应答前端调用，模型下轮重发后端调用，历史出现 `AI(mixed)→Tool(部分应答)→AI(reissue)`。旧版 sanitize 的占位 ToolMessage 补在**序列末尾**（只在遇到普通消息时 flush，AI(tool_calls) 分支不收口 pending），中段交替非法，上游每轮 400：`An assistant message with 'tool_calls' must be followed by tool messages responding to each 'tool_call_id'`，会话永久毒化（用户连发「好×4」全是浏览器端 `agent_run_failed_event`/「Error in input stream」）。修法两处：① sanitize 对**任何非 ToolMessage 消息**（含下一条 AI(tool_calls)）都先 flush 占位——补位必须落在 assistant 与下一条 assistant 之间；② `_unanswered_frontend_calls` 收窄为**尾部判定**（最后一条消息是 AI 且含未应答前端调用才 END 等浏览器）——历史中部滞留的未应答前端调用是陈旧轮次（刷新/断流丢结果），旧版据此中途 END 会把整条会话哑火（用户每发一条都被无声吞掉，090701 项目实锤）。无需迁移数据：毒在 checkpoint 里，清洗是发送侧每轮重建，修复后存量毒会话自动复活（离线重放验证：真实中毒载荷 200 且模型正常应答）。回归：`cd agent && uv run python ../scripts/chat-alternation-guard-test.py`（8 组场景无 LLM）。**混合调用纪律回归（2026-09-08 宪法重排当轮实测）**：上一轮审计把宪法「每轮只发起一次工具调用」当纯冗余删掉（理由写的是 `parallel_tool_calls=False` 机械强制 + 前端优先路由兜底）——**该判断实测不成立，已恢复**：script-to-canvas 4 次跑挂 1 次、intent-routing S2 挂 1 次，模型一条消息混发 read_skill/decompose_script + canvas_ops，服务端 END 等前端时**后端调用当轮不执行**、链条断（一例 agent 汇报「共落 11 张卡」画布 0 节点，一例模型自述「decompose_script 只返回了 ok」）。恢复的条文在「执行纪律」：「一条消息只发一个工具调用——尤其不要混发前端工具（canvas_ops/canvas_query/read_node/propose_plan 等）与后端工具（read_skill/decompose_script/generate_storyboard/research_* 等）：混发时后端调用当轮不会执行，链条会断」。配套修 `scripts/intent-routing-test.mjs` 两处探针失真：① `respondFrontendTools` 只答前端工具（旧版把后端调用落到 else 分支回 `{ok:true}`，模型把假结果当真——「只返回了 ok」的出处；浏览器本来就没有后端工具 handler，服务端下一轮用 sanitize 补「本轮未执行」占位）；② S5 夹具改回与对话同项目（旧夹具沿用 S3 的八仙饭店资产去接南洋悬案对话，模型按矛盾对质规则停下问「画布和项目对不上」——真实产品画布与对话同项目，夹具不该制造不可能状态把模型带出戏）。**第三批探针保真修正（2026-09-08 分集回归期间，三轮才收敛）**：① `canvas_ops` 模拟器补 `op` 键校验（真实 `normalizeOps` 拒绝缺 op 的操作，探针旧版当成功——模型偶发把键写成 `type`，一路错到底、断言才报红，真实产品会回错让它自纠）；② 波次上限 8→12→30（模型为发起考据逐张 `read_node` 读资产详情属正常行为，预算吃紧是探针容量问题，S5 曾因此假红）+ 每轮波次打点；③ S5 夹具改用轮2 模型自建画布（同项目画布是持久的，掉包一组新 id 会被矛盾对质规则拦下问「和我建的对不上」）。**结论：intent-routing 是概率性行为探针**——同日 7 次跑分 14/14、14/14、13/14、10/14、14/14、13/14、11/14，失败场景每次不同且均为模型侧波动（`type`/`op` 笔误、混发工具烧波次、措辞未命中断言关键词），与产品代码无因果；判读时看失败**形态**不看单次分数。
- **分集（纪录片多集，2026-09-08 用户拍板「不加集卡」）**：**一张剧本卡 = 一集**——不引入新卡型、不做账本/自动拆集（竞品调研：只有 ai-moive-studio 的 Chapter 与 open-ai-canvas 的 ProjectUnit 是真集实体，其余六家靠「一集一项目/一画布」或分组框糊；juben 的账本 + plan_episodes 服务「长源文自动拆几十集」，用户明确不要）。归属 = `data.episodeId`（= 所属剧本卡 nodeId），**出生继承**（加号手柄建卡 `inheritEpisodeId`、剧本卡「拆分镜表」、分镜表出图/出视频落卡、复制粘贴天然带、agent 侧 `applyOps` 同批 connect_nodes 源卡继承）——用户与 agent 都不用声明；**资产卡不带 episodeId**（跨集共享，纪录片一致性的前提）。可见性：剧本卡 footer 与画布摘要剧本行给「本集：分镜表 N · 镜头图 N…」（`episodeStatsLine` 读时计算、不落字段）、产物卡行尾 `⟨集名前6字⟩`、`canvas_query(episodeId)` 过滤、剧本卡工具条「下载本集」（≥2 张剧本卡才出现，按集打包 zip，`bundleZipName(项目名-集名)`）。存量兜底：sanitize 清悬空 episodeId（`fixedEpisodes`）。**口径落点 = canvas_ops 工具描述（常驻）+ script-to-assets 手册「多集项目」节；宪法不放**——2026-09-08 A/B 实测：宪法加一句多集口径后 intent-routing 13/14、10/14（S2/S4b/S5 掉），摘掉后 14/14；任务知识放手册、宪法只放路由是既定原则，勿再加回。回归：`scripts/episode-inherit-test.mjs`。**集号与排序（2026-09-08 二期）**：`data.episodeNo`（正整数，1 起）是集序的显式事实源——集序不再等于建卡顺序。**自动排号**：`store.addNode` 对无号 script 卡取 `nextEpisodeNo`（现有最大+1），所以 UI 建卡、agent `canvas_ops`、复制粘贴/Alt 拖拽（`renumberClonedScripts` 副本排末尾，防两张「第 3 集」）、选题池落卡全自动；显式给号一律尊重（ops 白名单 add_node/update_node 都收，非法值 `episodeNoIssue` 明报不静默丢）。**排序口径唯一入口 `episodeList`**（按号升序，缺号/同号殿后按画布顺序——老卡无号不假装有集号，`episodeLabel` 缺号只给标题）。**重排**：`moveEpisode(id,±1)` 交换相邻后**整表归一到 1..N**（缺号/重复号一次点击即得确定顺序）。可见性：剧本卡 footer「第 N 集」徽标（≥2 张剧本卡才显示）+ 摘要剧本行「（第 N 集 · 本集：…）」+ 分集面板。**分集面板**（`components/canvas/EpisodePanel.tsx`，底坞「分集」入口，≥2 张剧本卡才出现）：集目录（集号+标题+本集产物统计）+ 点集**聚焦**（`FOCUS_NODES_EVENT` 运镜 + 非本集卡/线 `ws-node-dimmed` 压暗，只做视觉不拦操作；再点该集或 Esc 退出，聚焦的剧本卡被删渲染期自动清）+ 悬停 ↑↓ 重排。**选题池按分集落卡（2026-09-08 二期，原「另案」）**：系列选题认领时可 `POST /topics/{id}/adopt?mode=episodes` → 一张总纲 note（母题/钩子/成片推演/对标/观众/原型出处）+ 每集一张剧本卡（正文=本集要点+母题指路，`episodeNo` 1..N，两列网格），分集构想从正文一段文本变成真卡片；**系列判定 `_is_series_topic`**=深挖卡 `research.scale∈{series,anthology}` 或生料卡标签「单元选集/系列网格」（单片/普通生料卡的 episodes 是段落节拍，不按集落卡——前端 `isSeriesTopic` 同源，按钮只在系列卡出现）；`mode` 非法 400、无分集构想 400 明报。**不做**：行级归属（一张分镜表混多集——口径是一集一张表）、集号重排的拖拽排序（↑↓ 够用）、按集聚焦的持久化（关面板即退出）。回归：`scripts/episode-plan-test.mjs` + `cd agent && uv run python test_topic_pool.py`（`run_adopt_episodes`）
- **聊天附件/引用在对话记录里显示为实体 chip，正文走带外通道（2026-09-11，用户反馈「应该显示文档实体而不是整个文档内容」）**：行业共识（codex/gemini-cli/opencode 源码实测：opencode 把附件存成 `part.type==='file'` 显示 chip、内容作 synthetic part 只喂模型；gemini-cli 历史留原始 `@path`、展开的全文只进 payload；codex 只给路径）是**记录显示实体、内容不进气泡**，我们此前把附件正文直接拼进用户消息正文再原样渲染——本地实测一条 1.2 万字剧本让气泡高 **21,469px**、占整个会话滚动区的 94%，并连带把全文倒进「编辑重发」输入框、把轮次轨摘要污染成「（见附件与引用的画布卡片）附件：- 文…」。**契约 = `lib/chat/messageContext.ts`**（写入口 `buildContextText`、读入口 `splitMessageContext`，必须一起改）：消息 = `显示文本` + `<<<WS-CTX>>>` + 人话上下文（引用卡 + 附件清单 + 文档正文，**与旧格式一字不差，模型侧零变化**）+ `<<<WS-MANIFEST>>>` + JSON 元数据（chip 用的 name/kind/url/nodeId/chars，**不含正文**）。人话段与 manifest 分开是刻意的：模型要自然语言、界面要结构化字段，让界面正则人话段会被文件名里的「」拆坏。**为什么不用 AG-UI 原生 `document` part（2026-09-11 实测否决）**：ag_ui_langgraph 会把它转成 LangChain `file` 块，而我们的模型端点（deepseek，OpenAI 兼容）`document` 变体直接「unknown variant」、`file` 块只收图片（webp/png/jpeg/gif，「unsupported file」）——正文只能继续走 text part，隐藏放在显示层。**消费者**（全部只认显示文本）：`Sidebar.UserBubble` 出 chip（文档=文件名+字数、图片=缩略图、视频/音频=文件名、引用卡=类型·标题；点击画布卡/资料卡运镜定位、媒体开原文件、失败项红 chip）、`ChatSearch`（搜不到附件正文——界面上没渲染它，命中数=高亮数）、`TurnLocator` 摘要（无文字只有附件时退 chip 名列表）、复制（那句话 + `[附件与引用] …` 一行）、`contentToMarkdown`（导出对话不复制正文，正文是画布资料卡的事）、**编辑重发**（铅笔改传原始 `content`，输入条恢复「那句话 + @ chip + 附件」——文档从画布资料卡取回正文重建 inline 附件，**重发后 payload 语义不丢**；此前铅笔把气泡全文 dispatch 回输入框，1.2 万字整篇灌进输入条）。**存量迁移**：旧格式（无界标）在 `ChatPersistence` 水合边界经 `migrateLegacyUserContent` 一次性改写（幂等，正文一个字不丢；老消息没记资料卡 id → chip 不可点击），实测 091001 那条气泡 21,469px → **36px**、会话滚动 22,804 → **1,371px**。顺修一处口径漂移：聊天 @ 引用行的正文此前偷偷 `slice(0,200)`，违反 2026-09-04「全站不截断」，已改全量注入。**勿回退**：把正文显示回气泡、或去掉界标/ manifest，都会让「所显即所发」与「编辑重发倒全文」两个坑复发；新增附件类型必须同步 `KIND_LABEL`/`AttachmentKind`/manifest 解析（脏项静默丢弃）。回归：`pnpm dlx tsx scripts/chat-context-contract-test.mjs`（32 项纯函数：模型侧一字不少 / manifest 不重复正文 / 坏 manifest 与老消息不炸 / 存量迁移幂等）（2 项真跑 LLM 见上）、`node scripts/chat-attachment-display-test.mjs`（31 项浏览器：气泡只出 chip + payload 仍带全文 + 搜索/复制/轮次轨/编辑重发（含 @ 引用回填成 chip、**`@标题` 字面量只出现一次**——回填前必须摘掉显示文本里的字面量，否则同一张卡上两次）+ 气泡高度 <300px（此前同类消息 21,469px）；`REAL=1` 追加 2 项真跑 LLM——正文里埋暗号，模型答得出来才证明模型侧没丢）
- @ag-ui/client 0.0.57：公共 API 是 `runAgent()`（自动带 runId/管理 agent.messages），误用 `run()` 会 422；nodeType 字段驱动 React Flow 渲染器选择

## Langflow 工作流（LLM 生成能力全走这里）

业务 flow 是**纯链式**（非 Agent 组件），只经 **v1 阻塞 API** 调用（agui 流不产 TEXT_MESSAGE）。

- **版本化源在本项目 `agent/flows/`**（README 有 flow 清单 / flow id / tweaks 节点对照表）；langflow 自己的 SQLite 只是运行时存储。**引擎源码 subtree 并入 `langflow/`**（fork：github.com/qq1151131109/langflow；flow id 是导入时生成的 UUID，换机器导入后会变——用 `scripts/setup-langflow.sh` 自动导 flow 并回写 `.env.local`）
- **调用**：`skills.run_flow_blocking`（`POST /api/v1/run/{flow_id}`，超时 300s）；flow id 存根目录 `.env.local` 的 `LANGFLOW_*_FLOW_ID`。参数两种注法——tweaks 按**节点 id** 注入（Prompt 模板变量只收**字符串**，传 int 会 500），或拼进 `input_value` 文本头（分镜表生成即此式，不怕节点重建换 id）
- **改 flow**：UI（localhost:7860）里改并**保存** → 下一次调用立即生效（运行时现读 DB，无需重启）；然后 `cd agent/flows && ./export.sh <flow_id> <文件>.json` 回写本目录保持一致。注意：删节点重建会换节点 id，代码里按 id 注参的（宣发文案 `PromptTemplate-Writer`、出图 `BatchAssetSheet-img02`）要同步；改自定义组件源码需重启 langflow（模块缓存）
- **新建能力**：UI 画 flow → 调试 → `export.sh` 收进 `agent/flows/` → flow id 记入 `.env.local` → agent 加薄端点/技能包装（参考 `POST /storyboard/generate`）

## 已知坑

- **CopilotKit react-core 有本地补丁须人工维护**（`patches/@copilotkit__react-core@1.69.3.patch`，pnpm-workspace.yaml 注册）：上游 `entryPointsToFunctionCallHandler` 把**每个前端工具 handler 包在 `flushSync(async () => …)` 里**——flushSync 根本不等 async 函数（只 flush 到第一个 await，纯反模式），且 AG-UI 事件在 React 渲染中撞进来时触发 React 19 的「flushSync was called from inside a lifecycle method」console 报错（2026-09-06 之前每轮带工具调用的对话都刷屏）。补丁摘掉外层 flushSync、handler 直接执行（保留 20ms settle）。**升版本时 patch 会 apply 失败报错**（好事，能看见）——先查上游 `entryPointsToFunctionCallHandler` 是否还包着 flushSync（1.70.1 已核实原样），修了就删补丁，没修就重打。`useLightbox` 里的两处 flushSync 是 View Transition 官方用法，保留勿动（全依赖审计过：patch 后整个 @copilotkit 系可执行 flushSync 仅此两处，Next dev overlay 会跨刷新积攒旧报错——再见到 flushSync 警告先硬刷新复现再排查，别当新事故）。验证口：`scripts/style-picker-handoff-test.mjs` 末行 console.error 汇总（补丁后 0 条 flushSync，仅剩无关 404）。
- **聊天多模态发送必须复刻 in-context sendMessage 内核（2026-09-06 修复：v2 迁移起发图片/视频附件一直是坏的）**：自定义输入条发多模态消息的正路 = `useAgent({agentId})`（`@copilotkit/react-core/v2`，取侧栏正在用的注册表包装 agent 单例）+ `agent.addMessage({id, role:"user", content:[text part, ...媒体 part]} 裸对象)` + `copilotkit.runAgent({agent})`（`useCopilotKit().copilotkit`）——这正是 v2 `useCopilotChatInternal` 的 sendMessage 实现（读 react-core dist/index.mjs 可验）。**三条歧路都别走**：①废弃版 `useCopilotChat().appendMessage` 把裸对象直塞 `gqlToAGUI` 炸 `message.isResultMessage is not a function`（它期望 GQL Message 实例、输出还是纯文本 TextMessage，多模态 part 会丢）；②`useCopilotChatHeadless_c` 是 v1-deprecated 的**独立 runtime**，消息 resolve 了也进不了侧栏可见聊天（页面级、树内调用都一样）；③槽位 `onSubmitMessage(string)` 只收字符串。文本消息走 onSend 即可（stock handleSendMessage 内部同样落到 in-context sendMessage）。**附件状态在 submit 里必须读 ref 不是闭包 state（2026-09-07 霸王龙项目「发了图 agent 没收到」五连事故）**：submit 会 `await Promise.allSettled(uploadsRef)` 等在途上传——await 归来后组件闭包里的 `attachments` 仍是点击那帧的快照（status="uploading"，ready/inline/error 三分支全匹配不上），附件被**静默**丢成纯文本消息（上传明明 200、chip 明明显示）。用户路径是 **Enter**（编辑器 onSubmit 无 uploading 闸直通 submit；发送按钮 disabled 会被测试的动作性检查等过竞态窗——E2E 复刻必须 keyboard.press("Enter")）。修法 = `attachmentsRef` 写穿（writeAttachments 同步写 ref+state，一切状态更新走它），submit await 后读 `attachmentsRef.current`。附件**不设张数上限**（juben c28ab13a 同款裁决：批量设定图一次带齐——曾有「一条 6 个」的拍脑袋上限把 13 张静默砍成 6 张；附件是 URL 型 part 无体积压力，上游视觉模型有限制会明报，不在此静默预砍）。**图片上卡唯一通道 = canvas_ops update_node 的 imageUrl 字段（2026-09-07 霸王龙项目「卡片里没看到变化」事故）**：用户上传图想挂到资产卡，模型因描述从未教过 imageUrl、自作主张把「参考图：/agent-service/assets/x.png」写进 body 正文——字段管线（ops.ts imageUrl/imageUrls 透传）一直都在，纯描述缺口。描述已教「imageUrl 填 URL 即上卡、多图 imageUrls 候选、禁止 URL 进正文」；存量污染用正则剥句子迁移（参考图/视觉参考 URL 句 → imageUrl 字段）。
**本地图内嵌必须降采样 + 总预算闸 + 只嵌当前用户轮（2026-09-07 霸王龙项目连环事故）**：视觉模型路径 `_embed_local_media` 把 `/agent-service/assets/` URL 转 base64——13 张 MB 级设定图全量内嵌 63MB，超 DMX「单请求图片总量 50MB」上限直接 400，且消息留在历史里**每轮重发同样 63MB，会话永久毒化**（「继续」也全 400）。修法三层：`_local_asset_to_data_url` 经 ffmpeg 降采样（1280 边 JPEG，典型 150-400KB/张；图名随机 hex 不可变 → 名级缓存，历史重放不重编码）；`_cap_embedded_media` 总预算闸 40MB（最新往旧保留，超限消息退 `_flatten_media_message` URL 清单——data: URL 不罗列只标记，防兆级串进文本）；**历史轮次内嵌图退 URL 清单（`_sanitize_messages_for_model` 只对当前用户轮嵌图，`_current_turn_start` = 最后一条 HumanMessage 往前的连续 human 区段）**——19 张降采样后的历史内嵌图（3.2MB，远低于预算闸）照样让 gpt-5.6-luna 把整个会话归类成「素材投喂等指令」：连「按附件出图」这种明示指令、甚至「为啥我在卡片里没看到呢」这种直接提问都答非所问，逐轮复读「未检测到制作指令」，且**不是自我锚定**（剥掉复读回复照样犯）、消息也没丢（checkpoint 逐条都在）——离线重放二分实锤：同输入只把内嵌图退成 URL 文本清单，模型立刻恢复（直接 read_skill 走出图流程/直接发 canvas_ops 写 imageUrl）。URL 清单对模型完全够用（干活本来就靠清单 URL），当轮新到的图才是视觉理解刚需；轮内工具往返（尾部是 AI tool_calls/ToolMessage）按最后一条 human 定位轮起点，本轮图不中途降级。排查法：checkpoint 反序列化（langgraph JsonPlusSerializer）+ 复刻 chat_node 发送管线直连上游离线重放，零副作用定位「模型看到了什么」。回归：`node scripts/attach-race-test.mjs`（两阶段：13 张拖入不截断 + 竞态红绿——旧代码纯文本丢附件见血）。
- **二进制文档附件走服务端文本提取（2026-09-07 修复：doc/docx/rtf/pdf 能选进聊天但 agent 读不了；2026-09-11 扩到表格）**：聊天附件里这些格式浏览器读不了，`ChatInput` 的 `EXTRACT_TEXT_EXT` 分支经 `POST /agent-service/extract-text?name=原文件名`（裸 body + 认证，`lib/projects.ts extractText`；实现在 `agent/doc_extract.py`）提取后内联进消息（`.docx` zip 直解 word/document.xml 零依赖、`.doc/.rtf` 走 `soffice --headless` 每请求独立 profile 防并发锁、`.xlsx/.xlsm` 走 openpyxl 读表转 Markdown（多表分节，`SHEET_MAX=5`/`ROWS_MAX=300`/`COLS_MAX=40` 超限按节明示截断）、`.xls` 先 soffice 转 xlsx 再读表（保多表与单元格结构）、`.pdf` 走 `pdftotext`——本机都装了）；失败明报中文原因（扫描件无文字层/损坏/加密/415 不支持的扩展），chip tooltip 与消息行都带原因。**二进制格式一律走后端提取，绝不回退前端「按 UTF-8 硬读」**——2026-09-11 大宋异事录事故：`.xlsx` 落到前端文本直读兜底，整份 zip 包被当正文发给模型（17000 字里 6967 个替换字符 + 446 个 NUL，`PK\x03\x04` 开头）。内联字符上限 50000（原 8000 会把剧本截残，「全站不截断」口径，超长靠对话滚动压缩兜底）。**调用必须带 name**（后端靠它推扩展名，漏传=415，实测踩过）；soffice 输出的 BOM 服务端剥掉。测试法：python zipfile 自造 docx + `soffice --convert-to doc/pdf` 反向生成夹具打端点；浏览器侧 Playwright 附件上传断言「（内联）」chip 与消息正文含提取文本。
- **卡内弹层必须 portal 到 body**（`.ws-card` 有 `overflow:hidden` 防内容越界）：卡内 `absolute` 弹层在矮卡里向上溢出卡顶被裁剪，Playwright 报「element is visible」但点击被节点级工具条（`absolute left-2 top-2 z-10`）拦截——视觉可见≠可点。先例：分镜行 @ 候选弹层与导出菜单（`ExportMenuButton`）都 `createPortal` + `fixed` + `getBoundingClientRect` 定位；注意 portal 内容不在节点 DOM 子树里，外点关闭要认 弹层+触发按钮 两个 ref，测试侧也不能按卡作用域定位菜单项。**z 序档位表**（2026-09-04 起，全文见 `app/globals.css`「z 序档位表」）：聊天侧栏 1200 < portal 锚定浮层（菜单/@候选/来源悬停卡/Toast）1250 < 全屏模态 1300 < 确认框/全局错误 1400（悬停提示 9999 恒顶）；画布内浮层在 `.react-flow` 层叠上下文内 z 再高也压不过侧栏——新弹层照档入座，勿散写 magic number（曾两批补漏：ResearchReader/ImageTemplateDialog/NodeInfoModal/VideoLightbox/导演台 z-50~60 被侧栏盖住）
- **聊天侧栏让位宽度 = v2 实测回灌，绝不传 `width` prop**（2026-09-04「顶栏账户头像被裁半个圆」事故）：CopilotKit v2 的 `CopilotSidebar` 只在 **width 缺省**时才挂 ResizeObserver 量 `aside` 实测宽度、据此写 `body { margin-inline-end }` 让页面让位；传了 `width={420}` 就把让位**钉死**在传入值，而实际宽度由可拖的 `--ws-chat-w`（存 localStorage）决定 —— 两者一不等侧栏就盖住画布右缘（顶栏「主题/分享/账户」被裁、小地图被吃一条、底坞与空态中心偏右）。宽度事实源单向：`--ws-chat-w`（globals.css 兜底 420px，**不再读 v2 自带的 `--sidebar-width`** —— 它现在由 v2 从实测宽度回写，读它会形成回显自锁、默认宽度静默漂到 480）→ 内联 `width !important` 落地（v2 的 adopted stylesheet 层叠在后，文档层同分规则赢不了）→ v2 量回来喂 body margin。拖宽期间 `html.ws-chat-dragging body { transition: none }` 掐掉 v2 的 260ms 让位过渡（否则拖一条慢半拍的橡皮筋）。**开合记忆（2026-09-08 用户拍板「默认展开 + 手动收起后记住」）**：`defaultOpen={readChatOpenPref()}`（`wingsight_chat_open`，默认展开）+ 关/开都落盘。两个 v2 事实决定了实现写法：①`defaultOpen` 只在挂载时当初始值（provider `useState(isModalDefaultOpen ?? true)`），运行时没有受控 `open`；②**存在外层 v2 配置**（`<CopilotKit>` 的上下文桥，`isModalOpen` 默认 true），sidebar 自己的 provider 在 `hasExplicitDefault` 时会**采纳父级值**（dist 里 `setInternalModalOpen(parentConfig.isModalOpen)`，dev StrictMode 双挂载下必现）——所以只在 `CopilotSidebar` 上给 `defaultOpen` 会被父级覆盖回展开；必须在 `ThemedSidebar`（外层 provider 内）挂载时把记忆值 `outerConfig.setModalOpen(pref)` 推一次，之后开合由用户操作经 v2 同步。回归：`node scripts/sidebar-width-reserve-test.mjs`（含「拖宽 300px 后仍贴合」的回灌链路断言，旧代码下 4 项见血；⑧a-⑧d 开合记忆四项）。**该测试的 `waitUntil` 必须是 `load` 不能是 `networkidle`**——`/api/v1/events/stream` 是常开 SSE，networkidle 永不达标（2026-09-08 实测 30s 超时）。
- **侧栏宽度三处同口径 clamp + 命令式写宽度必须补拍**（2026-09-04「窗口缩到 820 后画布只剩 140px」事故）：① clamp 唯一入口 `clampChatWidth`（`Sidebar.tsx` 模块级）——下限 340、上限 = `max(240, min(760, innerWidth - 活动栏 56 - 画布最小可用 420))`，**装载（陈旧 localStorage 存值 760px 也照砍）/ 拖拽 / resize 三处共用**，漏一处就是「装载合格、一缩窗又回到裸存值」。② 一次内联写入不够：v2 会在自己下一次 render 里把「它记得的宽度」重新提交成内联 style，覆盖掉我们的 `style.setProperty("width", px, "important")`（实测 `--ws-chat-w` 已 344px 而 aside 仍 624px、body margin 跟着 624，画布被挤成 140）——`apply()` 写完必须补两拍重写（下帧 rAF + 400ms，与装载时 applyInitial 的双写同一手法），并用 `applyGen` 让新的 apply 作废旧的补写（拖拽期间每帧都在改，回灌陈旧值就是抖动）、`disposed` 防卸载后写 DOM。③ `endDrag` 被 `blur`（切窗口/失焦收尾）复用，但 FocusEvent **没有 clientX**：`innerWidth - undefined = NaN` 会把 `"NaNpx"` 写进 localStorage（下次装载只能靠 isFinite 兜回默认值）——按 `Number.isFinite(e.clientX)` 分流，失焦时读当前 `--ws-chat-w` 重新 clamp。④ 视口 ≲800 时 v2 自己换成**覆盖式**（body margin=0，不再为侧栏挖位），「画布右缘贴合侧栏左缘」在此档不成立：贴合断言必须 `margin > 0` 才校验，不能拿「画布被挤窄」给覆盖式建模。⑤ 叠在画布上的固定宽控件（左上工具条与其中写死 `w-52` 的搜索框）必须受画布宽约束——工具条 `left-2 right-2`、搜索框 `min-w-0 max-w-52 flex-1`，否则窄画布下搜索框不收缩，把末子项（画布设置齿轮）整个顶到侧栏底下。回归：`scripts/sidebar-width-reserve-test.mjs` ⑦ 组。E2E 时序坑：Playwright 改视口与页面 resize 事件之间有间隙，「等稳态」判据除 `var==实测宽度` 外必须再加「宽度已在 clamp 上限内」，否则 624/624 的陈旧态会让同源断言空过；反向验证 = 摘掉补写必报「侧栏宽度始终不稳定」（9s 超时），证明补丁不是为测试时序服务
- **底坞/面板绝不吐内部模型 id**：曾用 `imageModels?.find(...)?.label ?? imagegen.model` 静默回落，目录未到达/加载失败时把 `gpt-image-2-03` 当标签甩在用户脸上。现在三态各有说法（「加载模型目录…」/「出图模型已下架」/「模型目录加载失败」），后两态亮红、tooltip 指「出图设置」重试入口；按钮定位按 `data-track="dock.imagegen"` 不按文本正则（失败态不拼档位，正则找空会让断言空过）
- **聊天间距单一事实源 = 容器 gap（2026-09-05 间距走查）**：消息间距曾是三层叠出来的——用户行（Sidebar.tsx 自绘 `group flex justify-end px-1`；v2 原厂 `.copilotKitUserMessage` 在本工程 0 命中，globals 里那套 padding/圆角/字号是死配置已清，改气泡外观去自绘组件）零 margin + `.copilotKitMessages` 零 gap + Tailwind typography 的 `p{margin:1.25em}` 从 `.cpk:prose` **折叠出壳外**（上/下各 16.25px 幻影）——连发两条用户消息 0px 贴死、助手连发 36.5px、工具卡混排 24px，且消息边界(16.3)≈段内边界(16) 分不清哪段属于哪条回复。现 `.copilotKitMessages { gap: 20px }` 唯一说话：助手壳垂直 padding 归零、prose 首末子 margin 归零掐幻影、纯卡消息的空 prose `display:none` 不占槽、消息内多块（正文→卡、卡→卡）10px。两个 `:not()` 坑（实测各偷 10px）：`+` 组合器把 display:none 的空 prose 也算兄弟（纯卡消息的卡会多吃一个顶槽），且 `+ *` 会命中零高悬浮工具栏（每条文字消息底部凭空多 10px）——内块间距规则必须排掉工具栏。段内 16px 仍归 `space-y-4`，与消息边界 20px 可分辨。横向内缩 v2 `px-8` + 滚动槽 `scrollbar-gutter: stable both-edges` 共 43px/侧（420 侧栏正文只剩 335、一行约 20 汉字）收到 20px/侧（359）。回归：`node scripts/chat-spacing-test.mjs`（反向验证 gap 归零 → 间距全 0 见血；导航用 `load` 不用 `networkidle`——SSE 常开永不 idle）。**聊天表格排版 + 思考块 margin（2026-09-09 用户反馈「表格第一列贴边」）**：①助手消息的 markdown 表格走 Streamdown（`w-full border-collapse`，包在 `overflow-x-auto` div 里），v2 样式表把首列 padding-left 押成 0（实测 td padding `8px 8px 8px 0`）、表头无底色、行无分隔——第一列文字贴死消息左缘。修法 = globals `.copilotKitAssistantMessage table` 系列自给纸感排版：td/th 四边对称 `7px 10px` + 表头 `surface-2` 底色 + 行分隔线（末行去掉）+ 外框收敛到容器（`:has(> table.w-full)` 圆角边框 + `overflow-x:auto`，窄侧栏宽表横滚不撑破）。②「Thought for…」思考块（cpk:my-1，react-core 的 CopilotChatReasoningMessage）是**独立消息行**且自带 `margin:4px 0`，叠在容器 gap 20 上凑成 24px 双倍间距——globals 掐掉 `.cpk\:my-1` 自我 margin 归 gap 统一说话。③顺带删 `.ws-md` 整块死规则（56 行：文本/剧本卡 Markdown 预览 `MarkdownView.tsx` 在 d496f08e8 已删、全仓无消费方）。④spacing 测试 ⑤ 的判据同步改：v2 现在把 正文→工具卡 聚合进**同一条**助手消息，卡不再贴壳顶（上面有正文）——改为断言「正文→卡块间距恰 10px 或纯卡贴壳顶」。排查 CSS 命中的方法：CDP 遍历 `document.styleSheets` 打印带 padding 的表格规则（新规则 > streamdown 原厂 px-4 py-2）。**表格设计 review（同日六轮探测后的补强）**：① wrapper `scrollbar-gutter: stable`——横滚条原贴在最后一行文字上；② wrapper `margin: 6px 0`——表格与前后正文贴死（Streamdown wrapper 无 margin）；③ th **不加 sticky**（wrapper 不限高、纵向滚动发生在消息区层面，sticky 永不触发纯增层级风险，实测后撤）；④ 暗色主题实测过关（th/td/wrapper 全走 token 变量，主题切换自动适配）；⑤ 窄侧栏 344px 下 5 列宽表横滚正常、不撑破消息区、页面无横向溢出（模型可能拒答宽表，用注入同构 DOM 的探针法测极限）。回归：chat-spacing 10/10。
- **开关标签必须显示生效值、空态不装神、tooltip 不双写**（2026-09-04 UI 走查）：① 底坞「画风」按钮曾只写「画风」两字，用户完全看不出选没选（画风闸只在卡上错误提示里露一次）——现直显 `styleLabel`：预设名 / 「自定义画风」（tooltip 带正文前 60 字）/ 「未选画风」，未选态 tooltip 说清「资产设定图·分镜出图等视觉产物会被拦，文字流程不受影响」。② 空画布不渲染小地图（`minimapVisible && nodes.length > 0`）——一块空画布挂在右下角像渲染故障。③ v2 的 `labels.welcomeMessageText` 在本工程是**死配置**（WelcomeScreen 闸在 `!hasExplicitThreadId`，而 `agent-provider.tsx` 传了 threadId 做多会话），已删不留；空态身份由 `suggestionView` 槽位的 `EmptyStateSuggestions` 自己说（一句「我能干什么」+ 最近会话 + 建议词，`hasMessages` 后隐藏）。④ 同一标签上 `title` 与 `data-tip` 双写会叠出两层浮层（浏览器原生 + 自绘），只留 `data-tip`（全站同标签双写已扫成 0，勿回退）
- **对话轮次快速索引（juben TurnLocator 范式，2026-09-07 移植）**：消息区右缘一条竖向圆点轨（每点=用户一轮发言，末点加宽 accent）+ 悬停/聚焦展开「对话轮次」标签面板（18 字摘要+轮次号，juben compactUserPrompt 同口径），点击 scrollIntoView 跳转 + accent 描边闪圈（globals.css `ws-turn-flash`）。组件 `components/copilot/TurnLocator.tsx`，三个关键决策：① **锚点清单来自 `langgraphAgent.messages` 订阅而非查 DOM**——v2 消息列表 >50 条走虚拟化会卸载远处消息，DOM 不全；瞬时 `progress_*` 与系统代发的「（任务通知）」不算用户轮。② **rail 经 portal 挂 body + fixed 贴「可视滚动区」右缘**：v2 没有落在消息区内的槽位，滚动容器是**无类名 DIV**（类名不稳定不能当选择器）——解析法 = 从 `.copilotKitMessages` 向上爬到第一个 `scrollHeight > clientHeight + 2` 且 `clientHeight ≤ innerHeight×1.2` 的祖先（高度上限排除内容级包装）。**视口元素必须 pin 住（2026-09-07「rail 上下跳」事故根因）**：爬升判据在流式重排帧里会瞬时翻脸——Streamdown 重解析流式消息时内容瞬时塌陷，真滚动容器在该帧不满足判据、climb 落到内容级包装（rect 高数千 px）上，region 随之爆炸再翻回、点列被甩上甩下（实测 rail 在 -193/2855 与 93/844 间翻转）；真容器身份稳定（流式全程 isConnected），`vpRef` 解析一次认到底、断连（v2 重挂）才重解析。**同款爬升判据别拿去当测量器**——探针曾用它采样 scrollTop 得出「视图 0↔300 弹跳」的测量幻影，追了三层（贴底库 clamp/scroll anchoring/原生 scrollTo 全排除，setter 打桩零写入）才靠「pin 引用 vs 实时 climb 对照」翻案：视图从没弹，跳的是测量和 rail 自己。③ **跳转前在滚动容器上合成一次向上的 wheel**（`escapeStickToBottom`）：use-stick-to-bottom 只认 wheel 向上为用户解除贴底的意图，程序化 scrollIntoView 它不认，流式中跳转会被贴底动画拽回底部。④ **跳转两段式**：目标在 DOM（<50 条常态）直接跳；被虚拟化卸载时按轮次序位比例估滚 → 200ms 后挂载了再精跳。⑤ **scroll-sync 当前轮指示（2026-09-11 补）**：滚动位置反过来标当前轮（滚到底=末轮实心 accent、滚到顶=首轮实心且末轮空心环，悬停面板同步高亮该行），配 Alt+↓/Alt+End 键盘跳轮。几何订阅：RO（pin 的视口本体+list+aside+body）/ resize / aside transitionend / 2s 轻量轮询，z-1250 照档位表。用户气泡的 DOM 锚 = Sidebar.tsx `UserBubble` 根上盖 `data-turn-id`（通知/瞬时消息不盖）。**横向位置（2026-09-11 用户反馈「挡字 + 有点靠下」）**：右侧开 42px 专用槽（正文右缘 r-42、20px 滚动条带、圆点落 r-22），**别再用「左右各 20px 内缩 + 让开滚动条 +24」那套常量错位写法**（旧版圆点稳定叠在正文最后一列 14px 上）；纵向基准是「可读带」（输入浮层之上）而非滚动视口整高，并订阅输入条 RO 跟随其高度。回归：`node scripts/turn-locator-test.mjs`（自建项目+合成 10 轮会话：点数含通知过滤/贴边/面板/跳转滚动/闪圈/回底 + scroll-sync 当前轮实心·末轮空心环 + Alt+↓/Alt+End 轮次导航 + 横向不压正文·不压滚动条带 + 纵向居中可读带，27 项，不跑 LLM 约 30s）
- 画布基础交互（对标 Figma）：**拖卡任意区域=移动整卡，图片区也是**（图片本体 `draggable={false}`+`pointer-events-none`，viedeo-workflow 范式——曾因图片挂 HTML5 拖拽（拖图=设为引用）+外层 nodrag 把节点拖动整个屏蔽，用户只能捏着卡边缘拖）。「拖出去当引用」收拢到图片区 hover 浮现的悬浮抓手（`MediaDragGrip`，双载荷同时喂 PromptBar（`x-ws-node-ref`）与聊天输入（`x-wingsight-node`），pointerdown 拦截防误触节点拖动）；E2E 不能按 `<img>` 定位点击/悬停（pointer-events-none 会被 Playwright 动作性检查拒绝），用容器 `div[title="双击：视口聚焦本卡"]`。左拖空白=框选（`panOnDrag={[1]}` 是前提，另配 `selectionMode=Partial`）、中键/Space+拖/滚轮/双指滚动=平移（`panOnScrollSpeed={1}` 物理跟速）、⌘+滚/捏合=缩放（`zoomOnScroll=false`+`panOnScroll` 常开；`zoomActivationKeyCode` 默认 mac=Meta，按住即把 wheel 处理器重绑回 d3 缩放）、Alt+拖卡=原位克隆（store 的 `altDragClone` 改道拖动帧，注意"先改道后清表"）、双击空白/Tab=「添加节点」选择器、**双击图片卡=视口聚焦本卡**（fitView 单节点，zoom 钳 0.78–1.25；单击图片只选中不弹大图，灯箱走角落 ⌕ 按钮——竞品五家共识「预览是显式动作」；CardShell 工具条 offset 随卡片屏幕位置钳制防贴顶出屏，视口量测用 xyflow useViewport+双 rAF）、右键空白=上传/添加节点/撤销/重做/粘贴菜单（reference 产品范式）。滚轮**不做鼠标/触控板设备判定**（按 deltaY 量级猜设备的启发式会被触控板快扫/惯性步进误判成鼠标轮，平移中途误缩放；`onWheelCapture` 只留 nowheel 动态化——可滚动容器滚内容，不可滚动的现场摘类放行画布平移）。右键拖不做平移（macOS contextmenu 在 mousedown 即触发，会和右键菜单打架）。库级陷阱 2：RF 的 `onPointerCancel` 不清 `userSelectionRect`，pointercancel/漏 pointerup 会卡死框选——`CanvasView` 的 `SelectionGuard` 窗口级兜底，勿删
- 画布快捷键（`CanvasShortcuts.tsx`，capture 阶段监听——**必须 capture**：xyflow 节点 a11y 会把聚焦卡上的 Enter 当作"带 multi 键的点击"切换选中（multiSelectionKeyCode 含 Meta），bubble 阶段读到的选区已被清空；⌘Enter 分支另加 stopPropagation 拦在它前面）：⌘0=适应画布（对齐竞品语义，⇧⌘0=复位 100%）、⌘G/⇧⌘G=成组/解组、⌘L=连线（恰好选中两张，去重）、⇧F=整理（有选区整理选区）、⌘Enter=生成选中卡（出图类、恰好单选；空提示词=按标题正文重生成；受画风闸与图生图参考闸约束）、⌘K=画布导航命令面板（呼出 OutlinePanel 并聚焦搜索，↑↓/Enter 定位）、Tab=视口中央弹「添加节点」、方向键微调、⇧E 连线显隐、`?`=速查表（`ShortcutsModal.tsx`，数据驱动与绑定一一对应，**改键两处同步**；底坞键盘按钮经 `OPEN_SHORTCUTS_EVENT` 呼出）。V/H 工具模式不做（直接操作范式，左拖=框选无模式状态）
- 卡左右**加号手柄**（建上下游卡菜单）：**屏幕空间恒定尺寸**——浮层/建卡菜单/命中半径按 `1/zoom` 反向补偿（`CardShell` 的 `handleScale`，钳 4×），任意缩放档可读可点（zoom<0.5 曾被 LOD 整档藏掉，96 节点总览时"画布上没有加号"事故；菜单不补偿会缩成 ~24px 不可读）。菜单**必须挂 `.ws-node` 根**——挂在 `.ws-card` 内会被其 `overflow:hidden` 整个裁掉（菜单位于卡外缘，曾"进 DOM 但不可见"双死）。命中半径放大走根节点 `--ws-hit` CSS 变量喂 `Handle::before` 的 inset：伪元素不改锚点盒尺寸，**不触发 xyflow 手柄重测量**（直接 scale Handle 会让每次缩放全画布边重渲）；连线端点取锚点中心，补偿不动中心无影响。`connectOnClick={false}`（全局）：手柄**点击语义归加号菜单**（pointerdown/up 位移 <4px 判定），拖拽才连线（走 mousedown，不受影响）；Handle 另挂 onClick stopPropagation 防"点加号顺手选卡弹工具条"——低缩放贴顶档钳制下移的工具条会盖住加号与菜单。回归 H 组：`scripts/image-node-ops-test.mjs`
- **flow 内嵌代码禁用 PEP 701（f-string 嵌套同引号）**：服务器 langflow venv 是 Python 3.10、本地是 3.12——`f"{d.get(k, "图")}"` 这类嵌套双引号本地全绿、服务器加载即「f-string: unmatched '('」整 flow 500（2026-09-08 事故：服务器全线出图 500，自由生图五模型批次实锤）。f-string 内层一律单引号；改 flow 后可用 `cd agent && uv run --python 3.10 --no-project python -c "import json,ast; ast.parse(next(n for n in json.load(open('../agent/flows/asset-imagegen.json'))['data']['nodes'] if n['id']=='BatchAssetSheet-img02')['data']['node']['template']['code']['value'])"` 做 3.10 语法预检
- Langflow 的 SSRF 白名单在 `langflow/.env`（仓库内，随 fork 走）的 `LANGFLOW_SSRF_ALLOWED_HOSTS`（含 dmxapi/amazonaws/deepseek/volces 等——volces 是 seedream 系出图产物 URL 的落域，缺了在保存图片一步报错）——出图报 Broken pipe / blocked IP 时先查这里，改后需重启 langflow
- 出图模型/分辨率/画幅切换：目录唯一事实源 `agent/models.py`（DMX 实探验证，双路径 generate+edit 通才收录；档位按 flow 真实计算尺寸探验——16 像素网格，16:9 4K=3840x2160；`max_references` 参考图上限——seedream-5-pro 融合通道实测 10 张，其余 edits 通道保守 4，桥接按所选模型校验明报、skills 按实际张数注 reference_count tweak；`aspects` 画幅枚举——主流 6 档，seedream-5-pro responses 通道像素上限收掉 21:9，`resolve_aspect` 预检；**`qualities` 质量档——只有 gpt-image 2.5 系声明 low/medium/high/xhigh/max、`default_quality=high`，其余模型不设该字段**），`GET /models/image` 下发。三级参数：项目级默认（`meta.imagegen`，底部坞「出图」）→ 卡片级覆盖（`data.gen`，PromptBar 模型 chip / 分镜卡出图设置写入；请求体里镜头级 params 赢过请求级）→ 聊天工具 model/resolution/**quality** 入参。画幅：资产/图片卡走 `data.gen.aspect`（PromptBar 模型 chip popover「画幅」宫格；空=自动——带参考图吸附首位参考图比例 `resolveAutoAspect`、无参考图按 flow 类型默认幅面，快照 genShot.aspect 供补出）；画布摘要与设定图重试消息带卡上画幅、聊天出图工具吃逐资产 `aspect` 字段（聊天重出不丢幅面）；分镜画幅仍 `data.aspect`（ShotGenSettings，6 档）；每镜候选张数 `data.genCount`（1/2/4，候选聚合成行图卡的 imageUrls 变体，任务 rid 带 `#k` 后缀）。非法组合整批 400 点名镜头；seedream 4.5 有最小像素约束（4:3 幅面 2K 档不够，见 models.py 注）。**默认模型 `gpt-image-2.5-sunburst` + 质量档（2026-09-11 用户拍板：cdx 渠道下线，为全档全画幅与质量档让路）**：`DEFAULT_MODEL_ID` = gpt-image-2.5-sunburst（1K/2K/4K 全档全画幅受控，实测 1K ¥0.21 / 2K ¥0.28 / 4K ¥0.50，edits 每多 1 张参考约 +¥0.06）；质量档走四级——项目级（底部坞出图设置「质量」行，切模型联动、chip 偏离默认显档）→ 卡片级 `data.gen.quality` → 请求 params，**卡片级 gen 缺 quality 继承项目级**（`startShotImageJob` 先查卡模型支持防 400）；`resolve_imagegen_params` 对不支持的模型传 quality 400 明报；flow `BatchAssetSheet-img02` 的 quality 下拉**只在 OpenAI images 分支透传**（gemini/responses 原语无此参数）；聊天工具 `generate_asset_images`/`generate_free_image` 收 quality 入参（LLM 可响应「最高质量出图」）。存量 `gpt-image-2.5-sunburst-cdx` id 由 `lib/imagegen.ts` 的 `RETIRED_IMAGE_MODELS` + `sanitize.migratedGenModels` 一次性迁移（幂等计数；`saneGen` 判等陷阱已重构——迁移改写 model 后旧判等会把合法卡级覆盖误判 null 丢弃）。21:9 4K（5040x2160）超官方最大边 3840，该组合由上游报错点名。**`input_fidelity` 已从 lfx edit 分支撤除（2026-09-11，与 cdx 下线同轮）**：2026-09-07 曾硬传 `input_fidelity="high"`（当时结论：缺省 low = 整图重渲染不保输入细节、改图观感「脏污」，high 才保脸/纹理/文字），但 DMX 2.5 官方参数表无此参数——无 cdx 渠道再传直接 400 `image_generation_user_error`，不撤则切默认模型后**带参考出图全灭**；将来渠道若重新支持保真改图，按模型白名单分支引入，**别再无条件硬传**。改 lfx 组件源码须重启 langflow（模块缓存），flow 内嵌 code 经 `from lfx...image_generation import generate_image` 引用同一函数，无需 PATCH flow。**gemini 无损输出 `imageOutputOptions.mimeType=image/png`（2026-09-07 补，flow gemini 原语 generationConfig）**：gemini 缺省吐 JPEG——改图产物落卡后作为下次改图的「本卡原图」参考，有损格式代际累积（改三轮噪点叠三轮）；显式要 PNG 断链。**上游输出不评判（2026-09-07 用户两次拍板的最终口径「尊重用户的选择：如实执行用户的指令就行了」）**：选型是用户的权利，管线的职责边界 = 把用户选的模型/档位/画幅**如实透传**，上游出什么就是什么——不做降档检测、不出 ⚠ 提示、不引导换通道（曾做过量尺寸明报机制（`_resolution_note`+note 透传+toast），用户裁决撤除，勿回魂）。背景知识仅作排查参考：gemini 编辑场景 `imageSize` 被上游静默降 1K 是已知 bug（js-genai #1461 等在案报告，客户端无参可修）
- 文本模型平台化多目录（剧本/分镜表/拆解/提示词优化等 LLM 文字生成）：同文件唯一事实源（TEXT_MODELS，每条目带 `provider` 字段=langflow 一等命名平台），`GET /models/text` 下发。平台由 fork 扩展包 `langflow/src/bundles/platforms/` 声明（加平台只改 extension.json+重启 langflow，见 bundles/platforms/README.md；setup-langflow.sh 从 .env.local 自动种子平台变量）：glm-5.3-flash/glm-5.3 → BigModel=智谱官方（BIGMODEL_BASE_URL/BIGMODEL_API_KEY）；deepseek-flash → DeepSeek 平台（DEEPSEEK_BASE_URL/DEEPSEEK_API_KEY；2026-09-07 起指向官方 api.deepseek.com——此前种子曾指向智谱 coding 网关，该网关只有 glm 系模型，DeepSeek 平台一用就 500「modelCode 不存在」，setup-langflow.sh 现从 .env.local 读 DEEPSEEK_BASE_URL 兜底官方）；gpt-5.6-luna/gemini-3.7-flash/claude-sonnet-5 → DMX（DMX_BASE_URL/DMX_API_KEY）。旧 "OpenAI"/"OpenAI Compatible" 劫持命名及 OPENAI_*/OPENAI_COMPATIBLE_* 全局变量已下线。**目录默认 deepseek-flash**——卡片级覆盖 `data.textModel`，chip 在**选中剧本/分镜表/文本卡后的下方输入条**（空=目录默认 deepseek-flash，分镜/剧本/拆解/提示词优化四条路径 agent 侧注入；剧本卡「拆分镜表」会把选择带给新建分镜表卡），端点透传：分镜表生成 `model` / 拆解 `text_model` / 提示词优化 `model` / 文本撰写 `model`（`/text/rewrite`，文本卡/剧本卡「撰写」直连管线——结果预览采用才覆盖正文，不再经聊天主循环），非法 id 400 明报。注入走 tweaks **逻辑组件名**（如 LanguageModelComponent）：`run_flow_blocking` 先经 `GET /api/v1/flows/{id}` 把键解析成真实节点 id（精确 id/display_name 透传 → `键-` 前缀唯一匹配 → 零/多命中明报不静默）——langflow 只按节点 id/display_name 精确匹配 tweaks 键，直接透传组件名曾让**所有文本模型/温度注入静默空转**（2026-09-02 修复）；`models.text_model_tweaks()` 同时注 model_name+provider（通道路由）。**全站 LLM 默认 deepseek-flash**：各调用点空模型一律落 `DEFAULT_TEXT_MODEL_ID` 注入（分镜/拆解/提示词优化/文本撰写/深度调研/选题池/剧本审查/指令合成），flow 内出厂模型不再参与；卡片级覆盖 `data.textModel` 照旧。提示词优化已拆双 flow——扩写态走原生链同此，看图反推模型在 flow 的 model_name 字段换（gpt-5.6-luna 视觉经 DMX）。聊天改分镜表不经此；**聊天主循环走 deepseek-flash**（2026-09-08 用户拍板换模型+开推理，2026-09-11 随全站统一改名：`AGENT_MODEL`=deepseek-flash、`AGENT_BASE_URL/AGENT_API_KEY` 走 **DeepSeek 官方**（`DEEPSEEK_*` 同值，模型老家直连省一跳；DMX 亦可用但带「reasoning_effort+工具同发 400」老坑）、`AGENT_THINKING=1` → graph.py 发 `extra_body thinking:{type:enabled}`（实测该通道 200；该模型**不传参数默认也思考**——三态探针 reasoning_content 均非空）、`AGENT_VISION_ENABLED=1`（128×128 真图端到端识色通过；**1×1 像素图会让模型答错色**，视觉回归夹具别用退化图）；GLM 系 thinking 特判按模型名自动失效）。DMX 余额可查：`GET /api/v1/dmx/balance`（欠费排查先打它）
- 聊天画布摘要走 AG-UI context 通道：useCopilotReadable → RunAgentInput.context，桥接层（上游不消费 context）由 `agent/patches/agui_context_injection.py` 写入图状态键 canvasSummary——**venv 重建后必须重跑该脚本**；绝不能注入为 message（消息会进 checkpoint/UI/持久化，泄露成聊天气泡）；v2 core 的 run 包装每轮会用 {messages, copilotkit} 整体替换 agent 状态，agent.setState / setProperties 都不是有效下发通道
- Next 代理默认对响应做 gzip——对上游 AG-UI SSE（text/event-stream）也压，gzip 缓冲把整个流攒到结束一次发给浏览器，打字机效果失效（回复"一次性吐出"；node/curl 直连无 Accept-Encoding 不受影响，故探针测不出来，必须用浏览器内 fetch 测）。`next.config.ts` 已 `compress: false` 全局关闭（自托管+隧道无带宽压力）
- DMX 的 gpt-5.6-luna 上游默认注入 reasoning_effort，与 function tools 同发即 400（"Function tools with reasoning_effort are not supported"）——chat 主循环非 GLM 系必须显式 `reasoning_effort="none"`（graph.py thinking_kwargs 二选一：GLM 系发 thinking:enabled，其余发 none）；症状为浏览器端 "network error / agent_run_failed_event"，agent 日志可见 DMX 400 traceback
- node 集成/E2E 测试直连 agent API 需认证（`AUTH_ENABLED=true`）：登录 `POST /api/v1/auth/token`（form 表单，admin + `.env.local` 的 `AUTH_PASSWORD`），脚本侧 fetch 带 Bearer、Playwright 侧 `context.addInitScript` 预置 localStorage `wingsight_studio_token`（先例 `scripts/shotlist-resume-compose-test.mjs` 头部）
- `references/` 是外部参考项目（已 tsconfig exclude + gitignore，勿编译勿提交）；`agent/data/`、`agent/static/assets/`、`agent/static/thumbs/`、`logs/` 均为运行时产物。图片展示分两级：小尺寸一律走 `/agent-service/thumbs/{stem}.webp`（落盘时 ffmpeg 产 512px webp，`agent/thumbs.py`；缺失时端点现场自愈生成，存量可跑 `cd agent && uv run python backfill_thumbs.py` 预热；前端 `lib/asset-thumb.ts` 的 `assetThumbUrl` 换算），Lightbox 放大/下载才用原图；`/assets` 与 `/thumbs` 均打 `Cache-Control: immutable`（文件名随机 hex 内容不可变），裸 FileResponse 不做 304 协商，别指望 etag
- 远程访问经隧道（bore/ddnsto），`allowedDevOrigins` 已放行，改访问域名需同步 next.config.ts
- xyflow 12.11 的 `fitView` prop **不是只在挂载时生效**：StoreUpdater 监听它，prop 值一旦翻转就置 `fitViewQueued` 重新 fit（空画布建第一卡会怼到 maxZoom）。要"只挂载时 fit"就用 `useState` 初值冻结（`CanvasView` 的 `fitOnMount`），勿写回随状态变化的表达式
- xyflow 新节点首帧带 **`visibility: hidden`**（等 ResizeObserver 测量出尺寸才翻 visible），此窗口内对节点内元素调 `focus()` **静默失败**（无报错无焦点事件）。要"建卡即输入"必须逐帧重试到 `document.activeElement` 落位（`nodes.tsx` 的 `focusWhenVisible`），裸 `autoFocus`/mount effect 一次 focus 都会丢
- 卡内就地编辑（`Editable`）必须**非受控**：xyflow 内部节点副本晚 zustand 一拍，受控 `value` 会在每次击键后让 React 强写一次"旧值"进 `.value`——刚打的字被抹掉再恢复、**光标甩到全文末尾、中文 IME 组合被毁**（用户表现：只能输英文/中途加字跑到最后）。Editable = `defaultValue` + 未聚焦守卫回写（外部改值：AI 撰写覆盖/版本恢复/撤销/agent 改卡都走 store，未聚焦时 effect 回写 DOM；聚焦中 DOM 即事实源，失焦 commit 落库，聚焦期间的外部改值由失焦 commit 以用户文本胜出）。打字流 onSave 带 `{history:"coalesce"}`——`updateNodeData` 的 800ms 窗口只入栈一次撤销快照（逐字符全画布 structuredClone 会撑爆撤销栈）；onKeyDown 忽略 `isComposing`/`keyCode===229`（组合中 Enter/Esc 是候选确认/取消）。回归：`scripts/card-editing-test.mjs`（CDP `Input.imeSetComposition` 真实 IME 时序；其 selectionStart/End 是相对 replacementStart 的偏移，别按绝对坐标断言）
- Next 同源代理对上游 socket 有**空闲超时**（`experimental.proxyTimeout`，2026-09-02 起设为 10 分钟；此前默认 ~30s，拆解/技能 flow 中段静默一过就把 SSE 流掐断，前端报 network error）。流式响应持续出数据会重置计时；**非流式的长阻塞请求**（>10 分钟的直连能力）仍须做**异步任务 + 轮询**（先例：`POST /storyboard/images` 返回 jobId + `GET /storyboard/images/{jobId}`），不要阻塞等 flow 跑完
- 改 `agent/graph.py` 系统提示后必须重启 agent（uvicorn 无 --reload）；改 langflow 自定义组件源码后须重启 langflow（模块缓存）
- 改 `langflow/src/frontend/` 源码后必须重新构建前端，否则改动不生效（后端伺服的是 `src/backend/base/langflow/frontend/` 里的构建产物，非源码）：`cd langflow/src/frontend && npm ci && npm run build && cp -r build/. ../backend/base/langflow/frontend/`。踩坑实录：ab59951 修了 axios baseURL 双前缀（`/langflow/langflow/api/...`）但没重建，浏览器拿到旧 bundle 后所有 API 都落到 SPA HTML 兜底（200+text/html），免登永不成功、`/langflow` ↔ `/langflow/login` 死循环。诊断口诀：web.log 里看浏览器实际请求路径有没有双 `langflow/`、状态 200 但 content-type 是 text/html 的"假 API"；登录页 `/langflow/login` 本身不触发免登（免登只挂根路径 AppInitPage），排查时直接访问 `/langflow` 而不是 login 页；`manifest.json 401` 是浏览器不带凭证抓 PWA manifest 的固有噪音，无关紧要
- **上传文档落卡要认得出 · 年代两个入口 · 考据出口别点名工具（2026-09-10 八仙饭店项目复盘）**：①**资料卡标记**：上传文档自动落的 note 卡带 `data.docCard=true`，画布摘要渲染成 `[资料卡] 《标题》（上传文档·正文已落卡 N 字，read_node 可取全文）`——此前只是普通 `[文本]` 行，agent 看着那张卡说「画布上目前还是空的」。**先别急着怪竞态**：`scripts/doc-card-context-test.mjs` 实测「落卡后立刻发送」与「隔 3 秒发送」摘要都带卡（无竞态），真因是呈现不可辨识。另：剧本卡（制作载体）与资料卡（原文留档）**并存是设计**，不是重复建卡；②**年代询问两个入口**：`promptEraIfNeeded()`（nodes.tsx，会话级跳过集）在**发起批量调研**与**拆完资产**两处都调——只挂调研入口会漏「拆完直接出图、从不点调研」的用户（生产 23 项目 era 全空即此类），era 空则跨项目考据复用永不生效；③**宪法决策原则 1 的考据出口删掉工具名**：原句带「（research_asset_references，…）」把出口指向按资产的参考图调研，整部戏的资产是成批的、宪法任务路由早已写「≥3 资产先走考证大纲」——**删掉工具名后 S3 场景真的自己走了 `get_research_material → propose_research_outline`**。A/B 实录（重要教训）：同一处**加了理由的长版本**（「一部戏的资产是成批的…别把出口说成『一轮参考图调研』」）复跑把 intent-routing 打到 **12/14**（S3 抢答 `start_deep_research`），改回**只删不加的最小版**才 **14/14** —— **宪法改动的安全形态是「删冗余」而不是「加解释」**，理由永远放手册不放宪法
- **`start_wingsight.sh stop` 会带走 langflow，`start` 不拉它（2026-09-10 假回归事故）**：stop 按端口收进程把 7860 也收了，而 `start` 只起 agent+前端——于是 langflow 悄悄不在，拆解/分镜/调研/出图全 `httpx.ConnectError: All connection attempts failed`，表现为「agent 行为回归」（intent-routing 11/14：轮2 只建 2 张资产卡、S3 连试三次 start_deep_research 失败、S4b 冒出「服务端说画布上没有资产卡」）。**跑真 flow 的回归前先 `./start_wingsight.sh status` 看 langflow 那一行**，缺了就 `./scripts/setup-langflow.sh`（幂等：健康即跳过、按名字跳过已存在 flow，`.env.local` 的 flow id 不会被改写——实测 diff 无变化）。另注意 **Bash shell 自带 HTTP(S)_PROXY/ALL_PROXY**，本地探测一律 `curl --noproxy '*'`，否则 127.0.0.1 也会走代理拿到 000 误判服务没起
- **出图/分镜靠检查（2026-09-10 资产流水线对齐 juben）**：①**服饰建卡判据**（`asset-decompose-costume.json`）= 近景及以上能看清服装主体 / 承载时代身份信息，命中任一即建，**「复用次数少」不是不建的理由**（旧口径「支撑全片造型一致性」会漏掉只在一场戏里近景出现的囚衣礼服）；②**分镜行「场次」字段**（`scene`，地点·时间，同场连续戏同名、换场换名）是相邻镜头连贯参考的配对依据——出图时同场上一镜的镜头图自动进参考（`type=shotref` 职责，锁光线/色调/陈设/造型），跨场不配；参考上限 5 张，资产身份参考优先。链路全链同改：`_parse_shot_rows`（skills.py）→ 行控件 chip → `ShotRow.scene`（store.ts）→ `normalizeRows`（ops.ts）→ canvas_ops 工具描述；③**名单外资产名不再静默丢弃**：`run_storyboard_flow` 返回 `(rows, missing)`，聊天工具返回串列「画布缺的资产」教 agent 补建、HTTP 端点带 `missingAssets`、前端出图前 toast——这是**换装服饰/关键道具漏拆的唯一发现回路**（旧代码 `if a in roster` 直接扔掉）；④**近景造型闸**：引用的角色有 Look 图、景别 ∈ {大特写/特写/近景/头肩景}、解析结果仍是定妆照时，出图前弹窗点名「有造型图但本行未指定用哪套」（远景不提示）；⑤**补考据软失败留痕**：真实题材补考据失败不再无声——图卡 `genShot.researchNote` 记「未考证」、分镜表底栏统计「未考证 N」、出图返回串点名（juben 未考证标记语义）；⑥**手改手册前先比部署实况**：`langflow/src/lfx/.../batch_asset_sheet.py` 与部署 flow 的内嵌 code **已漂移四处**（character 的「以本图为唯一事实源」、scene 的「不继承多格排版」、`reference` 职责、`shotref`）——**flow JSON 是运行时真相**，改职责/版式一律改 `agent/flows/asset-imagegen.json` 并 `./scripts/update-flow.sh asset-imagegen.json`，别改那个 .py；⑦E2E 浏览器脚本（`__wsCanvasStore` 钩子）**要求前端跑 dev 模式**（`./start_wingsight.sh dev`），生产模式下钩子不存在、脚本会以「Cannot read properties of undefined」假失败
- **考据是两条路不是一个选项 · 待补口径按参考图算（2026-09-11 091101 武则天项目事故）**：用户问「这个项目的调研情况」。实况——项目 `03741198980d`（本地库「091101」《凤临天下—武则天》，52 资产 / 13 角色 11 场景 13 道具 15 服饰）**参考图候选表 0 行、画布 0 张参考卡、没有任何参考图批次任务**；而 6 个主题的**文字**考据全部 done、52 张卡的 `researchBrief` 全在、重出的「祈福殿」图 `finalPrompt` 里「考据依据」段确实注入了。全会话 40 条消息里「参考图」三个字一次没出现。诊断：**手册把「考证大纲」与「research_asset_references」写成了二选一**——`script-to-assets` 第 3 站原文「一批资产（≥3 个）走考证大纲，单个/少量资产走 research_asset_references；**完成后系统自动采纳每资产 top-3 参考并物化成参考卡**」，分号把只属于图路的承诺挂到了两条通道后面；`asset-aware-generation` 第 3 条同一二选一（且「单个资产的参考图」暗示批量不走）；`graph.py` 的 `research_asset_references` 工具描述自己写着「本工具服务单个/少量资产的参考图需求」（**52 资产被工具级明确排除**）；宪法任务路由「≥3 个 → 先走考证大纲……单个资产的参考图仍走」。「先走」暗示后面还有一步，但四处都没说那步是什么。**架构铁律里那句「文字考据走主题级，参考图仍走资产级」从来没进过任何一本 SKILL.md**——它是给开发者看的设计判据。于是 agent 严格按手册执行了大纲，再按手册的「自动采纳」承诺以为参考图会自己长出来。四处同改（两个维度、各走一步、**大纲跑完 ≠ 这一站做完**、图路永远按全量发起，`script-to-assets` 第 3 站改成正交两行表格 + `asset-aware-generation` 写成「批量不例外」+ 工具描述删掉「单个/少量」+ 宪法加一句「参考图=research_asset_references 全量」），`real-documentary` 考据节标题改「文字考据的单位是时代」并新增「与参考图的分工」小节（**「不进大纲 ≠ 不做考据」**——只影响一张卡的资产也要走图路）。**配套口径修复（报告卡）**：旧「待补 = 无参考图 **且** 无考据」把「有文无图」整类漏掉——091101 头部显示「覆盖 0 个 ｜ 缺参考图待补 0 个」自相矛盾、连「补调研」按钮都不出现（这是 6eea84f「冯太后待补 70」修复的反向盲区：那次治的是「有图无文」）。口径定死 **待补 = 没有已采纳参考图的资产**（报告的轴是参考图，文案「缺参考图待补」本就该这么算），「覆盖 + 待补 = 资产数」算术自洽；有文字考据只缺图的行内标注「已有文字考据，只缺参考图」（`missing_ids` 判据）；零合并「上次失败：…」后缀（`；` 连接）。顺修同类不自洽：「覆盖 M 个」与「已采纳 X 张」都改为**只数画布资产**——`adopted` 里可能有「卡已删、候选仍采纳着」的孤儿节点（`n_gone` 夹具），算进去就成了「说 5 张只列 4 张」。回归：`cd agent && uv run python test_ref_report.py`（81 项：头部三口径 + 算术不变量 `覆盖+待补=资产数` 正则校验 + 有文无图进待补并标注 + 孤儿采纳不进覆盖）、`node scripts/ref-outline-api-test.mjs`（29 项，A8b/A8c 钉「有文无图」形态）、`node scripts/ref-report-reconcile-test.mjs`（34 项，A3c/B5b）、`node scripts/intent-routing-test.mjs`（新增 **S6 考据两条都走**：不点名走哪条时 `research_asset_references` 与考证大纲调用必须同时出现——091101 形态的常驻回归）。**教训**：手册里「A 或 B」的写法只要对应两条真实不同的产物通道，模型就会二选一——**两个维度必须写成「两条都要」，不能写成「按规模二选一」**；而「完成后系统会自动 X」这类承诺句必须紧贴产生 X 的那个工具，写在分号后面等于给了另一条通道一个假承诺
- **考据缺口的两处运行时信号（2026-09-11，紧接 e0fcc67 的机制层收口）**：文案改对只是第一层——**措辞正是这次出问题的层，保障不能继续依赖措辞**，所以把「这一站做没做完」变成 agent 每轮都看得见的东西。①**画布摘要**（`lib/canvas/store.ts` summarizeCanvas）：头部告警「⚠ 参考图缺 k/N 个资产（可 research_asset_references 全量补发起）」+ 资产行尾「⟨缺参考图⟩」逐卡标记。「有没有参考图」按**参考卡连线**判定（`refSource="research"` ↔ 资产四类卡，方向以物化布局为准也认用户手连的反向）——与出图参考通道同一事实源，不另算一套口径；**门控**：项目没有任何考据活动（无 `researchBrief`、无参考卡）时不标，否则动画/架空与尚未开工的项目会被催着做考据。②**出图前核查**（`skills.generate_asset_images`）：注入参考之后清点本批载荷**实际带的** `reference_images`，有缺口就在返回串**首行**插「⚠️ 参考图核查：本批 N 个资产里 M 个不带实物参考」+ 点名（前 6 个 + 等 N 个）+ 补法（全量发起 / 报告卡「补调研」）。放首行是因为 52 行逐张结果会把尾部说明淹没——091101 正是「一路顺利、没人提参考图」。虚构题材不提示（口径上就不做考据）；**只提示不拦**（用户要出就得能出）。工具描述（`_GEN_ASSETS_DOC`）与 `asset-aware-generation` 手册同步写了「看到核查报告要如实告诉用户缺参考、给补法，别把照着文字考据出的图当正常结果交付」。**顺修摘要降级逻辑（真 bug）**：本节注释与 `context-summary-test`「超预算先全省正文」写的都是「先降正文、仍超从尾部收」，实现却是**先丢行、正文留着**——尾部整卡消失而前面的卡还拖着一行正文，正好相反（该断言长期假红：30 张长正文卡的夹具在正文截断改成 24 字后**根本顶不爆 2000 预算**，断言名字喊着超预算而摘要没超）。现在先试带正文、装不下（丢过行）就改试省正文版，**只在省正文版丢行 ≤ 带正文版时采用**（严格不更差）；夹具同步加到 60 张让降级真被触发（行数断言 34→64，含连线行）。实测收益：091101 真实画布（59 节点/52 资产）在 2000 预算下可见资产行 17→24，头部告警恒在不受预算影响。回归：`pnpm dlx tsx scripts/canvas-read-channel-test.mjs`（79 项，新增 6：零考据活动不催/091101 形态告警 3/3/逐卡标记 3 行/非资产卡不标/有参考不标计数降/反向连线也算覆盖）、`node scripts/context-summary-test.mjs`（8/8，修红）、`cd agent && uv run python test_asset_research_brief.py`（132 项，新增 5：整批无参考明报/点名/首行/有参考不报/虚构不报）。**注意**：浏览器侧摘要断言（`__summarizeCanvas`）**要 dev 模式**（`./start_wingsight.sh dev`），生产模式下钩子不存在、会以「not a function」假失败
